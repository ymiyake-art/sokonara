# ソコナラ 引き継ぎ（2026-07-02・最新）

> 新チャットはまずこれを読む。基礎・詳細履歴は `HANDOFF_2026-06-12.md`／`HANDOFF_2026-06-24.md`、各テーマは専用ドキュメント参照。
> ⚠️ **複数チャットの同時編集は禁止**（上書き事故）。アクティブは常に1つ。切替時は必ず commit & push。

## 0. 基本情報
| 項目 | 値 |
|---|---|
| コード場所(git) | `C:\Users\User\AppData\Local\Temp\sokonara\` ※**tempは消える**→消えたら `git clone https://github.com/ymiyake-art/sokonara.git` 再取得＋git config再設定。**GitHubが正本** |
| ⚠️ ユーザーが普段見るフォルダ | `C:\Users\User\1_Claudecode\sokonara\`（ドキュメント置き場）。**作業物はTemp側**。読ませたいドキュメントはここへコピーする運用 |
| GitHub | `https://github.com/ymiyake-art/sokonara.git`（main・Vercel自動デプロイ・no-cache） |
| 本番URL | `https://app.sokonara.co.jp`（`/`=個人アプリTOP／`/pf/`=課題カード／`/biz/`=法人LP／`/personal/`=個人LP／`/meet/`=イベントLP／`/admin.html`=管理） |
| git | user.email `ymiyake@sokonara.co.jp` / name `ymiyake-art`。pushはPowerShellで `Set-Location` 後、`-m`1行 |
| Supabase | anon公開キーはindex.html内。URL/SERVICE_KEY/ADMIN_PASSWORD等はVercel環境変数(設定済)。Supabase操作はPython(urllib)・結果はJSON→Readで（文字化け回避） |
| 最新コミット | 497b7bf（Well-beingマトリクス管理ページ。本番反映済み） |

### 環境の注意（ハマりどころ・全部現役）
- **プレビュー(static)は誤ルート配信の不調**あり（`.claude/launch.json` の `--directory` が旧パスを向いていた事例＝2026-07-02修正済。`sokonara-temp`→`Temp\sokonara`）。検証は**ファイルgrep＋本番プローブ**が基本。ローカルプレビューを使う時はキャッシュバスター(`?v=`)必須。
- スクショは時々タイムアウト → evalで computed値/textContent 確認で代替。
- ADMIN_PASSWORDは手元に無い → 管理API(list等)は叩けない。読取検証は anon読取可のテーブル or `/api/report` action=get 等で。**admin.htmlをローカルで開くとanonキーで本番companiesが読める**（読取のみ・2026-07-02検証で活用）。
- ⚠️**`git add -A` 厳禁**：Tempは部分的に消えることがあり、消えたファイルを「削除」として拾う事故歴あり（2026-06-26）。**コミット前に必ず `git status`→対象を `git add <path>` で個別ステージ**。削除が出ていたら `git checkout <commit>^ -- <paths>` で復元。

---

## 1. 直近の変更（2026-06-29〜07-02・全部push済み＆本番反映）

### 自己発見ピボット（57f30f6ほか）
- **AIレポートを主役に**：LINEゲート撤廃で全文表示。記事CTA→反応(p2)→レポート(p3)→興味CTA→フォームに再接続。VS3回戦に短縮。LINEは詳細版/近況の**任意導線に降格**。
- 記事直後に**紹介CTA（興味を伝える）復活**＝最重要KPIへ直行路（診断は任意の副路）。
- AI診断は**4問に復帰**（材料増）＋streamモデルを**gpt-4o-mini**へ（速度優先。薄ければ4oに戻す）。
- フォーム導線最小化：共感→即P5。P5は2アクション（経営層名ワンタップコピー→フォーム貼付）。

### モヤモヤ逆引き（829b416/88a0592/e3a0b4b/69977b6）
- ホームに「今のモヤモヤから探す」＝キャリアアンカー式8択→タグマッチ→経営層3人＋偶発枠。**variant='moya'** で card_logs 計測（admin版別A/Bに「モヤモヤ(逆引き)」行）。
- 逆引きの構造化タグ＝`companies.btags/rtags`（`companies_tags.sql`）。経営層編集で記事→自動生成(cardgen)＋手動チェック→保存。

### Career Well-being（db05749/04bbd49/ee36f0f/4262fd9/497b7bf）
- **レポート本体**：6軸レーダー（この経営層×あなたの重心）＋高中低バンド＋3ブロック＋マニフェスト＋紹介CTA。p3から「Career Well-beingを見る」で遷移。
- 6軸＝`meaning`(意味・使命)/`agency`(裁量・挑戦)/`people`(人・近さ)/`local`(地域・つながり)/`time`(時間・ゆとり)/`econ`(経済)。
- **会社スコア＝`companies.wb`（jsonb・`companies_wb.sql`）優先**、無ければ rtags/btags/tags から自動導出。
- **Phase2 比較シミュレーター**：「今の仕事と比べる」＝年収帯1タップ＋足りない軸→現状レーダー重ね＋重み付き総合「今の仕事XX→この経営層YY」。年収ダウンも正直に注記。
- AI採点：`api/feedback.js` に `wbscore`（記事→4軸を0-100推定。time/econは手動）。経営層編集に6軸スライダー＋「記事から推定(4軸)」。
- ⚠️修正済バグ：`toCompanyRow`（DB行ホワイトリスト）に btags/rtags/wb が無く**保存時に破棄されていた**→追加済（ee90f10）。新カラム追加時は toCompanyRow への追記も忘れずに。
- **初期値seed**：`companies_wb_seed.sql`（7社・AI推定4軸＋time/econ=60・フロア45/上限90）→**Supabase適用済み✅**（2026-07-02本番プローブで7社全一致を確認）。
- **📈 Well-beingマトリクス管理ページ**（admin新タブ・497b7bf）：企業×6軸の横並びテーブル。**総合列なし・フロア45/上限90クランプ**・行別「✨AI推定」＋「全社AI推定(4軸)」（記事本文=companies.bodyから直列実行）・「💾一括保存」→`companies.wb`。未保存件数表示・保存失敗時は未保存表示を維持（saveCompaniesが成否を返すよう改善）。

### 計測・レポート強化
- **form_open**（P5フォーム開封＝CV直結）イベント追加（`card_logs_form_open.sql`＋card-log ALLOWED追記）。admin共鳴ファネルの最終CVに。
- **期間フィルタ**：共鳴データに全期間/今日/7日/30日＋日付指定（started_at基準）。
- **企業別・高デザインPDFレポート**：レポートタブ「PDFレポート出力」→A4品質（ロゴ/期間/KPI/刺さった言葉・求めるもの・状況グラフ/声）→window.printでPDF。**データ源はcard_logs**（interest_formsから修正済・共鳴データと一致）・期間フィルタ連動。genReport（詳細レポート）もcard_logsに統一。

### /meet/ イベントLP（7/30 ソコナラMEET）
- 新設→全面リデザイン（明朝ディスプレイ/縦タイムライン/FAQ/固定CTAバー）。**先頭の`EV`設定オブジェクト**（日付/会場/登壇者/gallery/heroImage/videoUrl/FAQ）だけ差し替えれば再利用可。
- 実写真ヒーロー＋当日の様子ギャラリー。登壇6名（シルエットアバター・社名当日発表）。ヒーローコピー＝無圧「会社を選ぶ前に、経営層に会う。／情報収集でOK・応募や選考ではない」。
- **参加者の声＝casesテーブルから動的取得**（個人LPと単一ソース＝admin編集が両LPに反映）。

### その他UI
- **CASES写真＝フォーカルポイント方式**（3:2枠・拡大/左右/上下スライダー。adminプレビュー＋個人LP・MEET LP両方に反映）。
- **adminサイドバー固定**（position:sticky・長いレポートでもメニュー常時表示）。

---

## 2. 変わらない基盤（詳細は HANDOFF_2026-06-24.md）
- **card_logs書込は全て `/api/card-log`（service_role・upsert）経由**（anon UPDATEはRLSで0行更新のため）。新カラムは ①SQLでALTER ②`api/card-log.js` の ALLOWED追記 の順。
- **companies書込は `/api/admin-data`（service_role）経由**＋admin側 `saveCompanies`。**新カラムは `toCompanyRow` にも追記**（忘れると保存時に破棄）。
- AI診断＝`api/feedback.js`（cardfeedback/values/cardgen/articlemeta/q1rewrite/wbscore）。ストリーミング中継・JSON厳格化。記事ルールの正本はNotion。
- UTM/チャネル別計測・Vercel Web Analytics・LINE OAuth state=logId の診断復元 等は06-24版の通り。
- 既定＝人物(偶然性)版 `/pf`。旧課題版は `?v=challenge` 温存。カード文言A/B（cc_what/cc_value/cc_life・arm_events）稼働中。

### 追記（2026-07-02後半・9b80211）＝「報酬の重心」ピボット Phase 0
- **戦略決定**：診断は売りにしすぎない→呼称「**報酬の重心を知る**」／LPは増やさずFVパラメータ出し分け／企業側は「自己開示（AI補助）」立て付けで既存PDFに報酬プロファイル頁を追加予定（Phase 0.5）／TOP改修はH1/H2データで判断。
- **レポート改修**：上段＝あなたの重心のみ（経営層スコア非表示＝評価印象の排除。wbは将来値推定に内部使用）。タイトル「仕事の報酬を、どう最大化するか。」＋年収以外の報酬リード文。**フロー予報**（意味×裁量から派生・チクセントミハイ注釈）。比較＝「いま vs **共鳴後の想定**」（projectWB: cur+(comp-cur)×0.6、econは×0.85）＋Δチップ＋参考値/経済正直注記。
- **p3上下重複解消**：要約(WORKING HINT)のみ無料。詳細（ことばの読み解き＋重心ミニマトリクス`fbWbMini`＋bridge）は**LINE後**（`fbDetailLock`ロック表示・fbLineCta文言=詳細レポート特典）。
- **H1計測**：`wb_view`/`wb_compare`（`card_logs_wb_events.sql`＝**⚠️Supabase未実行**・ALLOWED追記済・admin新導線ファネルに「報酬の重心を見た」行＋H1検証行=閲覧有無別CV率）。

### 追記2（2026-07-02・66f9230）＝MEET当日ページ＋運営基盤（7/30向け）
- **`/meet/day.html`**（受付QR配布用・noindex）：同意文＋氏名登録→企業一覧（写真/キャッチ/記事リンク=本体`/?cid=`）＋**気になるチェック最大3社**（付替自由・stickyバーに残数）→**最低限アンケート**（詳しく知りたい/個別に話したい/氏名共有可否/理由タグ8種/一言）→送信後に**報酬の重心ミニレポート**（チェック企業のwb＋理由タグから導出＝イベント×指標連携）。冒頭`EV`設定（eventId/日付/maxChecks/companyIds）で次回再利用可。localStorage `sn_meet` で復元。
- **PII設計**：`meet_entries`（`meet_entries_setup.sql`＝**⚠️Supabase未実行**・RLS有効/ポリシーなし＝anon全遮断）。書込=`api/meet-log.js`（service_role・ホワイトリスト・欠落列自動ドロップ・id必須`meet_`接頭）。閲覧=`api/admin-data.js`に**listアクション追加**（ADMIN_PASSWORD認証のみ）。重心計算にPII不使用。企業共有はconsent=trueの本人のみ。
- **admin「🎪 MEET運営」タブ**：統計（登録/チェック/アンケ/同意）・**社別の興味表**（チェック/詳しく/話したい人数＋氏名列挙＝班分け用・共有不可注記）・理由タグ匿名集計（企業レポート用）・参加者一覧・イベント切替。
- 企業レポートへの反映（チェック数/接触/希望/重心分布の頁）＝Phase 0.5と合わせて後日。

### 追記3（2026-07-02夜・4260c64）＝企業増加対応・デザイン磨き・思想ページ刷新
- **SQL適用済み✅**（meet_entries＝保存/anon読取遮断/anon書込401を本番実証、wb_view/wb_compare＝ドロップなし記録を実証）。テスト行 `meet_probe_*`/`probe_wbev_*` は当日前に削除すること。
- **企業増加対応**：マトリクスに行チェックボックス＋「✨選択をAI推定」「✨未採点のみ」「✨全社」＋名前絞り込み＋未採点バッジ。day.htmlは記事なし企業（body<50字）に「記事は近日公開」表示（リンク非表示）。
- **レーダー立体化**：index（p-wb/p3ミニ）とday.htmlのradarを層状フィル＋グラデ塗り＋feDropShadow＋白頂点ドットに統一。
- **MEET LP**：時刻正＝19:00–21:30（**受付18:40〜**明記）・タイムテーブルに時刻挿入・写真を白マット＋金ヘアライン＋シャドウの額装仕上げ・実例カードにも艶。⚠️**LPは登壇6名表記のままだが運営計画は7社**——7社確定なら7人目の紹介文が必要（ユーザー判断待ち）。
- **admin思想ページ全面刷新**：報酬の重心ピボット反映／**顧客動線の全体模式図**（WEB2入口→記事→反応→HINT→重心→CV、LINE分岐、MEETレーン、企業側wb/レポート、計測点線＝card_logs列挙）／6軸指標カード／理論に計画的偶発性（クランボルツ）とフロー（チクセントミハイ）を追加／計測学習ループ（North Star・H1/H2）。
- **Clarity導入**：マーケ会社提案に賛成の方針（無料・SPA計測の補完）。導入時は day.html の氏名入力とP5フォームを **clarity mask** 対象にすること。card_logs=意思決定ファネル／Clarity=行動観察／GA4=流入、の3層で重複なし。

### 追記4（2026-07-02深夜）＝呼称の全面改名「報酬の重心」→「あなたの仕事の軸」
- ユーザーから「報酬の重心という言葉が意味わからん」→ **「報酬」「重心」はユーザー向けに使用禁止**。呼称＝「あなたの仕事の軸」（就活・転職の“軸”に馴染みあり）。
- タイトル「あなたの“仕事の軸”は、どこにあるか。」／リード「年収以外にも、仕事から得られるものはたくさんある——年収や知名度だけが、キャリアの正解じゃない」／凡例「あなたの軸」／比較「あなたの軸で重み付けした“総合の手応え”」／p3ボタン・ロック文言・LINE後ミニ「あなたの仕事の軸」／day.html「今日のあなたの“仕事の軸”」(CAREER AXIS)／adminファネル「仕事の軸レポートを見た」・H1検証行。
- 内部識別子（wb_view/wb_compare/companies.wb/openWellbeing等）は据え置き。Career Well-being はフレームワーク名として存続。

### 追記5（2026-07-03）＝MEETリンク＋LP文言のadmin編集
- **リンク**：アプリヘッダー（mode-bar）に「7/30 MEET」→/meet/（別タブ）。admin公開ページナビに「🎪 MEET LP ↗」。
- **LP文言編集**：`site_settings` テーブル新設（`site_settings_setup.sql`＝**⚠️Supabase未実行**・anon読取のみ・書込はadmin-data）。meet/index.html は起動時に `site_settings.meet_lp` をEVへマージしてから描画（`/*EV_START*/`〜`/*EV_END*/`マーカーは管理画面の読込に必須＝削除禁止）。admin「MEET運営」タブ先頭に**LP文言編集**カード：📥現在の文言を読み込む（LPからEV抽出＋保存済み編集を重ねる）→フォーム編集（ヒーロー/日時/会場/定員/締切/登壇者見出し等＋リスト系はJSON。**7人目の登壇者はspeakersのJSONに追加**）→💾保存して公開（全文スナップショット保存・JSON不正はフィールド名付きで検出）。
- ⚠️PowerShell注意：コミットメッセージに全角引用符（“ ”）を入れると引用符として解釈されコミットが壊れる。使用しないこと（2026-07-03に実際に発生）。
- ⚠️**画像差し替えの事故ポイント**：ユーザーが `1_Claudecode\sokonara\images\` の元ファイルを直接上書きしても、そのフォルダはGit/Vercel未連携のドキュメント置き場のため**本番には一切反映されない**（2026-07-03に発生・case2.png）。画像を差し替えたい時は「◯◯の写真を差し替えた」と伝えてもらい、私が `1_Claudecode` 側の新ファイルをTemp側リポジトリへコピー→commit→pushする。
- ⚠️**MEET LP文言編集の重要な仕様**：admin「LP文言編集→保存して公開」は**EV全体を`site_settings.meet_lp`へスナップショット保存**する（一部項目だけでなく全フィールド）。一度保存されると、**以後コード側でEVのデフォルト値を直しても、この保存データがある限りそちらが優先表示される**（2026-07-03に発生：コードで18:30に直したがLPは18:40のまま→保存済みスナップショットが原因と判明）。コード修正だけで直したい場合は、該当フィールドを `UPDATE site_settings SET value = jsonb_set(value, '{フィールド,パス}', '"新しい値"') WHERE key='meet_lp';` でスナップショット側も一緒に直すか、`DELETE FROM site_settings WHERE key='meet_lp';` でスナップショットを削除してコードの既定値に戻す。

### 追記6（2026-07-03昼・5e175da）＝診断導線刷新＋TOP並列化＋SNS共有
- **診断導線の再設計**：記事後ボタン「可能性を見る」→**「仕事の軸を診断」**。診断送信（4問・約1分）→**マトリクス（p-wb）即表示**（AI待ちゼロ・無料側のAIコストもゼロ）。**AIの文章診断（WORKING HINT＋詳細）はLINE特典へ完全移行**＝p-wbの`wbDetailCta`（未登録=🎁特典カード→LINE／登録済=📝詳細レポート→`openDetailReport()`→p3生成表示）。4問を残した理由＝q1_phrase（刺さった言葉＝企業レポートの源泉）の収集維持。`logResonance(aiViewed)`分離＝**q1等は診断送信時に記録・ai_viewedはAI閲覧時のみ**。LINE復帰は既存restore（p3）でそのまま動作。
- **TOP並列化**：モヤモヤボタンを白ソリッド同サイズに（両入口対等）。リード文とステップ①を中立表現（①経営層を選ぶ）に。
- **経営層カードのフォント拡大**：フレーズ19px(SP17.5)/名前16.5/会社11.5/CTA13.5/moya推薦理由13.5。
- **SNS共有**：p-wbレーダー下「📤この“軸”を画像でシェア」＝canvasで1080×1350のネイビー×ゴールド画像を端末内生成→Web Share（非対応はDL）。**画像は本人の軸のみ・経営層名/社名は載せない**。
- site_settings適用済み✅（anon読取[]・anon書込401・admin-data疎通を本番実証）。casesお名前=アルファベット表記✅・受付18:30✅・許諾注記削除✅。

### 追記7（2026-07-03午後・638cb4f）＝記事移管の準備（表示順制御）
- **背景**：ソコナラWEB（sokonara.co.jp/company）の既存記事6社をアプリへ移管予定。LP順＝日野出→アド印刷→松永製作所→トランスパック→M・ZEC→増田桐箱店（中村さん）。アプリ既存はアド印刷/M・ZECのみ＝**日野出/松永製作所/トランスパック/増田桐箱店の4社は新規登録が必要**。
- **companies.sort（表示順）**：`companies_sort.sql`✅適用済み。admin掲載企業一覧の**↑↓ボタン**で並び替え（スワップ→10刻み振り直し→保存）。index/admin/day.htmlの取得を sort.asc(nullslast)→created_at.asc に。toCompanyRowにsort追加（保存破棄防止）。
- **Notion記事の必須フォーマット**（adminのparseNotionが期待する形・0680438でNotion必須3項目に完全対応）：`# タイトル`／`## ソコナラより`（記事冒頭・①〜③の要約。**冒頭セクションでも📋内`**ソコナラより**`でも可**＝メタに回収し本文には出さない）／`## リード文`／本文`## 見出し`／**Q&Aは見出しに「インタビュー」or「伺いました」を含め、質問は`**――質問**`形式**／末尾`## 📋 プラットフォーム登録情報`（**カードタイトル候補**=3案-箇条書き→**adminでワンクリック1案選択**・**タグ**(地域,テーマ,フェーズ)・**引用フレーズ**・**Q1選択肢**(-箇条書き・Why/Who/What/Life各1＝**4件でないと解析時に警告**・ラベルは自動除去)・**こんな人と出会いたい**・**会社一言紹介**）。**人が書いたソコナラより/出会いたいが正＝AI自動生成は欠落時のみ**。`<aside>`はスキップ＝本文厳禁・メモ用。見出しに「タイトル/本文/メモ/備考/下書き/TODO/ファーストビュー」を含むとスキップ。
- **写真**：移管後に sokonara.co.jp/company からダウンロード→リポジトリ `images/` へ→photoUrl設定（ホットリンク回避）。

### 追記8（2026-07-03夕・32e578e）＝実機UXフィードバック対応
- カードフォント差し戻し（拡大で改行崩れ）＋モヤモヤ推薦カードを一覧と同じ`leaders-grid`2列に。記事ヘッダーの0%（読了率）表示を削除（updateReadのパルス処理は残置）。
- **LINE復帰の軸リセットバグ修正**：sn_restore/stateにS.moya/memoChipsが無く復元後にレーダーが変わっていた→追加。**window.MOYA公開**（IIFE内部限定でモヤモヤラベルが全画面表示されない既存バグ）。
- 軸ラベルをフル表記（意味・使命／裁量・挑戦／人・近さ／地域・つながり／時間・ゆとり／経済（年収））＝index/day/共有画像。viewBox拡張ではみ出しなし確認済み。
- **「この軸の導き方」根拠表示**を仕事の軸レポート冒頭に追加（◯◯さんへの反応＋刺さった言葉＋モヤモヤ→「比べてみるとさらにあなた仕様に」）＝いきなり数字が出る不信感への当座の対処。
- meet LPのOGPをイベント版に（og:title/descから「福岡の夜」撤去・og:image=meet-hero.jpg・twitter:card追加）。曜日括弧の正規化（(木)と入力しても((木))にならない）。
- **設計課題（ユーザーと議論中）**：①自由記述の記入タイミング（LINE特典直前へ移す案）②「○○タイプ」命名＋キャラ＋X/Instagramワンクリックシェア（動的OG画像 /api/og が本命。タイプ名はシェア面限定・レポート本体は軸表示の2層で「診断と名乗らない」方針と両立）。
- **③叩き台は実装済み（e77cd69）**：思想ページに「6軸×理論×導出元の対応表」と「診断の配線表」（Q1〜Q3が軸マトリクス未接続＝❌を可視化）。改善提案＝Q2/Q3→軸の静的マッピング／モヤモヤをAI文章診断へ／自由記述をLINE特典直前へ。**配線の事実**：軸マトリクス=経営層wb28%+モヤモヤ+22のみ／AI文章=Q1〜Q4+自由記述+経営層文脈（モヤモヤ未接続）。

### 追記9（2026-07-03夜・1713fd5）＝設問刷新（4問＝感情/価値観/事実/意図）
- **設問設計の原則確定**：4問は種類を変える——Q1感情（記事への反応）／Q2価値観（求めるもの）／Q3事実（年収帯・忙しさ）／Q4意図（聞きたいこと）。「欲しい×足りない」の二度聞きはユーザーに見透かされるため不採用（ユーザー指摘）。
- **Q2刷新**：6軸対応の6択・**最大3つ**（優先順位付け＝アンカー顕在化）→選択軸に**+12**（`Q2_AX`）。モヤモヤ+22と併せ最大+34。
- **Q3差し替え**：年収帯（s3〜s8・6段階）＋忙しさ（b1多忙/b2ふつう/b3ゆとり）の**事実2タップ**。`S.sal/S.busy`＋AI互換で`S.about`に言語化。**比較シミュレーターがゼロタップ化**＝p-wb表示時に自動実行（年収→econ現状・忙しさ→time現状35/55/75）。比較レーダーは結果ボックス内に分離＝上段の重心レーダーと同時表示。LINE復元にsal/busy/moya対応済み。
- **思想ページ**：「原点」カード新設（都市部の“なんでだろう”層／年収は報酬の一要素／「下がるけど、これが上がる」の見える化が使命／創業者の転身＝意味・人・フロー・地域が上がり経済が下がる、が6軸の原体験）。配線表をQ2/Q3⭕に更新・設問原則を記録。
- 残る改善候補：モヤモヤをAI文章診断へ渡す／自由記述をLINE特典直前へ移動（①未実装）／○○タイプ＋ワンクリックシェア（②Phase A/B未実装）。

## 3. TODO / 意思決定待ち
- [ ] **記事移管**：4社の新規登録＋6社の記事貼付（Notion経由）→タグ/wb推定→写真移管→↑↓でLP順に並べ替え
- [x] ~~Supabaseで `site_settings_setup.sql` を実行~~ ✅（2026-07-03適用・本番実証済み）
- [ ] **7/30 LPの登壇者数**：現在6名表記。7社確定ならadminのLP文言編集→speakers JSONに7人目を追加（コード修正不要に）
- [ ] **Clarityタグ導入**（プロジェクトID入手後。index/pf/meet系に挿入＋PIIマスク設定）
- [ ] **計測テスト行の削除**（`probe_*`/`meet_probe_*`/`apicheck_*`）＝配布・当日前に
- [ ] Phase 0.5：企業別PDFに「報酬プロファイル」頁（6軸レーダー＋惹きつけている重心の分布）
- [ ] Phase 1：2〜4週データでTOPの主役を判断（H1=重心レポートのCV押上げ／H2=入口別）→FVサブコピー「年収だけが、仕事の報酬じゃない。」A/B
- [ ] admin画面の整理（方向性が見えた時点で・ユーザー指示）
- [ ] **全セッション消去**（計測リセット。テスト行 `probe_*`/`apicheck_*` 等一掃→クリーン計測開始）※配布前に
- [ ] マトリクスページの実機確認（admin→📈Well-beingマトリクス→AI推定→一括保存。本番ADMIN_PASSWORDが必要なためユーザー操作）
- [ ] Well-beingの time/econ を実態値に手動調整（現状は全社60の初期値のまま）
- [ ] **biz LP を偶然性訴求へ改訂**（personal/index は改訂済）
- [ ] **7/30 MEET機能**（企業一覧＋簡単アンケート＋登録者の班分け＝自動振分→手動調整）
- [ ] FVコピーA/B（偶然性 vs 具体ベネフィット）は後日

## 4. 関連ドキュメント（リポジトリ内）
- `HANDOFF_2026-06-24.md`（前回版：新導線・計測大改修・カードA/B の詳細）
- `HANDOFF_2026-06-12.md`（基礎・SQL実行状況・セキュリティ）
- `MARKETING_BRIEF_2026-06-29.md`（ユーザーフォルダ側）／`MARKETING_BRIEF_2026-06-24.md`
- `CARD_COPY_RULES_2026-06-24.md`／`DESIGN_v2_challenge_cards.md`／`BRIEF_biz_lp.md`
- SQL実行状況：lockdown_s1/s2✅ biz_inquiries✅ card_logs_kpi/utm/variant/form_open✅ cases_setup✅ companies_card_copy✅ companies_tags✅ companies_wb✅ **companies_wb_seed✅（2026-07-02確認）**

### 追記10（2026-07-05）
- MEET LPナビロゴ→アプリを UTM付き（meet_lp/lp/lp_meet0730/nav_logo）＋別タブに（2642dcb）。LP経由のアプリ流入はadminチャネル別で meet_lp として計測。
- LP見出しフォントは**明朝（Shippori Mincho）維持で確定**（ユーザー判断）。本文は元からNoto Sans JP。Noto統一案・Zen Kaku統一案は不採用＝編集記事トーンを差別化資産として残す。

### 追記11（2026-07-05・72510a1）＝軸リセット根治ほか実機FB第2弾
- **軸リセットの真因**：LINE復帰でlocalStorageが失われる経路（LINEアプリ内ブラウザ等）は stateパラメータから復元するが、**stateにQ2が未搭載**だった（Q2が軸+12に効くようになった1713fd5以降に顕在化）。→ Q2を短縮コード化（Q2_CODE: mn/ag/pe/lo/ti/ec）して**lg経路含む両ペイロードに搭載**・復元時に復号。localStorage全消失シナリオで軸レーダー座標の完全一致を検証済み。
- **レーダー出現アニメ**：`.wbGrow`（メイン多角形＋頂点ドット、scale .45→1・0.8s）→`.wbGrowSec`（現状線・0.3s遅延）。prefers-reduced-motion対応。index（p-wb/p3ミニ/比較）とday.html両方。
- マニフェスト「年収以外にも、」で改行。
- **足りないチップの挙動修正**（ユーザー指摘：両方動いて意味不明）：将来線（共鳴後の想定）は**事実ベース（年収帯＋忙しさ＋中立66）から固定**。チップは**現状線のみ**下げる＝「この環境で埋まる幅」が見える。チップは4軸（意味/裁量/人/地域）に削減し「時間と収入はQ3の回答を使用」と注記。

### 追記12（2026-07-05・1c36937）＝実機FB第3弾（再入口・前提可視化・重心の透明化）
- **診断の再入口**：スワイプで戻るとレポートに戻れない問題→ sn_fb_cache にmoya/sal/busyを追加し診断送信時に保存・起動時に24hハイドレート・**TOP（p-list）ヒーロー直下に「📋診断済み」バー**（あなたの仕事の軸を見る／LINE済みなら詳細レポートも）。goScreen(p-list)フックで表示制御（renderDiagReentry）。
- **比較の前提可視化**：「Q3の回答」という表現に依存せず、比較結果内に「前提：年収帯X・忙しさY」を明記＋比較カードに**前提①年収セレクト／前提②忙しさチップ**（変更で即再計算・キャッシュも更新）。
- **重心の経済・時間の透明化**：企業wbの影響を価値4軸（意味/裁量/人/地域）に限定。**時間・収入はQ2で選んだ時だけ+12＝選ばなければ50（こだわり控えめ）**。根拠文に「時間・収入は求めるもので選んだときだけ高くなります」を明記。「なぜこの点か」が説明可能に。
- ユーザー所感「まだまだ見直す余地あり」＝仕事の軸まわりは継続改善フェーズ。

### 追記13（2026-07-05・a583ea7）＝自由記述移設＋タイプ命名＋シェアPhase A
- **自由記述をQ4から詳細レポート特典カード内へ移設**（wbFree・oninputでS.free同期・書き足して再度開くと再生成）。診断は実質3問＋任意チップに軽量化。「書く→すぐAIが読む」の隣接で納得感。
- **○○タイプ（15種）**：上位2軸の組合せ→`TYPE_NAMES`（wb IIFE内・書き換えだけで命名差し替え可）。**シェア面限定の遊び**でレポート本体は軸表示のまま（「診断と名乗らない」方針と2層両立）。p-wbにタイプバッジ表示。
- **ワンクリックシェア（Phase A）**：𝕏（twitter intent）／LINE（line.me/R/share）ボタン＋既存の画像保存。テキスト＝「私の仕事の軸は『◯◯』タイプでした。年収や知名度だけが、キャリアの正解じゃない。#ソコナラ」＋URL。共有画像にもタイプ名を金字で描画。
- **Phase B（未着手・本命）**：結果ごとの共有URL＋Vercel /api/og で動的OGP（レーダー＋タイプ名カード）→Xで自分の結果が展開される。記事OGP動的化と同じ技術で一石二鳥。

### 追記14（2026-07-05夜・138362e/51df82b）＝構造課題の解消と大型バッチ
- **画像軽量化（c4f2945）**：case1/2/4→webp・case3.jpg同名再圧縮・OGP→1200x630のjpg。計11MB→420KB（96%減）。**⚠️casesテーブルのphoto_url更新SQLが未実行**（case1/2/4のみ.webpへ。下記TODO）。
- **動的OGP＋共有URL**：`package.json`新設（@vercel/og のみ・buildなし＝静的配信維持を本番確認済み）。`/api/og`＝記事カード(?cid)と軸カード(?d=6値&t=タイプ名)の2モード（Noto Sans JPを使用文字だけサブセット取得）。`/s`（api/share）＝クローラーにOGタグ・人間は即リダイレクト（utm_source=share付与）。記事SHAREコピー→/s?cid、X/LINEシェア→/s?d&t。**両カードの生成画像を目視検証済み**。
- **adminサイドバー**：企業一覧を五十音順＋details折りたたみ（開閉状態はlocalStorage保持）。
- **タイプ命名エディタ**：マトリクスタブ末尾。15種の名前＋キャラ絵文字→site_settings.axis_types→アプリが起動時マージ（TYPE_DEFAULTSはindex.htmlのTYPE_NAMESと同期させること）。
- **day.html経営層化**：「気になる企業」→「気になる経営層（最大3名）」全文言・カードをleader-card化（写真＋キャッチ重ね＋斜線＋「想いを読む→」）。admin公開ページに🎫MEET当日ページリンク。
- **入口別計測**：経営層一覧・関連カードからの記事openに`variant='list'`行を作成（セッション中1行使い回し・以降の診断/CVも同行にpatch）。admin版別表＝カードタップ(person)/モヤモヤ/一覧(直接)/直リンク(direct)の4入口比較が可能に。

## TODO追加
- [ ] **Supabaseで実行**：`UPDATE cases SET photo_url='/images/case1.webp' WHERE id='case1'; UPDATE cases SET photo_url='/images/case2.webp' WHERE id='case2'; UPDATE cases SET photo_url='/images/case4.webp' WHERE id='case4';`（case3は同名のため不要）

### 追記15（2026-07-05深夜・b548599/0444bdb）＝比較ロジック根治＋MEET班分け
- **比較レーダー根治**：共鳴後(未来線)＝この環境が提供する6軸そのもの(=comp)に変更。従来projectWBで現状から会社へ寄せていたため年収帯変更で両線が動く不具合。修正後は年収帯/忙しさ/足りないチップは現状線だけ動かし未来線は環境固定＝創業者の原体験を正直に描く。実操作で現状線1本のみ動くこと確認。
- **記事なし登壇者も選べる**：day.htmlに`EV.extraSpeakers`（DB未登録の経営層。{id,ceo,name,catchphrase}をEV先頭で定義→末尾合流・選択/班分け対象）。
- **班分け自動割り当て**：admin「MEET運営」の🧩班分けカード＝巡数(3/4)選択→自動計算→分布プレビュー→保存。アルゴリズム＝**希望した経営層に必ず行ける/同卓重複なし/全巡充足**（Python1800試行で違反0検証済）・空き枠は最も空いた卓へ均等配分。卓＝経営層1社1卓、roster自動導出（DB企業∪チェックされた未知ID）。保存は各entryのgroups列へmeet-log API経由。day.htmlは登録後`loadMyGroups`で自分の巡回スケジュール（巡目/卓番号/経営層名）を表示。meet-log APIに`groups`書込許可＋`action=get`（本人のgroupsのみ返す・PII非公開）。

## TODO追加
- [ ] **⚠️ Supabaseで実行**：`ALTER TABLE meet_entries ADD COLUMN IF NOT EXISTS groups jsonb;`（班分け保存先。未実行だと保存が列自動ドロップされ反映されない）
- [ ] 当日運用：日野出/松永製作所/トランスパック/増田桐箱店の記事が無いなら day.html の `EV.extraSpeakers` に4名分記入（id/ceo/name）。記事移管が済めば不要

### 追記16（2026-07-05・87fc41a）＝再選択バグ根治・イベントバナー・GTM
- **⚠️重要バグ根治**：pfで選び直して記事を開くと前の人が表示される→真因は追記12の診断キャッシュハイドレートが`selectedCompanyId`を復元し、`loadPrimaryCompany()`がURLの`?cid`より`selectedCompanyId`を優先する仕様と衝突（リグレッション）。**URLにcid/companyがある時はハイドレートで復元しない**修正。E2Eで再現→修正→汚れキャッシュ有でもURL通り表示・再入口機能無事を確認。※教訓：selectedCompanyIdはURLパラメータより強い。復元系の機能を足す時はこの優先順位に注意。
- **記事内イベント告知バナー**：`site_settings.meet_banner`＝admin「MEET運営」📣カードから**ON/OFF・あおり・イベント名・ボタン文言・リンク先・対象経営層（チェックボックス・未選択=全記事）を操作**。アプリは起動時読込→対象記事の末尾（出会いたいセクション直後）に赤アクセントバナー表示（utm_medium=article_banner）。**イベント終了時はONを外して保存するだけ**。対象に中嶋・古賀＋移管記事を追加するのはadmin上の操作（コード不要）。
- **GTM設置**：GTM-KW8RTRQB を公開6ページ（/・/pf/・/personal/・/biz/・/meet/・/meet/day.html）の`<head>`先頭＋`<body>`直後に設置。admin.htmlは非設置。マーケ会社のGA4/Meta広告タグはGTM側で発火設定。

### 追記17（2026-07-05・28feae2）＝イベントバナー改善（実機FB）
- バナー文言が空保存だと「謎の赤ボタン」だけになる問題→**既定文言で自動補完**（アプリ側フォールバック＋admin側は既定文言プリフィル・保存時も空なら補完）。
- **スティッキーCTAバー（この経営層、気になりましたか？）の2ボタンの上にミニバナー**（1行コンパクト版）を追加＝記事読了前から見える導線。記事末尾のフルバナーと同じ設定（ON/OFF・文言・対象経営層）に連動。

### 追記18（2026-07-05・43be2dd）＝グローバルイベント告知リボン
- スマホでヘッダー(mode-bar)の「7/30 MEET」ボタンに横スクロールで届かない→**全画面のヘッダー直下に固定リボン**（赤グラデ・🎪＋あおり＋イベント名＋「参加する→」）を追加。**admin MEET運営のバナー設定ONで出現**（同じmeet_banner設定に連動・companyIdsは無視して全画面表示＝ナビ導線のため）。`body.sn-ribbon-on`で`.view`/`#project`の上余白を+40pxしコンテンツ非重複、OFFで完全復帰。utm_medium=global_ribbon。
- これでイベント導線は3層：①全画面ヘッダー直下リボン②記事末尾フルバナー③記事スティッキー上段ミニ——全部admin ONで同時表示、OFFで同時消滅。

### 追記19（2026-07-05・905bccb）＝イベント導線の赤をリファイン
- フラット赤#d43a2aがイマイチとのFB→共通クラス`ev-ribbon`/`ev-card`/`ev-btn`/`ev-pill`（index.html style内）に集約。**深紅グラデ(coral #ef5f3d→crimson #c0341a・135deg)＋inset白ハイライト(立体感)＋backdrop-blur 8px(透過)＋多層シャドウ(浮き)**。3導線（グローバルリボン/記事末尾フルバナー/スティッキーミニ）を統一。色替えはこの4クラスのみ編集で全導線反映。navy+gold案も候補として保留。

### 追記20（2026-07-05・47d8bf0）＝LP/当日ページ実機FB
- 当日ページの経営層カード文言切れ：catchphraseを`.split('\n')[0]`で1行目のみ表示していた→全行`<br>`表示に。lcard-top min-height190px。
- LP曜日(dow)を10px→15px太字に拡大。
- LP主要テキストに`word-break:auto-phrase;line-break:strict`適用＝スマホの変な位置の改行を抑制（見出し/リード/登壇者/声/FAQ等。auto-phrase非対応環境でも無害）。
- admin班分けに**ダミーデータ投入ボタン**（12/24/40名）＝実データ・DBに触れず分布と個人スケジュール例を確認。ダミー結果(meet_dummy_接頭)は保存拒否。プレビュー描画を`renderGroupPreview`に共通化。

### 追記21（2026-07-06・fc327f9）＝当日ページのカードをアプリ経営層一覧と同寸法に
- 全幅1列(aspect16/9)だと写真が横長でキャッチのフォントが潰れる→**2列グリッド＋上部210px固定**（アプリの`.leaders-grid`と同じ）に統一。`.lcard-grid=repeat(2,minmax(0,1fr))`、`#coList`にクラス付与。lcard-bottomをflex縦にしco-linksを`margin-top:auto`で下端揃え。

### 追記22（2026-07-06・f9026b6）＝LPフォント刷新＋日付欄
- 「フォントがAIっぽい」FB→**Zen Old Mincho（見出し・数字）＋Zen Kaku Gothic New（本文）に統一**。AI感の元凶Noto Sans JP(本文)とShippori Mincho(見出し)を置換。JetBrains Monoは英字ラベルのみ残す。
- 日付欄：左ブロックをflex縦中心にし右metaと中心線を揃える。「木」のフォントが**JetBrains Mono(英字用)指定で日本語がフォールバックしていた不具合**を修正→Zen Kaku Gothicのピル表示に。数字36px/900。
- ※LP見出しは追記10で「明朝維持」と決めたが、今回Shippori→Zen Old Minchoへ更新（明朝路線は維持しつつ字面を刷新）。

### 追記23（2026-07-06・b6ba4f4）＝フォント残存フォールバック一掃ほか
- JetBrains Mono指定で日本語がフォールバックしていた10箇所（残席バー/タイムテーブル時刻/開催概要キー/hero-kick/実績行/登壇タグ/写真キャプション/声/クロージング日時）をZen Kakuへ。**monoは英字キッカー（FOR YOU等）と登壇番号のみ**。
- Zen Old Minchoに存在しないweight800（ブラウザ疑似ボールド）→700修正（h2/hero-h1/closing h2）。
- **記事スティッキーのミニバナー廃止**（ユーザーFB：リボン＋記事末尾で十分・ヘッダーにくっつく）。イベント導線は2層に：①全画面リボン②記事末尾フルバナー。
- meet-hero.jpg 301KB→189KB（1360w/q78）＋`<link rel=preload>`＝LP初速改善。

### 追記24（2026-07-06）＝SQL適用状況の実証＋記事移管の吸い出し・写真一括取得
- **⚠️解消：未実行と記録していたSQLは両方とも実行済みだった**（本番プローブで実証）。①`meet_entries.groups`列＝存在確認（anon select 200）②cases photo_url＝case1/2/4が.webpに更新済み。**未実行SQLはゼロ**。教訓：⚠️は適用後にHANDOFFから消すこと。
- **ソコナラWEB（sokonara.co.jp）はSTUDIO製＝静的取得では本文が読めない**。記事本文はブラウザ描画（Chrome拡張get_page_text）で全文取得可能。**全8記事吸い出し済み**（日野出/日野・増田桐箱店/藤井・同/中村・トランスパック/貝原・松永/河野・柳川合同/荒巻・ミックウェア/村岡・イトキュー/中原）→ `1_Claudecode\sokonara\articles_raw_2026-07-06.md`。次はユーザーがNotionで記事化→admin登録。⚠️イトキューは元記事に表記ゆれ（タイトル=中原正臣/本文=中原理臣）＝記事化時に要確認。
- **写真の一括取得手法（確立・再利用可）**：ページHTMLの `vmid="{pageViewId}_..."` からIDを取り、`https://storage.googleapis.com/studio-publish/projects/VGOK5QJkan/Ra4boAKVOD/page-views/{id}.json` を取得→JSON内の `studio-design-asset-files` URLが写真原本（クエリ無しで最高解像度）。ブラウザ不要・Python(urllib)で完結。
- **7社15ページから142枚DL済み**→ `1_Claudecode\sokonara\studio_photos\`（会社別フォルダ・3社以上共通は_common）。対象＝増田桐箱店/日野出/トランスパック/松永製作所/柳川合同/ミックウェア/イトキュー。
- **会社スラッグ確定**：masudakiribakoten（fuji=藤井博文・nakamura=中村亮太）／hinode（hino=日野寿人）／trans-pack（kaihara=貝原義彬）／matsunaga（kawano=河野譲二）／yanagawagoudou（aramaki=荒巻陽佑）／micware-mobility（muraoka=村岡和裕）／itokyu（nakahara=中原正臣）。

### 追記25（2026-07-06）＝記事移管の実装完了（SQL実行待ち）
- **Notion完成記事8本を検証→修正**：parseNotion仕様と突合し全記事フォーマット準拠を確認。修正9箇所（「議事メモによれば」等の内部用語露出6箇所削除＝藤井/中村/柳川×2/イトキュー×2、誤字3箇所＝老舌卸→老舗卸×2・大手食会社→大手食品会社）をnotion-update-pageで直接修正済み。
- **写真22枚を本番反映**（0df62a2）：カード用 `leader_{hino,fuji,nakamura,kaihara,kawano,aramaki,muraoka,nakahara}.jpg` ＋本文はめ込み `article_*_{a,b}.jpg`。**人物特定はpage-view JSONの設計ツリーで「〜さんにお伺いしました」直近画像＝公式ポートレートを機械特定**（kaiharaのみストック写真誤ヒットのため目視で修正＝作業着で電話中の写真）。⚠️STUDIOサイトには**ソコナラ汎用ストック写真（同一撮影の面談風景）が複数社に別URLで混在**——URL固有でも本人とは限らない。
- **登録SQL生成済み＝⚠️Supabase実行待ち**：`1_Claudecode\sokonara\register_articles_2026-07-06.sql`（8名INSERT＝ON CONFLICT DO NOTHING＋既存2社のsort UPDATE）。実行後に本番プローブで検証すること。
- **設計**：companies.bodyはセクション配列のJSON文字列（type:section/qa）。tags/q1/btags/rtags/wbは全部jsonb。sort＝日野出10/アド印刷20/松永30/トランスパック40/M・ZEC50/増田藤井60/増田中村70/柳川80/ミックウェア90/イトキュー100（LP順＋新3社を後置）。type=keieisha/plan=light/articleType=standardで統一（表示には未使用）。
- **カードタイトルは3案から私が1案選択済み**（ユーザー微修正前提）。wb={}＝未採点→admin📈マトリクスで「✨未採点のみAI推定」→一括保存。btags/rtags=[]→経営層編集でcardgen推定。
- 残タスク：①SQL実行（ユーザー）→②本番プローブ＋実機確認→③wb/btags/rtagsのAI推定（admin・ADMIN_PASSWORD要）→④カードタイトル・写真の微修正（ユーザーFB）
- ✅ 追記：SQL実行済み（本番プローブで8名登録・sort・body JSON整合を確認）。wb値もadminでAI推定・保存済み（貝原のみ全60＝未推定）。

### 追記26（2026-07-06・ab79c0a）＝仕事の軸レポート再構成（設計思想v2）
- **背景FB**：①現状マトリクスとbefore/afterマトリクスが別物で混乱 ②adminのWBマトリクス値が記事下部の軸に反映されない ③LINE詳細より共感フォームCTAを優先したい（form_open最大化）。
- **設計思想v2**（コード冒頭コメントに正本。index.htmlの`“仕事の軸”レポート`ブロック先頭）：
  - **1画面・1レーダー**：現在を表す線は種類1つ（あなた）。①求める軸（塗り）②いまの仕事（点線・比較ONのみ）。**旧「共鳴後の想定（企業wb由来の未来線＝projectWB）」は廃止**（“現在”の線が2種類ある混乱の根治）。
  - **「求める軸」＝2情報源の合成**：(a)共鳴した経営層の環境=`companies.wb`（adminマトリクス）が価値4軸(意味/裁量/人/地域)の**主ドライバー**（「この人に共鳴した＝この環境が強い軸を求めている」）(b)あなたの明示反応（モヤモヤ+16/求めるものQ2+11/**刺さった言葉Q1共鳴+9**）で上乗せ。
  - **⚠️今回の根治**：`userWeights`で企業wbが**×0.28減衰の微調整**でしか効かず「admin値が反映されない」に見えていた→**ベース化＋係数0.75**（`w[k]=clamp(48+(cw[k]-58)*0.75)`）。admin値の変化が軸レーダーにはっきり出るように。
  - **時間・経済の透明性は維持**：time/econは企業wbを効かせず、Q2で選んだときだけ高い（＝なぜこの点かを必ず説明できる）。比較の「いまの仕事」線は事実（年収帯→econ・忙しさ→time・足りない選択→35）から。
  - **比較の意味が変化**：現状 vs 求める軸。Δ＝「いまの仕事が求める軸をどれだけ満たしていないか」＝転職動機の可視化（旧＝現状vs企業環境の未来像とは別物）。拡散画像には点線で載る（数値なし＝年収帯が透けない配慮）。
  - **CTA優先順位**：①**共感を伝える（フォーム直行=最重要）** ②シェア ③LINE詳細（**折りたたみに降格**・撤去はしない）。「この軸のまま、」等の意味不明な前置きは削除。
- **Q1→軸マッピング**：`Q1_AX_BY_INDEX=['meaning','people','agency','meaning']`（企業q1配列=[Why,Who,What,Life]順・Notion必須フォーマット準拠）。配線表の❌だったQ1が⭕に。
- **計測追加**：`wb_cta`（共感CTAタップ）＝①`card_logs_wb_cta.sql`（**⚠️Supabase未実行**）②card-log.js ALLOWED追記済み。H1検証＝軸レポート発のフォーム誘導効果。
- **⚠️要追随**：admin思想ページの「配線表」「顧客動線図」が旧仕様（共鳴後の想定線・0.28）のまま＝v2に更新が必要（未対応）。

### 追記27（2026-07-06・1f905cd）＝実機FB4件対応
- **①中嶋さんカードが小窓に出ない＝MPO形式が原因**：M・ZEC中嶋さんの写真が**MPO**（マルチピクチャJPEG＝一部スマホの3D/連写）で、拡張子.jpeg・Content-Type image/jpegでもブラウザ（特にスマホ）が描画できないことがある。→PILで標準JPEGに変換＋最適化し `images/leader_nakajima.jpg`/`hero_nakajima.jpg` として反映。**⚠️教訓：admin写真アップロードはMPOを弾くか変換すべき（未対応・他社が今後アップした時の再発リスク）**。他14社はJPEG/PNGで正常。
- **②スマホでヘッダーが全項目見えない**：mode-barが固定幅5ボタンで溢れていた→`@media(max-width:640px)`で**横スクロール可**（scrollbar非表示）＋ブランド余白/ボタンpadding圧縮＋右端フェード。全項目に届く。
- **③モヤモヤ逆引きで地域の荒巻さんが偶発枠に落ちる**：真因は**全社btags/rtagsが空**でタグ加点(+3)が効かず、キーワードも存在有無(+1)のみで"地域に濃い"企業が差別化されなかった。→(a)`score()`を**出現回数重み**(min3)＋本文走査700→3000字に改善(b)`fix_realfb_2026-07-06.sql`でbtags/rtagsを全社設定（**localは地域主テーマの荒巻/中原/中嶋/園田/井上に限定**＝配りすぎ回避）。シミュレーションで地域モヤ→荒巻2位(1位は"まちづくり創造業"中嶋)を確認。
- **④偶然カードに優先企業を高頻度表示**：`pf/index.html`のVS抽選を`shuffleArr`→**`weightedShuffle`**（Efraimidis-Spirakis・`key=random^(1/w)`降順）。`companies.pf_weight`（既定1・3-4で約3-4倍出やすい）。admin企業編集に「偶然カードの出現倍率」入力欄追加＋`toCompanyRow`/save/load/フォールバックstrip対応。pf は `select=*` なので新カラム自動反映。
- **✅ SQL実行済み**：`fix_realfb_2026-07-06.sql`（pf_weight ALTER・btags/rtags全社・中嶋photoUrl差替）本番プローブで反映確認（荒巻local付与・中嶋=/images/leader_nakajima.jpg）。pf_weightはadmin「偶然カードの出現倍率」欄で個社調整。

### 追記28（2026-07-06・30ceda9/9dc0368）＝VS選ばれたカード演出＋写真アップロードMPO対策
- **VSチャンピオンカード格上げ**（`pf/index.html`・実機スクショ確認済み）：カード名を**TOPヒーローと同じ`Zen Kaku Gothic New` 900**に統一。`finishVersus`の`#vsFinal`に**額装風金ヘアライン(#c9a95c)＋YOUR PICK金バッジ＋ポップイン＋一度きり光沢スイープ(champSweep)＋CTA行復活**（元カード踏襲）。`prefers-reduced-motion`対応。`leaderCardHTML(champ,{champion:true})`で出し分け。
- **写真アップロードのMPO根治**（`admin.html`・実機検証済み）：`normalizeImage(file,maxEdge,q)`＝**canvasで標準JPEGに再エンコード＋長辺リサイズ**。全4経路（プロフィール1600/ヒーロー1920/汎用uploadPhoto/本文D&D uploadPhotos）に適用。**MPO(2406KB)→標準JPEG(252KB・MPFマーカー除去)を実証**。失敗時は元ファイルにフォールバック。→今後スマホのMPO写真をアップしても中嶋さんの再発なし＋巨大画像も自動圧縮。
- **⚠️既存の他社写真**：貝原(2.8MB PNG)・園田(1.7MB JPEG)等は大きめだが正常表示（MPOではない）。気になれば admin で開いて再保存すればnormalizeImageで自動圧縮される。

### 追記29（2026-07-06・6f8bc73）＝admin思想ページをv2に更新（追記26の残タスク消化）
- **配線表**：企業wbを「28%注入」→「**価値4軸のベース（主ドライバー）**」に。**Q1刺さった言葉を❌→⭕（軸に+9）**。モヤモヤ+22→+16・Q2+12→+11に更新。**wb_cta行（H1検証）追加**。「設問が軸に効かない＝反映されない感」の議論見出しを「v2で解消済み」に。自由記述移設・Q1接続を解決済みに反映。
- **比較の意味**：「いま vs 共鳴後の想定」→「**いまの仕事 vs 求める軸のギャップ（転職動機の可視化）**」。
- **顧客動線図(SVG)**：**WORKING HINT(要約・無料)ステップを廃止**（AI文章はLINE後・無料は軸レーダー即表示）→反応→仕事の軸を直結。LINE登録/詳細レポートを軸レポートから分岐する配置に。企業wbの矢印ラベル「共鳴後の想定に内部使用」→「求める軸のベースに反映」。座標検証済み（重なりなし・viewBox内）。
- **タイプ生成ロジック**：旧「スワイプカードのタグ集計＝btags上位2×rtags上位1」→現行「**求める軸の上位2軸→15種TYPE_NAMES**、site_settings.axis_typesで編集」に修正。
- ⚠️残：思想ページ内の理論カード（RIASEC/シャイン等）は据え置き（btags/rtagsは今も稼働のため妥当）。

### 気づき（設計レビュー・要検討）
1. **求める軸は今や6割方が「共鳴した経営層のwb」**。同じ経営層を選べば別人でも軸が似る＝「あなたの診断」というより「この環境のプロファイル」寄り。ユーザー反応(モヤ+16/Q2+11/Q1+9)で最大+36動くので差別化は残るが、**"診断と名乗らない"建前と実態(企業由来)の整合**は継続監視。将来「あなた由来/経営層由来」の内訳提示も一案。
2. **Q1→軸マッピングが配列インデックス依存**（`Q1_AX_BY_INDEX=[Why,Who,What,Life]`）。company.q1の並び順が違う/4件でないと誤爆。**Q1選択肢に軸ラベルを明示**する方が堅牢（parseNotionでq1に軸メタを持たせる案）。
3. **pf_weight（偶然カード優先出現）と"計画的偶発性"の緊張**。優先企業が3-4倍出ると多様性が落ちる＝偶然の出会いの価値が薄まる。倍率は控えめ運用推奨。モヤ逆引きの「意外枠」は一様抽選のまま維持済み（多様性の担保）。
4. **思想ページは"不変の思想"と"変わりうる実装配線"が混在**し、実装を変えるたびに陳腐化する（今回まさに）。将来は「思想（原則）」と「実装の現状（配線・係数）」を節で分離すると保守が楽。

### 追記30（2026-07-07・d8c3699）＝Q1→軸マッピングを内容ベース化（気づき2の実装）
- **旧バグ**：Q1（刺さった言葉）→軸が**スロット位置の決め打ち**（`Q1_AX_BY_INDEX=[Why→意味,Who→人,What→裁量,Life→意味]`）。日野さんのWhoスロット「自分の**裁量**で前へ進めたい」が人・近さに+9される等、**刺さった中身と軸がズレていた**（実データで日野[1]/藤井[3]/荒巻[3]が誤配線を確認）。並び順が違うと全軸ズレる脆さも。
- **修正**：位置決め打ちを廃止→ ①企業が **`companies.q1ax`** で軸を明示していればそれ ②無ければ**選択肢の文言からキーワード推定**（`q1KeywordAxis`）。time/econは対象外（Q2のみの透明性維持）。**既存全社が再登録なしで即改善**（index.htmlのフォールバックが効くため）。シミュレーションで日野[1]/藤井[3]/荒巻[3]が全て正しくagencyになるのを確認。
- **admin authoring**：Q1入力を**「軸：本文」形式**で編集（軸＝意味/裁量/人/地域）。fillMeta が軸を自動推定してプリフィル→書き手が確認・上書き→保存時に `q1`（クリーン）と `q1ax`（軸配列）へ分離。parseNotion は明示軸ラベルのみ軸として拾い、Why/Who/What/Life は軸扱いせず文言推定に回す（後方互換）。toCompanyRow/saveCompaniesフォールバック/体言止め変換/カードラベルchip も q1ax対応。プレビューでparseNotion往復を実証。
- **✅ SQL実行済み**：`q1ax_column_2026-07-07.sql`（companies.q1ax jsonb）。以後adminの経営層編集で「軸：本文」を保存すると軸が明示保存される。未設定でもindex側の文言推定で表示は改善済み。

### 追記31（2026-07-07・1cb3cb9）＝イベント導線を赤→ネイビー+ゴールドに刷新
- **背景**：MEETリボンの赤（コーラル〜クリムゾン）が「セール/警告/キャンペーン感」で世界観から浮き、告知が主役に見えていた（実機FB＋別AI指摘）。
- **判断**：ティール案は却下（本文メインCTA・ナビ下線・③ステップと同色＝色の役割が衝突）。**ネイビー+ゴールド（案B）採用**＝ヘッダーと馴染む“特別な招待”トーン。ティールは行動導線に温存。**MEET LPの金額装（白マット＋金ヘアライン）と統一**。3案をモックで比較しユーザー承認。
- **実装**：`index.html`の5クラスのみ変更＝3導線一括反映。`ev-ribbon`=navy #243a6b＋金上枠hairline／`ev-card`=白＋金枠／`ev-btn`=navy＋金枠（記事末尾）／`ev-pill`=金の細枠+金字（リボンの参加ボタン＝白ベタをやめ押しつけ感排除）／`ev-title-accent`=#a6801f。赤系のtext-shadow/絵文字彩度もnavy基調に。実機スクショで「赤消滅・ヒーローが主役・ティールはCTAに温存」を確認。
- 色替えは今後もこの5クラスのみ編集で全導線反映（navy案の色は据え置き記録）。

### 追記32（2026-07-07・5c1b024）＝記事→フォーム活性化（North Star改善のC+E）
- **課題**：記事を読んでもフォーム（form_open=メインCV）に行かない。**adminファネル実数で崖の位置を特定**：カード開始213→経営者選択151(71%)→記事読了141(66%)→**軸レポート10(5%・ほぼ素通り)**→興味P5到達13(6%)→**form_open 9(4%)**。⚠️**本当の崖は「記事141→興味CTA13＝9%」＝記事を読んだ人の91%がそもそもCTAを押していない**。P5→formは9/13=69%で通っている＝コピペ/外部フォーム摩擦(A/B)は本丸ではなかった（当初の私の見立てを訂正）。診断も5%でほぼ未使用＝「診断が興味CTAを奪う」のではなく両方未クリック＝活性化そのものが起きていない。
- **判断（ユーザー選択）**：B(フォーム内製化)=コンプラ不可／A(WEBCAS事前入力)=技術可否不明で保留／D(WEBCAS完了計測)=WEBCAS操作困難で保留／**C(フレーミング)+E(入口1本化)を実施**。
- **実装（記事の固定CTAバー＝崖の場所に集中）**：(1)2ボタン(診断/興味)対等→**1アクション化**（診断は小さな任意リンクに降格）(2)見出しを閉じたYes/No「気になりましたか？」→**経営層名入りの開いた誘い「◯◯さんに、あなたの&ldquo;いいな&rdquo;を届けませんか？」**（renderP1FromCompanyで名入れ）(3)ボタン「興味を伝える」(重い約束)→**「いいな」を届ける**(軽い動詞)(4)安心文言「匿名でもOK・応募や選考ではありません・30秒」を前に。**P5**：送信前の「共感が届きました🙌」(完了感で失速)→**「あと少しで、届きます」**＋STEP1/2のmono手続き感を①②に緩和。実機スクショ確認済み。
- **⚠️次の本命（未実施・141→13に一番効くと想定）**：「**匿名の軽い共感の一手→任意で連絡先**」の2段化。今は「共感を伝える(軽い約束)」と言いながらP5で氏名・メール(重い)を要求＝約束と中身のズレ。最初のタップを匿名完結にし、そのあと任意で連絡先深掘り。C/Eは文言で刈り取り、これは構造で本丸を狙う位置づけ。
- 計測の穴：form_openは「WEBCASに到達」までしか測れず実送信は不可視（WEBCAS完了計測=D未実施のため）。効果判定は当面form_openの相対変化で。

### 追記33（2026-07-07・d45277c）＝移管8記事の本文写真を増量
- **課題**：昨日移管した8記事は本文写真が各1〜2枚と少ない（既存記事は全6セクション=6枚＝各セクションに写真）。
- **対応**：`studio_photos`（各社13〜29枚）からコンタクトシートを作り目視選定→**34枚を最適化してリポジトリ配置**（`article_{key}_{c,d,e,f,g}.jpg`）→既存photoUrlを保持しつつ**空きセクションに順に割当**。全8記事が6/6（日野のみ6/7）に到達＝既存記事並みの密度。本番プローブ＋実機で反映確認済み。
- **選定方針**：ロゴ・スライド・ライフラインチャート・汎用ストック・既使用ポートレートを除外し、本文向き（工場/製品/物流/働く様子/対話）を選定。例=貝原:精密機械トラック/フォークリフト、荒巻:廃校/郷水トラック/隊列、中原:送迎バス/スポーツバス、藤井:桐箱/職人検品/端材オセロ。
- **⚠️ミックウェアだけ素材が弱い**（IT企業でスライド/ロゴ多く実写が少ない）＝談笑/交流会/勉強会の4枚止まり。先方から追加写真をもらえると締まる。
- SQL：`1_Claudecode\sokonara\add_body_photos_2026-07-07.sql`（実行済み）。写真選定の手法（コンタクトシート→目視）は今後の記事にも再利用可。

### 追記34（2026-07-07〜08）＝VS重複解消・採用事例ページ・内部端末フラグ・個人LP改修・カード視認性
- **VSで同じ会社を出さない**（b6681ac）：`pf/index.html`の`dedupeByCompany`で会社名重複排除（増田=藤井/中村、Schoo=井上/園田が1社1人に）。VSプールはシャッフルで1人だけ出す。
- **採用事例ページ＋TOP設置**（311cb56/bfb151b/24c7ccd）：`cases`テーブル（personal LPと同一・anon読取）をアプリ内表示。①ヘッダー「採用事例」→`p-cases`画面 ②TOPのABOUT直後に`casesGridTop`。`fetchCases`で1回取得を共用。**PCは2列固定**（casesGridTop/casesGridApp、モバイル1列）。見出し「出会いのあとに、動き出した人たち／経営層の話を直接聞いたことが、移住・入社・幹部候補への挑戦につながりました」。
- **⚠️内部テスト端末フラグ（②）**（df9d29f）：`?test=1`でその端末をlocalStorage内部化→card_logsを**is_internal=true**で記録、adminファネル/経営層別レポートで自動除外。`?test=0`で解除。pf=`_saveLog`1箇所/index=全書込5点に`_snInt()`/card-log.js ALLOWEDに`is_internal`/admin 3クエリで除外。**⚠️SQL未確認**：`1_Claudecode\sokonara\card_logs_is_internal_2026-07-07.sql`（①`ALTER card_logs ADD is_internal boolean`＝要実行 ②配布直前の`DELETE FROM card_logs`＝クリーン計測開始・コメントアウト中）。**運用**：配布前に三宅さん・アントプロさんの各端末で`?test=1`を1回開く→内部除外。
- **個人LP改修（景表法セーフ化＋アプリ実画面）**（512677a）：`personal/index.html`。比較表を**中立化**（vs従来の否定を排除・事実の対比）、PROBLEM/HERO/CTAを**否定形→肯定形・本人主語**、**VOICES新設**（MEETアンケートの生声・匿名）。**HEROにアプリ実画面のスマホmockup＋HOW4ステップに実スクショ**。スクショは**Playwright+ヘッドレスChromiumでローカル配信(localhost:8124)から撮影**（claude-in-chromeはapp/localhost共にブロック）→`images/app_{leaders,article,wb}.jpg`。**⚠️VOICESの生声は公開可否を三宅さん要確認**（本人特定されない範囲か）。CASESの名前付き実例には生声追加せず（要本人確認のため）。
- **カードのキャッチ視認性＝Dela維持でふち処理**（a9cb025→824cefd→ad35019）：`.leader-phrase`は**Dela Gothic One**（インパクト）で確定。Zenに変えたら「インパクト減」FB→Dela復帰。硬い4方向ふち取りは「ダサい」FB→**柔らかい暗ハロー(ぼかし影)＋写真下部グラデ0.88→0.93**で自然に読める形に。**index/pf/moya全カードで統一**（font=Dela18px/16pxモバイル・PC没入25px、字間.04em・行間1.6、text-shadow soft halo、overlay0.93）。カード名(.leader-name)はZen 900（TOPと一致）。
- **重要な学び**：フォント不一致FBの真因は「カードのキャッチが`.leader-phrase`＝Delaで、名前(.leader-name)だけZenにしていた」ズレ。カードは**名前=Zen900／キャッチ=Dela**の2フォント構成。
- **カード見た目を全カードで統一＋文言修正**（ad35019）：pf VSカード/チャンピオンのキャッチもDela18px+柔らかい暗ハロー+overlay0.93に統一（font linkにDela追加）。採用事例サブ「経営者の話を〜」→「**経営層**の話を〜」。
- **admin企業並べ替えをドラッグ化**（5efdf95→d97abad）：掲載企業一覧の各行に**つまみ⠿**、pointer eventsでマウス/タッチ両対応。離した順で`sort`10刻み再採番→`saveCompanies`でアプリにも反映。↑↓ボタンも残置。**⚠️実装注意**：move/upリスナーは`document`に付ける（つまみに付けてsetPointerCaptureだと実機で追従が切れて動かなかった）。`#coSortList`にイベント委譲。`initCoDrag`/`commitCoOrder`（renderCoList末尾で呼ぶ）。

### 追記35（2026-07-08・a8448e8）＝is_internal適用確定＋SecurityScorecard対策（HTTPヘッダ）
- **is_internal＝適用済み確定✅**：本番プローブで実証（`card_logs?select=is_internal`→クエリ成功で`[]`／存在しない列`zzz_nope`は`42703`エラー＝列は存在。anon読取は0行＝RLSで正しく遮断）。追記34の「実行済みらしい・要最終確認」は解消。残るは各内部端末で`?test=1`を1回開く運用と、配布直前の`DELETE FROM card_logs`（クリーン計測開始）のみ。
- **背景**：親会社がSecurityScorecardで外部セキュリティチェックを依頼予定→減点対策。
- **HTTPセキュリティヘッダを`vercel.json`に追加**（本番反映＆全6ヘッダ実証済み）：`Strict-Transport-Security`を`max-age=63072000; includeSubDomains; preload`に強化／`X-Content-Type-Options: nosniff`／`X-Frame-Options: SAMEORIGIN`／`Referrer-Policy: strict-origin-when-cross-origin`／`Permissions-Policy: geolocation=(), camera=(), microphone=(), payment=(), usb=()`／`Content-Security-Policy: frame-ancestors 'self' https://*.sokonara.co.jp; object-src 'none'; base-uri 'self'; upgrade-insecure-requests`。admin.html/admin に`X-Robots-Tag: noindex, nofollow`。
- **⚠️CSPは意図的に`default-src`/`script-src`/`style-src`を設定していない**＝inlineスクリプト・Supabase・GTM・Google Fonts・@vercel/ogを一切制限しない非破壊構成。設定するとアプリ全体が即死するため。将来フルCSP化するなら`Content-Security-Policy-Report-Only`で違反を収集→ホワイトリスト化してから本適用（要テスト）。
- **⚠️唯一の破壊リスク＝iframe埋め込み**：`frame-ancestors`は`*.sokonara.co.jp`を許可済みだが、`sokonara.co.jp`（apex・www）や外部サイトがLP（/personal/等）をiframe埋め込みしていると表示されなくなる。現状そうした埋め込みの証跡なし。もし埋め込み運用があれば`frame-ancestors`に該当オリジンを追記する。全ページ200・アプリ正常動作は確認済み。
- **Supabase anonキー（index.html内のJWT）は"漏洩"ではない**：公開前提のanon公開キーでRLSで保護済み。スキャナが「HTML内にAPIキー」と指摘しても対処不要（SERVICE_KEY等はVercel環境変数で非公開）。
- **⚠️DNS側は sokonara.co.jp（＝ソコナラ自身の管理・親会社は不掛。親会社はスキャン依頼のみ）＝三宅さんがレジストラ＋Google Workspace管理コンソールで直せる**。2026-07-08にMX実測で**実害が判明**：
  - **DNS管理＝お名前.com**（NS=`01〜04.dnsv.jp`）。SPF/DMARC/DKIMのTXTは全部お名前.com Navi→ドメイン→DNS→DNSレコード設定で編集。⚠️SPFはTXTで1つだけ＝既存値を「置換」（新規追加で2つになると無効化）。
  - **メール基盤＝Google Workspace**（`MX = smtp.google.com`。`y.miyake@sokonara.co.jp`はGoogle経由）。SPFに`_spf.google.com`が無い（`include:webcas.net`だけ）→本業メールがSPF未保護＝SecurityScorecard減点＋Gmail/Outlook宛の迷惑メール判定リスク。
  - **① SPF（既存1件を置換）**：`v=spf1 include:_spf.google.com include:webcas.net ~all`。⚠️`-all`は他送信ツール（メルマガ/CRM等）を全部SPFに入れ切るまで使わない。**2026-07-08時点：お名前.com管理画面に新値入力済みだがライブDNSは旧値のまま＝保存未完了。要保存完了**。
  - **② DKIM＝✅既に設定済み（先の「未設定」は誤り・訂正）**：セレクタ`mail2025._domainkey.sokonara.co.jp`に有効なRSA鍵がライブ存在（当初`google`セレクタだけ見て未設定と誤判定）。作業不要。⚠️後日任意確認：この鍵がGoogle用かWEBCAS用か（Google管理コンソール「メールの認証」のセレクタが`mail2025`ならGoogleメールもDKIM署名済み。違えばGoogle分を後日追加）。
  - **③ DMARC**：現状`v=DMARC1; p=none`→`rua=mailto:...`でレポート収集を追加、整合確認後に`p=quarantine`→`p=reject`へ段階強化。
  - ⚠️**お名前.comの「ドメインプロテクション」（1,364円/税込）はDNSレコード編集に不要な別サービス**（不正移管/NS変更/登録者変更のロック。TXT編集は保護対象外＝無料でSPF保存可）。申請フローに自動で入っていたら外してよい（2026-07-08にユーザーが遭遇）。
  - WEBCASはこのまま動作（`include:webcas.net`残置）。
- 本番プローブ手法：`curl -sI https://app.sokonara.co.jp/?v=$(date +%s)`でヘッダ確認。DNSは`nslookup -type=TXT sokonara.co.jp` / `_dmarc.sokonara.co.jp`。

### 追記36（2026-07-08・7b7792b/c8baed7）＝個人LP広告CRO改修（別AI提案の吟味→採用分を実装）
- **背景**：別AIから広告用CRO提案（思想LP→安心して1回触らせるLP）。実LPと突合し、既済/在庫都合/やり過ぎを切り分けて採用分のみ実装。全て本番反映＆プレビュー検証済み。
- **FVコピー刷新（❷折衷＝明快さ＋偶然性を1語残す）**：H1「条件だけでは、出会えなかった。／福岡・九州の“経営層”と、"偶然"、出会う。」。別AIの「偶然性を全部消す案」は独自性が死ぬので不採用（OG/製品思想と一貫）。
- **CTA統一**：本文中CTA＝「無料で経営層カードを見てみる」／ナビ・フロート＝幅の都合で動詞を揃えた短縮「無料で見てみる」。`出会う/出会ってみる`の不統一を解消（軽い動詞＝追記32方針と一致）。
- **FV CTA直下に安心文言**「登録不要・無料 ／ 応募・選考ではありません（約1分）」。FINAL CTAも同旨。
- **1分/3分の不整合を約1分に統一**。計画的偶発性の**理論名だけ削除**（概念は残す）。比較表トーン緩和（リード「両方使ってOK」・見出し「一般的な転職活動」）。
- **CASES（実例）をFEATURES直後＝上部へ移動**（social proofを早く見せる）。Pythonで68行ブロックを決定的に移動→プレビューで順序・casesGrid4枚ロード・本番プローブ（REAL STORIES＜FOR YOU）確認。
- **⚠️既存バグ発見（未修正）**：`personal/index.html`の`<section id="how" class="section"`に**閉じ`>`欠落**（他セクションは全て有）。HOWのcontainerがsection属性に飲まれる。要単独修正（レイアウト変化の可能性ありで分離）。
- **⚠️FV背景写真＝`/images/TOP.jpg`（商用ストック調）**。三宅さんからMEET実写真3枚提供（過去マッチング会の対話写真）。判定：**2枚目推奨**（ジャケットで笑顔・前傾＝温度感最高／「選考じゃない」と矛盾しない）、1枚目=代替、**3枚目=スーツで商談/選考に見えFV不可**（後半のMEET枠向け）。差替は①**写っている本人（参加者＋経営層）の肖像権/掲載許諾**②選定画像を**Tempリポジトリ`/images/`へ配置**（チャット貼付は本番に出ない＝画像ルール）が揃ってから、`#hero`の`background:url()`1行変更。許諾不明なら顔特定不可カットに留める。
- 未採用（低優先A・要判断で保留）：共創QUOTEを後半へ／こんな方へをFV直下へ（CASESと両立不可のためCASES優先）。

### 追記37（2026-07-08・複数commit）＝個人LP実機FB第2弾（構造バグ＋コピー刷新）
- **HOWの`>`欠落バグ修正✅**（追記36で発見分）。HOWにcontainerが効くように。
- **CASESグリッド余白修正✅**：4枚が3+1で並ぶ余白問題→`#casesGrid`をCSSで desktop4列/≤900px2列/≤520px1列に（inlineのgrid-template-columns撤去）。orphanなし。
- **番号フォント脱AI化✅**：`.step-num`/`.problem-num`のJetBrains Mono（01/02がAIぽい）→本文フォント（Noto Sans）に。
- **コピー刷新✅**：こんな方へ4文を具体的に／PROBLEM見出し「こんな気持ち、ありませんか？」→「思い当たること、ありませんか？」／「いつもの円」→「同じ顔ぶれの中だけ」／problem01「働く相手で、選べていない」。
- **比較表を撤去→特徴リスト化✅**：「一般的な転職活動」との対比列を削除（**求人サイト直接比較=法的整理の疑義回避**・ユーザー指摘）。「ソコナラの、はじまり方」5特徴の単カラムリストに。**話す相手の不正確さも修正**（「経営層本人」→「まず経営層本人へ（内容により採用担当の場合も）」＝必ず本人とは限らない）。
- **全角“”を全廃✅**（AIぽいとのFB）：静的HTML43個→0。VOICES装飾大引用符divも撤去。「」（鉤括弧）は正用として残置。
- **FAQ 9→5に削減✅**（離脱防止・無料/登録不要/転職前提でない/匿名/応募でない を残す）。**VOICES 4→3に削減✅**（文字だらけ緩和）。
- **⚠️残：CASESカードの“”はDB由来（`cases`テーブル本文）で本番に8個残る**。HTMLでは直せない＝`cases`テーブルのUPDATEが必要（次回SQL生成→ユーザー実行）。
- **⚠️WEBCASフォーム＝氏名フルネーム必須**（添付画像）。アプリ/LPは「匿名OK」なので**約束と中身のズレ**（追記32の崖の一因）。**私の推奨＝氏名を任意（またはニックネーム可）に**＝摩擦減でCV改善。WEBCAS側設定なので三宅さん作業。
- **FV背景`TOP.jpg`のMEET実写真差替は保留**（ユーザー判断で今回は現状維持）。ENCOUNTERS/共創QUOTEは据置（共創は「」使用で残置）。

### 追記38（2026-07-08）＝個人LP実機FB第3弾（殺風景解消・直リンク判断）
- **FEATURESリストをアイコン付きカードに刷新✅**（「枠に文字を置いただけ」FB→白カード＋テアイコン円5種＝はじまり方/選ぶ軸/出会い方/話す相手/費用）。`.feat-row/.feat-ic/.feat-k/.feat-v`。
- **ENCOUNTERSテーマカードに人物線画イラスト注入✅**（殺風景FB→右上に2人の線画SVG `.tc-deco`・opacity.16・z-index0でテキスト背面）。
- **ENCOUNTERS→各経営層ページ直リンクは実装せず（判断）**：氏名社名を伏せる好奇心ギャップ＝計画的偶発性の入口を壊す/「ピンときてから開く」原則と矛盾するため。ティーザー維持。
- **CASESの“”はDB由来だがユーザー判断で対応不要**（追記37の残タスク①は取り下げ）。
- **使い方(HOW)画像を"ぼかし依存"から"名前が写らない専用クロップ"に刷新✅**（cf22dc4）：初回はPILぼかし＋object-positionスライスで対応したが「ぼかしがずれて安っぽい・上が途中で切れて雑」FB→**方針転換**。`images/how_01〜04.jpg`を新規作成し各ステップに割当（`object-fit:cover;object-position:center`）。
  - `how_01`=app_leadersのカード写真+キャッチのみ（名前部を含まない上部をcrop）／`how_02`=記事の写真／`how_03`=CTAの「匿名OK行＋緑ボタン」（氏名も"仕事の軸"の“”も含まない）／`how_04`=**レーダー**。ぼかし不要＝名前が最初から写らない領域だけ切り出す方針でクリーンに。
  - **how_04レーダーは`/api/og?d=6値.区切り&t=ソコナラ`（軸カード動的OGP）から取得→レーダー部分をcrop→navyで横長パディング**。⚠️`t`に未サブセットの文字（例「まちの探究者」）を渡すと豆腐文字化け→サブセット内の文字（`ソコナラ`等）を使う。
- **FV mockup(app_leaders.jpg)のみ名前ぼかしを残す**（実アプリ画面の演出のため）が、モザイク調→**滑らかなGaussianBlur(10)＝フロストガラス調**に変更（安っぽさ解消）。座標`(100,462,258,538)(372,462,518,538)(100,1044,258,1122)(372,1044,522,1156)`。
- app_article.jpg/app_wb.jpgは旧ぼかし版がcommit済みだが**現在LP未参照**（how_*に置換したため）。元画像は`git checkout <commit>~ -- images/xxx.jpg`で復元可。
- **PILは環境にあり／Playwrightは無し**。座標調整の手法＝`crop→_cal_*.jpgで目視→補正`、複数画像確認は`_sheet.jpg`（コンタクトシート）が有効。

### 追記39（2026-07-08）＝使い方画像モバイル修正・記事写真の重複解消・case3差替
- **case3画像差替✅**：ユーザーが`case3.png`(3.78MB)を更新→1200×675jpg(156KB)に最適化して`images/case3.jpg`上書き。DB photo_url同名で変更不要。
- **使い方(HOW)画像がモバイルで崩れる→根治✅**：固定`height:200px`枠に横長画像でモバイル(狭い2列)が縦長枠になり酷いトリミング→**枠を`aspect-ratio:3/2`に変更＋4枚を900×600(3:2)で作り直し**（how_01=カード写真/02=記事写真/03=CTAボタン/04=レーダー。レーダーは`/api/og`から生成）。両デバイスで歪みなし。
- **⚠️記事本文写真の重複を発見・修正✅**（ユーザー指摘）：aHash比較で**8社が同じ写真を記事内に2回使用**（追記33の本文増量時にa/bを別名c/d/e/fで二重保存＝byte違いだがaHash一致＝別圧縮の同一画像）。fujiのみクリーン。→`studio_photos`から企業実写を9枚選定し**ファイル内容のみ差替**（body/DB不変）。差替後にaHash再チェックで重複ゼロ確認。選定：距離≥24で候補抽出→白率/彩度でロゴ/スライド除外→コンタクトシート目視。⚠️aramaki/muraoka/nakaharaは企業実写が乏しく人物/作業シーンで代替（追記24の汎用ストック混在注意）。

### 追記40（2026-07-08・fedc444）＝個人LP文言のadmin編集機能✅実装
- ユーザー要望「adminで編集できるようにしてくれたらやる」→スコープ「**主要コピー**」で実装。
- **方式（MEET LPと別・data-lp自動検出方式）**：`personal/index.html`の編集可要素に`data-lp="key" data-lp-label="ラベル"`を付与（60要素/58ユニークkey）。LP起動時に`mergePLP()`が`site_settings.personal_lp`を取得し`[data-lp=key]`へ`innerHTML`上書き（loadCases隣・同じanon KEY）。**admin側は`/personal/`をfetch→DOMParserで[data-lp]を自動収集**するので、フィールド追加時にadmin側改修不要（LPにdata-lp足すだけ）。
- **admin新タブ「👤 個人LP文言」**（`ni-plptext`/`renderPlpText`/`plpLoad`/`plpForm`/`plpSave`）：📥現在の文言を読み込む（LP fetch＋DOMParse＋保存済みsite_settings重ね）→2カラムtextarea編集→💾保存して公開（`adminWrite('site_settings','upsert',{key:'personal_lp'})`）。**空欄or既定と同じ欄は保存しない＝空にすると既定へ戻る**。メインCTAは`cta_main`で全3ボタン連動。
- **site_settingsテーブルは既存（MEET LPで作成済み）→SQL不要**。`personal_lp`は新keyのみ。
- 編集可フィールド：FV見出し(br/em可)/サブ/注記/メインCTA、こんな方へ4、PROBLEM見出し+リード+3組、はじまり方見出し+リード+特徴5+注記、VOICES見出し+リード+3、経営層セクション見出し+リード+フレーズ4、実例見出し+リード、使い方見出し+リード+ステップ4組、FAQ見出し+質問5、引用文、最終CTA見出し+サブ+注記。⚠️FAQの回答文とENCOUNTERS/CASESの動的部分は未対象（必要ならLPにdata-lp追加で拡張可）。

### 追記41（2026-07-08・3438219）＝カードフレーズA/B（cc_*）の整理・エディタ拡張・8社投入
- **カードフレーズの構造**：`catchphrase`（○○な人へ＝主・一覧カードの表示）＋`cc_what`(What:事業)/`cc_value`(Why+Who:価値観)/`cc_life`(Life:転機・"人へ"にしない)の**A/Bバリアント3種**。表示＝`/pf/`は`cc_{arm}`出し分け（arm=what/value/life）→無ければcatchphraseフォールバック（pf/index.html:588）。**index.html(メインアプリ一覧カード)はcatchphraseを直接使用（cc_*未接続）**。→ **catchphraseは残す**（消すとメインアプリが壊れる）。cc_*は出し分け用の追加。
- **充足状況**：元7社（石倉/import系5/co_1781…）は4パターン全部あり。移管8社はcatchphraseのみだった→**8社分のcc_*3案を作成**（既存7社の型に統一：What/Value＝〜人へ、Life＝転機の一言）。**⚠️SQL（8社UPDATE cc_what/value/life）は三宅さんがSupabaseで実行予定**（本チャットにSQL提示済み・catchphraseは不変）。実行後に本番プローブで検証すること。
- **記事管理エディタ拡張✅**：`renderArticleEditor`（📝記事管理）の「カードフレーズ」欄の直下に**3バリアント欄（art-cc-what/value/life・textareaで改行編集可）**を追加。saveArticle→cc_what/value/life保存、fillMeta/openArticleEditorで既存値ロード（Notion再解析ではcc_*を消さない`if('cc_x' in meta)`ガード）。toCompanyRowは既にcc_*対応済み。※別の`renderAddCo`エディタにも同3欄あり（単一行input・改行なし）。

### 追記42（2026-07-08・63f4e92）＝WBマトリクスが個別ページに反映されない不具合を修正
- **症状**（ユーザー指摘）：adminの📈Well-beingマトリクスで編集したwbが、経営層の個別ページ（仕事の軸レポート）に反映されない。
- **原因**：`getCo()`（index.html:4417）が`loadCompanies()`＝**メモリ/localStorage `sn_companies`キャッシュ**から企業を取り、`companies.wb`もキャッシュ値を使用。起動時`loadCompaniesAsync`で更新されるが、アプリを開いたまま/キャッシュ残存だと古いwbのまま。
- **修正（都度連携）**：レポート表示`openWellbeing`の`renderWbMain()`直前に、`supabaseClient.from('companies').select('wb,btags,rtags').eq('id',selectedCompanyId)`で**その経営層のwbを都度取得→`getCo()`の企業オブジェクトの`c.wb`を差し替え→renderWbMain再描画**。キャッシュに関係なくadmin最新値が即反映。本番プローブ＋実データ(wb 6軸取得)確認済み。
- ⚠️p3ミニ(fbWbMini)/day.htmlのレーダーは別経路（今回は主レポートのみ対応）。同様の古さが出たら同じ都度取得を足す。

### 追記43（2026-07-08・25865ca）＝admin「保存済み写真から選ぶ」機能追加
- 背景：貝原さんのカード写真が別人。ユーザー要望「アップロードだけでなく、既にStorageに保存済みの写真から選んで差し替えたい（実質同じバケットを覗くだけ）」。
- **`openPhotoPicker(bucket,prefix,urlInputId,previewFn)`**（admin.html）＝`supabaseClient.storage.from(bucket).list(prefix)`でバケット一覧→サムネイルグリッドのモーダル→クリックで`__pickPhoto`がURL欄に公開URLを入れてプレビュー更新。記事管理エディタの**カード写真（company-photos/companies）とヒーロー画像（article-images/articles）両方に「📁 保存済みから選ぶ」ボタン**追加。
- 現状 company-photos/companies に11枚。貝原の正しい写真がバケットに無ければアップロード（追記25：貝原＝作業着で電話中の写真がstudio_photosにあり）。選択後は必ず「Supabaseに保存する」で確定。

### 追記44（2026-07-09）＝VOICES参加者アイコン（イラスト）＋how_02中嶋＋記事記号クリーン
- **VOICES参加者アイコン✅**：SVGシルエット→`studio_photos/_common`のキャラクターイラスト（男女アバター）を透過合成＋頭肩クロップで`images/voice_1/2/3.jpg`（160×160）を作成、40px丸で表示。ユーザー提案の`_common`イラストを採用。
- **使い方02の写真を中嶋さんに✅**（日野が低解像度）：`hero_nakajima.jpg`(1400×1050)を3:2クロップして`how_02.jpg`差替。
- **⚠️記事本文の記号クリーンSQL（三宅さん実行）**：body内に`**`(Markdown太字マーカー)52個＝AIぽい、`“”`(全角引用符)17ペア＝強調。→SQL提示済み：`UPDATE companies SET body=replace(replace(replace(body,'*',''),'“','「'),'”','」') WHERE body LIKE '%*%' OR body LIKE '%“%' OR body LIKE '%”%';`。全15社で変換後もJSON有効を実証。アポストロフィ`'`は英語スローガン`Let's be happy`内なので残す。セミコロンは0。⚠️catchphrase/quote/cc_*の“”は未対象（記事内のみ・希望あれば同様に）。実行後に本番プローブで検証。

### 追記45（2026-07-09・afe4921）＝MEET LP抜本デザイン改修（CV単価5万円→数千円狙い）＋Temp消失復旧
- **⚠️Temp消失再発**：`Temp\sokonara`の`.git`＋一部ファイルが消失→`rm -rf`後に`git clone`再取得＋git config再設定で復旧（全作業push済みで損失なし）。CLAUDEルール通り。
- **MEET LP改修（`meet/index.html`のCSSのみ・EV/構造/文言は不変）**：ユーザーFB「フォント含めデザインが堅苦しい、CV単価5万円と高騰、数千円狙いで徹底改修」。主因＝**見出しが全部Zen Old Mincho(明朝)＝格調高いが敷居が高く見える**＋ダーク基調＋密度。
  - **明朝を全廃**→`--mincho`変数をゴシックにエイリアス（全`var(--mincho)`が即ゴシック化・ゼロリスク）。font linkからZen Old Mincho削除、Zen Maru Gothic(丸ゴシック=親しみ)追加。見出しウェイト700→800/900＋字間タイト化。
  - **配色を明るく**：teal `#2f9c8d`→`#12a58c`(鮮やか)、paper明るく、`--coral`追加(未使用・将来のエネルギー差し色用)、金の額装トーン抑制。
  - **ヒーロー軽量化**：min-height 96vh→84vh(FVでCTA到達)、オーバーレイを薄く(会場写真が明るく見える)。CTAボタン拡大＋角丸14px＋強シャドウ。dateの数字は丸ゴシック。
  - ブラウザプレビューで全セクション（hero/aud/speakers/closing）を目視確認・コンソールエラーなし・本番プローブで明朝0/新フォント導入を実証。
  - ⚠️プレビューは`mcp__Claude_Browser__preview_*`（`mcp__Claude_Preview__`は当セッション不可）。sokonara-tempで8124配信。
  - 次の一手候補（CVなお高いとき）：①構造/導線の見直し②coral差し色でエネルギー③FVコピーのA/B。まずは今回の刷新の効果を実データで見る。
- **追記45b（4a5ccab）＝MEET LP全セクション監査＋ポリッシュ**：ブラウザプレビュー(mobile375px)で全セクション目視監査→修正：①**日付ボックス再設計**（ユーザー指摘のアンバランス）＝左「2026/7.30/木」一体化＋右「時間＋参加無料テアバッジ」、背景を半透明白→frosted navy(0.52)で写真上でも可読。②**金の額装ヘアライン除去**（scene/case画像のgoldアウトライン→none、キャプション線gold→teal）＝カジュアル路線に統一。③**Why3項目に番号01/02/03**追加＝視認性。monoやアバター(匿名silhouette=意図的)・trust strip等は現状維持と判断。全て本番プローブ実証済み。
- **追記45c（6e169e4）＝MEET LPフォント文字化け修正**：三宅さん「フォントが文字化けしてるものがある」報告。原因＝`Zen Maru Gothic`（追記45で日付数字用に追加）が**ロード失敗**（`document.fonts.check('900 16px "Zen Maru Gothic"')`＝fonts.ready後もfalse。他2フォントは正常・ページ内の置換文字U+FFFDは0）。ロード不能なフォント指定が端末により文字化け／崩れの原因。対応＝**Zen Maru Gothicをlinkと`--round`から完全撤去**、日付「7.30」は確実にロードされる`Zen Kaku Gothic New`(900)へ（太字で見た目ほぼ不変）。プレビューで.bigフォント解決＝Zen Kaku・置換文字0を確認、本番プローブでlinkが`Zen+Kaku+Gothic+New`＋`JetBrains+Mono`のみ（Zen+Maru消滅）を実証。※プレビュー上では視認可能な豆腐は再現せず＝端末依存だったが、根本原因フォントを除去して解消。
- **追記45d（131794b）＝MEET LP「フォント未反映」修正（真因）**：三宅さんが「2026」画像を提示し「文字化けではなくフォントの未反映」と明確化。真因＝**`JetBrains Mono`（英字/数字ラベル用）が端末でロードされずフォールバック表示**（＝以前も個人LPで「01,012がAIぽい」と指摘のあった、あのモノスペース）。対応＝**JetBrains Monoをlinkと`--mono`から完全撤去→`--mono`をZen Kaku Gothic Newへエイリアス**。影響ラベル＝`.kicker`(For You等英字ラベル)／`.datebox .yr`(2026)／`.spk-no`(01)／`.why .wn`(01/02/03)＝全てゴシックに統一。プレビューで4要素すべてfont=Zen Kaku Gothic New解決を確認、本番プローブでlinkが`Zen+Kaku+Gothic+New`**単独**（JetBrains/Zen+Maruとも消滅）を実証。**現MEET LPは日本語フォント1本のみ＝未反映が起きない構成**。※派生タスク：他LP(personal/biz/pf)や本体でJetBrains Mono/英字モノを数字に当てている箇所があれば同様の未反映リスク＝要点検（未確認）。
- **追記45e（2bb36d6）＝MEET LP導線＆FV視認性改善**：三宅さん「序盤に登壇企業情報が文字で来る段階で離脱しそう。雰囲気/成功事例の写真を早めに」「FVフォントの視認性が低い、読むの疲れる」。①**Scene（当日の雰囲気＝実写真）をFor Youの直後（2番目）に前倒し**。旧順=Hero→ForYou→Speakers(匿名シルエット＋文章)→Positions→Overview→Scene(6番目)で、写真到達まで文章4セクション＝離脱リスク。新順=Hero→ForYou→**Scene**→Speakers→Positions→Overview→TimeTable→Cases→Why→FAQ。Sceneはteal-soft背景で写真バンドを際立たせ、h2を「まずは、当日の雰囲気を。」に（早出し位置に合わせ）。Speakersは#fff→border-bottomのみ、Overview直下のTimeTableにborder-top追加（背景整合）。②**FV視認性**：オーバーレイを読む領域だけ濃く（0.72→0.82等）、`.hero-lead`を14px/行間1.95/白90%→**14.5px/行間1.8/font-weight500/純白/影強化**、H1影も0.4→0.55。プレビューでkicker順・重複IDなし・写真描画・font-weight500・コンソールエラー0を確認、本番プローブでScene<Speakers順序＋lead weight500を実証。※Speakersのアバターは意図的な匿名シルエット（本番前非公開）＝写真化は不可、だからこそ写真の早出しが効く。
- **追記45f（22d4488）＝MEET LP 登壇者に実写真アバター対応（三宅さん方針＝「顔は出す／社名は当日」）**：`.spk-av`を52→56px、`.spk-av img{position:absolute;inset:0;object-fit:cover;border-radius:50%}`追加、spkList描画で`s.photo`あれば`<img onerror="this.remove()">`（読込失敗/未指定はシルエットに自動フォールバック）。**本番は写真未指定＝シルエットのまま安全**、写真が揃えば即オン。プレビューで3人に仮写真注入して円形アバター＋番号バッジの見た目を検証済み。⚠️**重要な構造事実**：MEET LPの文言・登壇者データは**DB `site_settings.meet_lp` が正本**で、起動時に`Object.keys(v).forEach(k=>EV[k]=v[k])`の**浅い上書き**（meet/index.html:491）＝speakers配列ごと差し替え。よって**コードEVに写真を書いても出ない**。写真を出す手順＝①6枚の顔写真を正方形化して`/images/meet-spk-01..06.jpg`（speaker順）に配置しコミット②DBの`meet_lp.speakers`各要素に`photo`キーをSQLで追加③spkIntroの「※お顔は当日のお楽しみ」系文言を「顔は見せる/社名は当日」へSQL更新。⚠️admin「LP文言編集」がmeet_lpを再保存する際にphotoキーを落とす恐れ＝要確認（companies toCompanyRowと同型リスク）。**未完の残作業（三宅さん待ち）**：(a)6人全員の顔出し同意 (b)各登壇者(商社/DX,印刷/販促,製造/自動化,物流/梱包,建設/まちづくり,伝統産業/M&A)＝どの会社か のマッピング。studio_photosは7社あり(hinode/itokyu/masudakiribakoten=桐箱・伝統/matsunaga/micware-mobility/trans-pack=梱包/yanagawagoudou)。マッピング＋同意が来たら私が顔写真選定→クロップ→配置→SQL→本番反映まで実施予定。
- **追記45g（64c91b7）＝MEET LP 登壇者6人に実写真を実装（本番反映済み✅）**：三宅さんから氏名（登壇者順）＝①日野 寿人(日野出/商社DX)②古賀 照也(アド印刷/印刷販促)③河野 譲二(松永製作所/製造自動化)④貝原 義彬(トランスパック/物流梱包)⑤中嶋 佳奈恵(M・ZEC/建設まちづくり・女性社長)⑥中村 亮太(増田桐箱店/伝統M&A)。companiesテーブルの`ceo`＋`photoUrl`で各社の顔写真を特定→PIL(scratchpad/crop.py)で**顔中心の正方形240pxにクロップ**→`/images/meet-spk-01..06.jpg`。**⚠️貝原さんの正写真＝Supabaseの`company-photos/companies/1783328882793.png`（G'sACADEMYでスーツの方）**。ローカルの`leader_kaihara.jpg`（作業着・木材構内）は**別人（誤り）**なので使わないこと。実装方式＝**写真はコードEV(meet/index.html speakers[].photo)に持たせ、DB上書き後に`_codePhotos`でindex補完**（meet/index.html:487-）＝git管理・admin文言編集で消えない・SQL不要。`.spk-av`は写真+番号バッジ、読込失敗はシルエットにフォールバック。本番プローブでmeet-spk-01..06すべて200＋code反映を実証。※クロップ座標はcrop.pyのJOBS参照（調整時は再実行）。※スピーカー並び順が変わると写真がズレる（index対応）ため、admin等で順序変更時は写真indexも要調整。※spkIntroは現状「※登壇企業・登壇者の詳細は当日公開予定」のまま（顔は出す/社名は当日と整合。必要なら後日調整）。
- **追記45h（a439765）＝MEET LP 日本語の改行位置（禁則）調整**：三宅さん指摘（モバイルでクロージング付近が「ありませ／ん。」「福岡市中央区天／神エリア」と語中で折れる）。対応＝`.nw{white-space:nowrap}`を追加し、**意味のまとまりをnowrapで束ねて区切り（句点・／・スペース）だけで折れる**ように。修正箇所：①クロージング`.sub`＝2文をnw+`<wbr>`で文単位改行②`closeWhen`(日付/場所/無料)＝各chunkをnwにしinnerHTML化③`heroProof`(実績)＝／split各chunkをnw④`scarBar`(下部スティッキー)＝nw化⑤`.trust span`にwhite-space:nowrap⑥フッター住所の住所/ビル名をnw。モバイル375pxで全て良い位置で改行することを目視確認、本番プローブでnw/wbr反映を実証。※`word-break:auto-phrase`も検証したが当プレビューChromeで視覚効果なし＝不採用。※For You等のDB自由文（audience/hero-lead/spk-intro）の語中改行は残存＝admin文言編集で読点調整する領分（過剰nowrapははみ出しリスクのため触らず）。

- **追記46（77d7858）＝MEET班分け機能の刷新（本番反映済み✅／admin実機テストは三宅さん）**：三宅さん要件＝①卓＝登壇企業だけ（チェック選択）②admin で誰がどの班か名前で表示③テスト1..の実データ作成→割当。決定事項＝ローテーション維持だが**卓あたり人数は参加人数で均等割り(cap)を最優先**・チェック企業へは**なるべく**（必ずではない）／**当日ページにも登壇企業を連動**。
  - **卓＝登壇企業の選択**：admin「🎪 MEET運営」に「🎤 登壇企業（＝卓）を選択」カード追加＝企業チェック→`site_settings.meet_speakers`(value:{eventId,ids})に保存。`meetRoster()`はこのidsだけを卓に（選択順=卓番号）。**day.htmlも起動時にmeet_speakersを読み、その企業だけ表示＋選択順に整列**（卓と選択肢が一致）。未保存時はEV.companyIds→全社にフォールバック。
  - **アルゴリズム**（admin.html `computeGroups`）＝①均等優先グリーディ（最少人数の卓・希望はタイブレーク）②各巡の偏りを交換で**spread≤1**まで修復。cap=ceil(N/卓数)。巡数RはT超え不可でclamp、同卓再訪なし。Python検証(scratchpad/grp_test.py)で全ケース**均等spread≤1・再訪0・希望充足81〜100%**を確認。
  - **名前で見える**：自動計算プレビューに**全員名簿（氏名→巡目卓）**＋各巡人数分布＋充足率。保存後は「参加者一覧」に**「班（巡目→）」列**（`meet_entries.groups`の巡目卓番号、hoverでceo名）。保存(`saveGroupsUI`)後renderMeetAdminで即反映。
  - **テスト参加者**：「🧪 テスト参加者を作成」＝`meet_test_1..N`を実データ作成(/api/meet-log、event_id=`meet_2026_07_30`、name=テストi、登壇企業からランダムchecks)。「🗑 テスト参加者を削除」＝admin-data deleteでmeet_test_*一括削除。**本番前に必ず削除**。旧「ダミー(保存不可)」は撤去。
  - **データ**：`meet_entries`(id[`meet_`必須]/event_id/name/checks/deep/talk/groups…)。保存=/api/meet-log(ALLOWEDにgroups有)、閲覧=/api/admin-data(list)、削除=/api/admin-data(delete)。定数`MEET_EVENT_ID='meet_2026_07_30'`（day.html EV.eventIdと一致）。
  - **⚠️未テスト**：admin UIの実動作（認証＋バックエンドが要るためローカル不可）。JS構文エラー0・day.html描画OK・アルゴリズムPython検証済みまで確認。**三宅さんがadminで通しテスト要**：🎤で6社(日野/古賀/河野/貝原/中嶋/中村)選択→保存→🧪テスト作成→自動計算→保存→参加者一覧の班列＆当日ページ(6社のみ)を確認→🗑削除。※並び順を変えると写真index(追記45g)とは別に卓番号も変わる点に留意。

- **追記46b（bb9917d）＝当日ページに全社が出る件の原因と対処**：三宅さん「adminで6名に絞ったのに当日ページに全員出る」。調査＝DBに`meet_speakers`が**存在せず**、代わりに`meet_banner.companyIds`が同日02:23に6社で更新→**「📣 バナー」カードの「対象の経営層」チェック（見た目が同じ企業チェックUI）に保存された**と判明（UI配置の設計ミス）。対処＝①三宅さんに`meet_speakers`直接投入の**SQLを提供**（ids順=卓番号=日野→古賀→河野→貝原→中嶋→中村。co_hinode_hino/co_import_1776760337434/co_matsunaga_kawano/co_transpack_kaihara/co_1781331906205/co_masuda_nakamura）②admin UX修正＝**🎤登壇企業カードをバナーカードの上へ移動**（teal枠で強調・「未保存＝当日ページは全社表示のまま」赤警告・バナー側に「班分けとは無関係」注記）。※バナーの6社には中村でなく藤井(co_masuda_fuji)が入っている＝バナー対象としては本人たちの記事選択なので正しい可能性あり（増田桐箱店は藤井/中村の2記事）。SQL実行後に本番プローブでmeet_speakers確認→day.htmlが6社のみか検証すること。→**✅完了（2026-07-11）**：三宅さんがSQL実行→DBにmeet_speakers確認（6社・ids順=卓順）＋day.html実レンダリングで**6名だけ・卓番号順（日野→古賀→河野→貝原→中嶋→中村）**表示を検証済み。

- **追記46c（318ae14）＝当日ページに班分けの「🔄 更新」ボタン追加**：三宅さん「参加者側で割り振りの班がわからない」。従来は班取得がページ読込時のみ＝admin保存後に参加者が気づけない。対応＝①groupBox（YOUR TABLES）を**氏名登録後は常時表示**（未発表時は「班分けはまだ発表されていません。運営の案内があったら『🔄 更新』を押してください」）②ヘッダー右に**🔄 更新ボタン**（refreshGroups→loadMyGroups(cb)→トーストで結果通知「班分けを更新しました」/「まだ発表されていません」）③説明文言を新アルゴリズムに合わせ「チェックした経営層になるべく行けるよう配分」に修正（旧「必ず含まれています」は不正確）。ローカルで登録→ボックス即表示→更新クリック→取得中…→復帰→トーストまでフロー検証済み・本番プローブ反映確認済み。⚠️claude-in-chromeのjavascript_toolは**隔離ワールド**＝ページのJS変数(EV/M等)は見えない（DOMは共有・clickでページハンドラ発火はOK）。ページ関数の直接呼び出し検証には使えない点に注意。

- **追記47（fc5c3fa）＝MEET運営・当日ページ一括改修（三宅さん要望8件）**：①**発表者交代（中村→藤井）が反映されない件**＝またmeet_banner側に保存されていた（meet_speakers未更新）。対処＝SQL提供（藤井=co_masuda_fuji）＋**バナーの対象チェックを`<details>`折りたたみ化**（誤操作防止）。**MEET LP 6人目写真も藤井に差し替え**（leader_fuji.jpg→meet-spk-01..06のcrop.py再実行、本番200確認）。②**当日ページの経営層カードのフレーズ改行をアプリ一覧と統一**＝`.lcard-phrase`をアプリの`.leader-phrase`と同一タイポ（Dela Gothic One読込・18px/16px@480・lh1.6・ls.04em・word-break:auto-phrase・line-break:strict）。③**発表順の設定UI**＝admin🎤カードに↑↓並べ替えリスト（`_mspOrder`／mspToggle/mspMove/mspRenderOrder）。保存順＝卓番号＝当日ページのカード表示順。④**参加者の自己データ削除**＝day.html regDoneに削除ボタン→`/api/meet-log`に`action:'delete'`追加（id=端末秘匿値＝本人のみ）→localStorage消去→reload。本番でcreate→delete(204)→消滅をE2E実証済み。⑤**アンケートをクロージング用と明示**＝「🕘 ここから先は交流会のあとで」区切り＋svCardを点線枠+STEP 3 — CLOSING化。⑥**admin参加者一覧に選択削除**＝行チェック＋全選択＋「🗑選択した参加者を削除」(deleteSelectedParticipants)。⑦**氏名共有の質問③を廃止**（規約で同意取得済み・三宅さん判断）＝送信時に`consent:true`自動記録、STEP1の「氏名や連絡先等を…」以降も削除、番号繰上げ(④→③理由/⑤→④コメント)。⑧**アンケート後のCTAをLINE登録に変更**＝svThanksにLINE緑ボタン(lin.ee/qT5A0zN)主導線＋記事リンクは小さく従属。※ローカルでアンケート番号①〜④・区切り・削除ボタン表示を実機確認、本番プローブで全変更確認済み。⚠️**残**：meet_speakersの藤井SQLは三宅さん実行待ち（実行後に本番プローブ確認）。相談事項（アプリ×イベント連動・カード単位の共感チェック・理由タグ統合案）は提案済み＝三宅さんの返答待ち。

- **追記47b（ef5a231）＝アプリ×イベント連動 Phase1「チェック引き継ぎ」実装（本番反映済み✅）**：三宅さんKPI＝**参加者アプリ活用率50%／経営者との接点30%／アプリ再訪率25%**。day.htmlとアプリは同一ドメイン＝localStorage `sn_meet` を共有→renderCompanyListで`m.checks`を読み、**チェック済み経営層を一覧の先頭にソート＋「MEETで気になる ✓」tealバッジ**（.meet-badge）。ローカルで実フロー検証（チェック1件→アプリ一覧先頭＋バッジ表示）・本番プローブ済み。⚠️ローカルpython http.serverが頻繁に落ちる＝preview_start再実行で復旧（エラーページ相手にlocalStorage probeするとSecurityErrorになる罠に注意）。**Phase2以降（三宅さんと設計中）**：リアクション6種（＝WB6軸に1:1対応させる案を提示中）→当日ページ全カードに常設・チェックだけで成立/一言は任意。ネガ理由の収集は当日はやらない方針提案。Phase3＝アプリに「あなたの仕事の軸」恒久ページ（データ系譜の整理を三宅さんに説明済み：companies.wb=企業の軸[admin設定]／当日レーダー=チェック企業のwb重ね合わせ+理由タグ加点／card_logs=共鳴データ[企業側]で個人の軸には未使用）。

- **追記48（d7781d8）＝リアクション6種＋軸計算の一本化（Phase2+3・本番反映済み✅）**：三宅さんOK（「個人情報は残らない？」の確認あり→下記）。①**共通モジュール`/meet/axis.js`新設**＝AX/TAG_AX/centroid/radarを一元化。day.htmlとアプリの両方がこれを読む＝**「どのアウトプットがどのデータか」問題の根治**（day.htmlのローカル実装は削除・委譲）。②**リアクション6種＝軸キーと1:1**（meaning意義ビジョン/agency挑戦変革/people人柄雰囲気/local地域/time働き方/econ条件待遇）。当日ページ全カードに常設チップ（`.rxchip`・タップ複数可・チェックとは独立＝全社に付けられる）→`M.reactions={coId:[axisKey..]}`→meet_entries.reactionsへpush。**アンケート理由タグも同じ6キー＋undecidedに統一**（旧タグleader/biz等はTAG_AXに互換マッピング残し）。③軸の計算＝企業wb重ね合わせ(0.35)+リアクション(×0.7)+理由タグ。④admin社別の興味に**「共感ポイント」列**（会社×軸の集計チップ）、MEET_RTAGSに新6키追加(旧は「(旧)」表記)。⑤**アプリに軸バナー＋モーダル**＝経営層一覧上部にsn_meetがある端末のみ「🎯あなたの仕事の軸」→モーダルでレーダー＋チェックした経営層リスト（記事へ導線）。**端末内localStorageから計算・サーバー送信なし**。⑥検証＝ローカル通し（6カード×6チップ・タップ保存・アンケート送信→consent自動true・レーダー描画・LINE CTA・アプリバナー→モーダルのレーダーにタップ内容反映）＋本番プローブ(axis.js 200/rxchip/meetAxisBanner)。⚠️**要SQL（三宅さん）**：`ALTER TABLE meet_entries ADD COLUMN IF NOT EXISTS reactions jsonb;`（未実行でもAPIのPGRST204リトライでreactionsだけ落ちて他は保存される＝壊れないが、リアクションが保存されない）。**PII設計の実証**（追記48で3点プローブ済み）：anonでmeet_entries読取→空/書込→RLS拒否/API getはgroupsのみ返す。氏名はサーバーmeet_entriesのみ（admin認証閲覧限定）・端末localStorageは本人削除ボタンあり・アプリ連動は端末内完結。

- **追記49（0fdbeef）＝“軸”の統合＝「記事を読むほど軸が明確になる」（三宅さん承認・本番反映済み✅）**：①**累積ストア`sn_axis`**（端末内localStorage・PIIなし＝軸キーの回数と企業IDのみ）をaxis.jsに実装（readLog/appendHits[dedupeKey対応]/hasData/cumulative/matchTop）。②**記事診断の明示反応を累積へ配線**＝wb IIFE内に`window.SN_WB_LOGAXIS`（Q2_AX/MOYA_AX/q1Axisで軸キー化）→`submitEmpathy()`から呼ぶ（dedupe=SN_CARDS.logId＝復元・再表示で二重記録しない）。**診断レポート自体は従来どおり単発スナップショット**（表示ロジック不変・材料が裏で累積されるだけ）。③**全レーダーを`SN_AXIS.cumulative()`に統一**＝アプリの軸モーダル／day.htmlアンケート後（文言「今日の」→削除）／レコメンド。④**レコメンド**＝軸モーダル内「🧭いまの軸に近い経営層」上位3（matchTop＝軸の内積。**スコア/%は出さず重なった軸名チップだけ**＝企業を点数化しない原則維持）。バナー文言「あなたの仕事の軸と、軸に合う経営層」に一般化（表示条件=SN_AXIS.hasData()＝記事だけのユーザーにも出る）。⑤**admin設計思想ページに「“軸”の一本化と蓄積」カード新設**（入力→貯まる場所→効き方の表・出力3つ計算1本・レコメンドの見せ方・プライバシー・端末単位の限界とLINE連携の将来課題）＋顧客の動き図のMEET箱を「仕事の軸（累積）」に修正。⑥検証＝ローカルでappendHits/重複防止/**MEET共感と記事診断の合算**（meaning76=両ソース反映）/バナー→モーダル→レーダー→レコメンド3件（軸名チップ付き）/day.htmlレーダー描画/adminコンソールエラー0、本番プローブ全マーカー確認。※プローブのgrepで全角引用符含みパターンは失敗しやすい＝条件を分けて確認すること。

- **追記50（c9269b4）＝当日ページ：大型カード＋確定送信＋負荷対策（本番反映済み✅）**：①**経営層カードを1列・1画面1枚サイズに**（grid 2列→1fr、top高さmin(85vw,400px)、カード高≒画面の8割を375pxで実測622px/812px）。フォントは**この画面専用スケール**（フレーズ24px/21px@400・氏名18px・共感チップ12.5px・☆ボタン14px。書体はアプリと同じDela Gothic One維持）。②**「この内容で確定・送信する」ボタン**をSTEP2末尾に＝全量(name/checks/reactions)をupsertし**応答を確認して「✓サーバーに保存されました」**表示（自動保存が万一落ちても確定で全量上書き＝自己修復。失敗時はエラーtoast＋再試行可）。③**自動保存を800msデバウンス**（toggleCheck/toggleReactの連打を1リクエストに圧縮＝会場での書込数を大幅削減）。④**負荷実測**（本番）：/api/meet-log連続20書込＝平均466ms・最大3405ms（初回コールドスタート）・全件成功、companies読取0.15〜0.44s。⚠️「以前アプリが止まった」件の詳細（いつ・どの画面・何人）は三宅さんから未聴取＝聞けると対策の精度が上がる。当日リスクは主に会場Wi-Fi輻輳とSupabase無料プランの制限＝**開場30分前に運営端末で当日ページを一度開いて温めておく**運用を推奨済み。

- **追記51（b6a9e0a）＝傍聴者モード（本番反映済み✅）**：三宅さん「傍聴者もタップする可能性→班分けに入らない整理が必要」。①day.html STEP1に**「🎧傍聴のみで参加」チェックボックス**（登録後もボタンでいつでも切替可）。M.observer→meet_entries.observerへ保存（自動保存・確定送信の両方に載る）。②傍聴中は班ボックスに「傍聴のみで参加中のため班分けには入りません」表示・ckbarに（傍聴）。③**adminのcomputeGroupsでobserver===trueを除外**（班分けカードに明記）、参加者一覧に🎧傍聴バッジ＋班列「傍聴＝班なし」。④チェック・共感データ自体は傍聴者も記録＝企業レポートには活きる設計。⑤ローカル通し検証（傍聴登録→表示→参加へ切替→復帰）＋本番プローブ済み。⚠️**要SQL（三宅さん・下の2本まとめて）**：`ALTER TABLE meet_entries ADD COLUMN IF NOT EXISTS reactions jsonb;`（追記48）＋`ALTER TABLE meet_entries ADD COLUMN IF NOT EXISTS observer boolean;`（追記51）。未実行でもAPIリトライで該当列だけ落ちて他は保存される（壊れない）が、共感ポイント/傍聴フラグがサーバー保存されない。

- **追記52（0e5dcd6）＝アンケート刷新・共鳴改名・ボタン色（本番反映済み✅）**：①**アンケート①を「経営層ごとの共鳴度」に刷新**＝発表順（COS＝卓順）に経営層+企業名が縦に並び、横に**1〜5のタップセグメント**（同じ点の再タップで取消）。**4点以上＝紺色**で「個別に話を聞きたい意思表示」（注記明示）。送信時に`talk=[score>=4]`/`deep=[score>=3]`を**導出**（既存のadmin集計・個別連絡フローと互換）。生スコアは`scores` jsonbに保存。②**④友人紹介度（NPS 1〜5）**追加＝`nps`列。adminの統計カード「共有同意」→「紹介したい度(平均)」に差し替え、社別の興味に**「共鳴度Ø(平均+件数)」列**追加。③**「共感ポイント」→「共鳴ポイント」に全面改名**（Wantedly被り回避・三宅さん指示。アプリ既存の「共感を伝える」CTAは別機能なので不変）。④**☆気になるボタン＝teal縁取り**（グレー→押せるボタンと分かる色。選択=teal塗り+影）。⑤検証＝ローカル通し（6行発表順・5点紺/2点teal・talk/deep導出・nps保存・サンクス表示・rxラベル共鳴・ckボタン色computed確認）＋本番プローブ。⚠️**要SQL（三宅さん・累積4本）**：`ALTER TABLE meet_entries ADD COLUMN IF NOT EXISTS reactions jsonb; ALTER TABLE meet_entries ADD COLUMN IF NOT EXISTS observer boolean; ALTER TABLE meet_entries ADD COLUMN IF NOT EXISTS scores jsonb; ALTER TABLE meet_entries ADD COLUMN IF NOT EXISTS nps int;`

- **追記53（344bcc1）＝企業向けレポート出力（本番反映済み✅）＋SQL4本実行確認✅**：三宅さんがALTER4本実行→本番プローブでreactions/observer/scores/nps全列の書込を確認（dropped無し）。**admin MEET運営に「📄 企業向けレポート出力」カード**＝登壇企業を選択→印刷用A4ページを別タブで生成（ブラウザ印刷→PDF保存で配布）。構成：①貴社への反応サマリー（チェック数・共鳴度平均[イベント平均併記]・個別希望4点以上人数）②共鳴ポイント6軸の横棒グラフ③個別のご案内対象リスト（**氏名はチェックボックスでON/OFF可**・各自の共鳴度とコメント付き）④貴社に共鳴した参加者の声（3点以上のコメント引用）⑤イベント全体（参加登録[傍聴を分離表記]・アンケ回答・紹介したい度平均・理由タグ上位）⑥この後の流れ（ソコナラが個別接点調整）＋規約フッター。ダミーデータで生成HTML全要素を検証済み（window.openスタブ方式）。⚠️**後日アンケートの設計は三宅さんと相談中**＝当日は軽量（共鳴度+NPS+一言）で確定、会・各パートの感想はLINE経由の後日アンケで取る案を提示（インセンティブ再設計が論点：従来の「連絡が来るかも」は当日4点以上方式に移行済みのため、詳細軸レポート/次回優先案内等の別インセンティブ案）。

- **追記54（fe56ab1）＝後日アンケート（本番反映済み✅・SQL1本待ち）**：三宅さん方針＝**インセンティブなし・「ご協力のお願い」ベース**（詳細レポート特典案は実務フロー不明瞭のため見送り）。①**`/meet/after.html`新設**＝①プレゼン②テーブル交流③運営・進行（各5点+任意一言）④会全体の感想⑤改善要望。**同じ端末なら当日の記録（sn_meet.id）と同じ行に紐づく**（別端末は任意氏名+新id meet_after_*）。配布＝LINE配信でURL送るだけ。②admin MEET運営に**「📮後日アンケート」集計カード**（回答数・パート別平均・コメント一覧[パート/全体/改善のラベル付き・氏名付き]）。③**企業レポートに「参加者の声（会全体・匿名抜粋）」**セクション自動追加（after.overall上位5件）。④検証＝ローカル（挨拶表示/氏名欄出し分け・バー5段toggle・取消・失敗パス）＋本番after.html 200。⚠️**要SQL（三宅さん）**：`ALTER TABLE meet_entries ADD COLUMN IF NOT EXISTS after jsonb;`（プローブでdropped:["after"]確認済み＝未実行だと回答が保存されない）。実行後にE2E再プローブすること。

- **追記55（558b909）＝admin左ナビにMEET専用セクション（本番反映済み✅）**：三宅さん依頼。散在していたMEET関連を**「🎪 MEET（7/30）」セクション**（メニューと登録・管理の間）に集約＝①🎛MEET運営（班分け・レポート）タブ ②📣MEET LP↗ ③🎫当日ページ（受付QR）↗ ④📮後日アンケート↗(/meet/after.html)。旧「メニュー」のMEET運営と「公開ページ」のMEET LP/当日ページは移動（重複なし・switchTab('meetops')健在を検証）。

- **追記56（1785c92）＝企業レポートのデザイン刷新＋直接連絡モデル化（本番反映済み✅）**：①**デザイン強化**＝濃紺ヘッダーバンドに**白ロゴ（logo-02.png）をdata URI埋め込み**（別窓document.writeは相対パス不可＝PDF保存後も確実表示のためfetch→FileReaderでbase64化。失敗時はテキストロゴfallback）、tealグラデのアクセント線、h2に縦バー装飾、濃紺フッターバンド（ロゴ再掲）、`print-color-adjust:exact`で印刷時も色維持。②**運用モデル変更**＝三宅さん方針「ソコナラは強く介在しない」。「個別のご案内対象（ソコナラが連絡）」→**「直接ご連絡いただく参加者リスト（共鳴度4点以上）」＝貴社から直接連絡**に変更。この後の流れ文言＝「本レポートの参加者リストを基に、貴社から直接ご連絡を。極力、経営層ご本人からのコミュニケーションとして。ソコナラは強く介在いたしません」。氏名OFF時は警告表示（直接連絡には氏名必須）。フッターの個人情報注記も「貴社が直接連絡するためにソコナラが共有」に変更。③openMeetReportをasync化。④iframe描画でcomputed style検証（濃紺rgb22,34,63・白ロゴIMG naturalWidth11387ロード・teal accent・6軸バー・4点以上のみ2名・濃紺フッター）＋本番プローブ。⚠️スクショはブラウザペインで30sタイムアウト頻発＝computed style/DOM検証で代替する運用。

- **追記57（d233c0b）＝レポート文言削除・後日アンケの経営層メッセージ・CV明細（本番反映済み✅）**：①企業レポートの「ソコナラは強く介在いたしません（〜三宅まで）」の一文を三宅さん指示で削除。②**後日アンケート(after.html)に「⑥気になった経営層へ伝えたいこと」＝経営層ごとのメッセージ欄**追加（meet_speakers読込→登壇者順にtextarea・任意）。`after.msgs={coId:msg}`で保存→**企業レポートに「参加者から貴社へのメッセージ」セクション（実名）**で反映。⚠️実装中に**after.htmlのplaceholder文字列でクォート閉じ`'`を`"`と誤記→スクリプト全体が構文エラーで不動**の事故（submitAfter等全滅）。ローカルでtypeof確認して即修正。教訓：JS文字列内の全角文の後のクォートは要注意、編集後はtypeofで関数存在を必ず検証。③**共鳴データのファネルに「フォーム到達（CV）の明細」表**追加＝各form_openを日時/対象経営層/流入variant/utm/端末ID先頭で列挙し、テストか本人か見分け可能に。?test=1タグ済みの除外テスト数も併記（`_resInternalForms`）。※`is_internal`除外は既存（line592）だがタグ付け忘れテストは明細の日時・端末IDで判別する運用。⚠️副次観測：DBのmeet_speakers.ids順が発表順になっていない（貝原が先頭）＝day.html/after.html両方に影響。admin🎤の↑↓で要整順（三宅さんに委ねる・データ問題であり不具合ではない）。

- **追記58（d021aac/700406e）＝当日ページ文言・フォント＋全記事チェック（本番反映済み✅）**：①発表順のDB反映確認＝meet_speakersが貝原→日野→中嶋→古賀→河野→藤井に更新済み。**当日アンケート①も後日アンケのメッセージ欄もこの順に自動追従**（実装済み設計・検証済み。追加実装不要と三宅さんに回答）。②当日ページ`.note`を11→12.5px・色text3→text2に拡大。③「あなたの“仕事の軸”」の直下に**サブタイトル「年収や知名度だけが、キャリアの正解じゃない。」**（day thanks＋アプリ軸モーダル両方）。④「後日ソコナラからご連絡します」系を全廃→**「4点以上を付けた企業から後日直接ご連絡が届くことがあります（企業側の状況により、ない場合もあります）」**（アンケート①注記＋サンクス）。⑤**全15記事チェック完了→`ARTICLE_REVIEW_2026-07-13.md`**（repo直下＋1_Claudecode\sokonaraへコピー済み）。🔴主要指摘＝柳川合同（**ブリヂストン実名**・平均年収公開・M&A相手特定リスク）／Schoo井上（**「前福岡市長の高島さん」→現職のはず＝事実誤認疑い**・世界初検索広告の時系列）／石蔵（「難病」の主語曖昧＝センシティブ）／藤井（創業96年→2026年は97年）／日野出（**写真重複**qh3gtk5dkok.jpg×2・年商300億/1000億の文脈）／松永（「Valueの元」誤字・ベタ生レスポンス表記）／M・ZEC（**タイトル空欄**・「数百万から数億円の家」）／相対時間表現（先月/昨年/今年で96年）の絶対年号化。経営層への確認依頼フォーマット3点も文書末尾に記載。**修正指示が来たら記事JSONを一括修正する**（次チャットのタスク候補）。

- **追記59（6184297）＝後日アンケの配布=メール化・記入例削除＋データ切り分け確定（本番反映済み✅）**：①後日アンケの配布はLINE選別が難しいため**メール送付**に（admin📮カード文言を「参加者へメールでURLを送るだけ」に変更）。②after.htmlの経営層メッセージ欄の記入例「○○さんへ、あれば」を削除→placeholder「（任意）」のみ。③**企業レポートのデータ切り分けを三宅さんと合意・確定**：《個人特定で企業に渡る》①共鳴度・共鳴ポイント・④一言コメント(3点以上)・後日アンケの経営層メッセージ／《匿名・全体傾向で企業に渡る》**②気になった理由の上位タグ＝残す方針で確定**（三宅さんOK）／《企業に渡らない=個人の軸専用・端末内のみ》②理由から導くwb軸。④経営層メッセージ(after.msgs)→企業レポートへの抽出は**完全自動**（手集約不要）と三宅さんに確認済み。

- **追記60＝「読むほど軸が見える」の実データ検証（コード変更なし・分析のみ）**：axis.jsのcumulative計算式をPythonに忠実移植し、**全15社の実wb値**で4ペルソナの旅をシミュレート（scratchpad/axis_sim.py）。結果：✅**売りは成立**＝明確な好みを持つ人は記事1本(明瞭度+11.8)→3本(+31.2)→MEETで軸が段階的に浮き上がり、意味地域派と時間経済派で形が明確に分かれる。⚠️**弱点3つ（実測）**：①全社のwbが価値4軸70-90と高いため、**チェック企業を増やすほど裁量・人が全員上がる＝形が鈍る**（P1の明瞭度が後日+23.0に低下、P2は一時top2逆転）②**無言ユーザー（チェックのみ）にも「地域・裁量」の似た形が出る**＝wb重ね合わせだけの企業側平均形③全部タップすると全軸100で明瞭度0（極端ケース）。**改善案（未実装・三宅さん判断待ち）**＝wb寄与を「全社平均との差分」ベースに相対化（全社共通の持ち上げを除去し、会社選びの偏りだけが軸に乗る）。②**AI分析と軸データの関係（コード確認済み）**＝genFeedback→/api/feedbackの入力は**その回の診断のみ**（values型:q1/q2/about/q3/free＋経営層プロフィール／cardfeedback型:picked/dropped等）。**累積軸(sn_axis)はAI分析に未使用**。有料化検討時は累積軸をプロンプトに足す実装が小規模で可能。③需要シグナル＝admin共鳴データの補助指標「AI診断(レポート)を見た」(ai_viewed)が既に継続計測中＝AI分析率として使える。

- **追記61（5c68855）＝軸計算式v2「形と大きさの分離」（本番反映済み✅）**：三宅さん懸念「差分のみ加算だとレーダーが極端に低くなって変」への解＝**形（どの軸が高いか）と大きさ（レーダーの膨らみ）を分離**。①**形**＝チェック企業のwbは「全社平均との差分×0.5」だけ効かせる（`_avg`＝渡されたwbById内の平均。全社共通の高さは効かない）＋本人の明示反応（共鳴・理由タグ・診断＝従来通り主役）。②**大きさ**＝関わった量（チェック/タップ/回答の回数events）×1.5で全体底上げ・最大+15（`_lift`）＝**「使うほど育つレーダー」が視覚として明示**。③centroid/cumulative両方を`_core`+`_lift`構造に再編（外部API不変）。④**実wb15社でビフォーアフター検証**：P1意味地域派＝旧式は後日読み増しで明瞭度+32.8→23.0に低下していたのが、**v2は+10.5→+25.0→+32.5→+34.5→+36.8と単調増加**（＝売りが最後まで成立）。P2時間経済派＝旧式でチェック時にtop2逆転していたのが崩れなくなり最終+36.8。P3無言（チェックのみ）＝旧式+18.2の偽の確信→**v2は+9.5〜12.5の控えめな形（60前後の大きさは維持＝貧相にならない）**。P4全タップ飽和は残存（極端ケース・許容）。実端末＋モーダル描画・レコメンド動作もローカル確認、本番プローブ済み。

- **追記62＝進行用スライドv2作成（G:共有ドライブ・コード変更なし）**：三宅さん「当日の流れが変わったので進行用pptx更新できる？」→`260730_進行用.pptx`（38枚・73MB・4/9開催版の流用）をXML直接編集で更新し、**`260730_進行用_v2.pptx`として別名保存**（原本は未変更）。確定事項のみ反映＝①slide38の日付「2026.4.9→2026.7.30」会場「@G's BASE→@ethnico FUKUOKA」②slide36「送付メールより4/10(金)中に振り返りフォーム回答」→「アンケートはお手元のスマホ（当日ページ）からその場でご回答」③振り返り（明日まで）→（その場で）(slide16/36)④**関心度→共鳴度を全編置換**（6箇所・ノート含む）。validate.py PASS・python-pptxで開封確認・置換もれ0を検証。⚠️**三宅さん判断待ち**：(a)スケジュール表が4枚あり内容が新旧混在（slide1/5/17/19=4月版4社×8分+アイスブレイク、slide9=6社×3巡の新版）＝正の時間割で統一要 (b)仮想転職ゲーム（紙ワークシート）の存続/アプリ移行（slide20-29）(c)QRコード等の画像差し替え（当日ページQRへの更新は画像なので手作業）(d)後日メールアンケの告知を足すか。pptx編集はスクリプト化済み＝指示があれば追加反映可能。

- **追記63＝進行用スライドv3（G:共有ドライブ納品済み・コード変更なし）**：三宅さん指示「1(時間割統一)・2(当日ページ一本化＝スクショで説明資料)・4(後日メール告知)お願い」。`260730_進行用_v3.pptx`として納品（v2は統合し削除・**原本未変更**）。①**時間割4枚(slide1/5/17/19)をslide9の正に統一**＝19:00オープニング/19:10-20:10プレゼン8分×6社/20:10-20:55グループセッション(3班)/20:55-21:20交流会/21:20-21:30クロージング。アイスブレイク行削除（slide1は写真撮影・遅刻者受付をオープニング行に引継）、仮想転職ゲーム文言削除、slide1のワークシート回収・PDF化タスク削除。②**「当日ページの使い方」5枚を新規作成しslide19直後に挿入**＝ヘッドレスChrome(CDP)で実画面5状態を撮影（①受付登録+傍聴チェック②カード=☆気になるON+共鳴チップON③班スケジュール+更新ボタン④共鳴度アンケート(5点紺/4点紺/3点teal)⑤送信後=軸レーダー+LINE）→python-pptxで白紙レイアウトに組込（tealチップ番号+紺タイトル+箇条書き+スマホ枠画像。デッキ既存フッター自動継承）。**旧ゲームスライド7枚(20-25,29)削除**、28は「グループセッション 経営層と直接対話！」に改題して存置。③slide36に「後日、メールで感想アンケートをお送りします」追記。全38→36枚。validate PASS＋**PowerPoint COMで新5枚をPNG出力し目視QA済み**。⚠️残＝③QRコード画像の差し替え（三宅さん手作業と合意）。撮影スクリプト(shots.py)・組立(build_v3.py)はscratchpadに＝再生成可能。CDPスクショ手法＝chrome --headless=new --remote-debugging-port --remote-allow-origins=* をwebsocket-clientで駆動（拡張やペインのスクショ保存が使えない時の代替として有効）。

- **追記64（079b162/de319ec/2f66fe7）＝MEET LP・A/B改善提案レビューと実施**：外部提案書（Downloads/ソコナラ様LP改善案.pdf・PyMuPDFで読解）＝AパターンCPA6,079円 vs BパターンCPA23,470円(3.9倍)。切り分け「申込ボタンクリック後の完了率A91.3%/B30.8%に差が集中」は**数字内部整合を確認済み**（申込ボタンクリック率×完了率=参加申込率が成立）＝妥当と評価。**三宅さんと合意した実施**：①**登壇者6h**「老舗企業の若手経営層」→「老舗企業の30代若手社長」②**proofLine/trustから「30代前後がメイン」削除**（20代の自己排除を防ぎ間口拡大。trust[1]→「はじめての方も歓迎」・proofLine→「申込のべ400名超」のみ）③**FV H1をベネフィット主導化**「会社を選ぶ前に、経営層に会う。」→「求人票では分からない、会社のリアルが分かる。」＋リード重複整理④**CTA文言**「今すぐ参加申込」→「参加申込フォームへ（無料）」（この先フォームありと予告し完了率のハードルを下げる＝提案の「CTA弱める」の的を射た解釈）。**見送り合意**：氏名任意化（イベントは氏名必須）／残席わずか煽り弱め（クリック減るだけ）／FV写真変更（写真はクリック=入口に効くが問題は完了率＝下流のため触らない）。⚠️**要SQL（三宅さん・未実行）**：①②③はLP本番=DB(meet_lp)正本のため下記SQL実行が必要（CTA④はハードコード＝反映済み）。SQL＝jsonb_setで speakers,5,h / proofLine / trust,1 / heroH1 / heroLead を更新（本文に記載済み）。実行後に本番プローブで検証すること。※最速CPA改善は当面Aに予算戻す案も提示済み。

### 追記65（2026-07-15・コード変更なし＝検証のみ）＝追記64のLP A/B改善SQLは実行済みと確定＋AI基盤の確認
- **追記64の⚠️「要SQL未実行」は解消✅**：三宅さん「やった気がする」→本番 `site_settings.meet_lp` を anon read で直接検証（scratchpad/meet_lp.json）。5項目すべて新コピーで反映済みを確認＝①heroH1「求人票では分からない、会社のリアルが分かる。」②speakers[5].h「…30代若手社長」③proofLine「申込のべ400名超」④trust[1]「はじめての方も歓迎」⑤旧「30代前後がメイン」消滅。→ **A/B改善は本番稼働中**。CTA④はもともとハードコードで反映済み。
- **AIフィードバックの基盤＝OpenAI**（`api/feedback.js:315`）：エンドポイント`https://api.openai.com/v1/chat/completions`・認証`process.env.OPENAI_API_KEY`（Vercel環境変数）・モデル`gpt-4o-mini`。Anthropic/Claudeは未使用。将来Claude化するならこの1ファイルのfetch先/認証/モデルとSSEパースの差し替えで対応可。

### 追記66（2026-07-15・0543549）＝⚠️重大な計測バグ根治：直リンク流入のCTA/CVが1件も記録されていなかった（本番反映済み✅）
- **症状**（三宅さん指摘）：admin「興味通知フォーム」に13件入力があるのに、共鳴データの新導線ファネルは「興味（共感→P5）0件・フォームを開いた(CV) 0件」。
- **⚠️私の初期診断は誤り**（`interest_forms`にis_internalが無い非対称、と説明した）→ 三宅さんの「マーケ会社のクリックでは？」が正解のヒントで、コードを追って**実在バグ**と判明。
- **真因**：`logDirectArticle()`（記事直リンク`?cid`＝**広告/LINE/メール配信の流入**）が card_logs 行を作るのに、**`SN_CARDS.logId` をセットしていなかった**（`moya`=2177 と `list`=2514 は両方セット済み。directだけ抜け）。`patchCardLog`は先頭で `if(!(SN_CARDS && SN_CARDS.logId)) return;` ＝**logIdが無いとサイレントに何もしない**。→ 直リンク流入者は `contact_cta`／`form_open`／`wb_view`等が**永久に記録されない**。一方 `interest_forms.insert` はlogId不要なので成功 ＝「フォームには入力があるのにファネルは0」の完全な説明。
- **⚠️過去データの解釈に影響**：追記32の「記事141→興味CTA13＝9%が本当の崖」は、**直リンク（広告）流入のCTAが全て欠落した数字**。崖は実際より深く見えていた可能性が高い。C/E施策の効果測定はクリーン計測以降の数字で見ること。
- **修正（moya/listと同型に統一）**：①`logDirectArticle`で `SN_CARDS={logId:id,cid,variant:'direct'}` を設定＋`sn_card_ctx`へ永続化②`openCompanyArticle`の回遊時cid追従を `list||direct` に拡張③**入口(4085)で「URLが古い文脈に勝つ」張り替え**＝別の配信リンクで違う経営層に来たら `cid`更新＋`recommended_company`をpatch（追記16の教訓＝復元系はURLパラメータより弱くすること）。`person/moya`は推薦そのものなので**触らない**（pfのselには`variant`キーが無く分岐が発火しないことを確認済み）。
- **副次効果**：直リンクの**リロードごとに新規行が増える水増しも解消**（従来は`_directLogged`がページロード単位のため再読込で別行＝started膨張）。セッション中1行使い回し（追記34の設計意図）にdirectも合流。
- **検証（ローカル8124＋本番プローブ）**：①直リンク→`SN_CARDS.logId=art_...`/variant=direct 生成②`patchCardLog({contact_cta:true})`→ `/api/card-log` へ **id=art_... / set={contact_cta:true}` の送信を捕捉**＋form_open用logIdも非null③汚れた文脈(貝原)残存下で別配信リンク(日野)→**URLが勝ちcid張り替え・logId再利用・正しい記事表示**④汚れた文脈下で**カード経由(from=cards&sel)→pfのlogIdで上書き・日野を正表示・directログ未発火**（追記16型リグレッションなし）⑤コンソールエラー0・本番3マーカー実証。
- ⚠️**残（任意）**：`interest_forms`には`is_internal`が無く、テスト端末の行も一覧に並ぶ（ファネルは除外済み）。付けるなら`ALTER TABLE interest_forms ADD COLUMN is_internal boolean;`が**先**（列が無い状態でinsertにキーを足すとPGRST204で行ごと失敗＝興味ログが消える。card-log APIと違い自動ドロップ機構が無い）。配布前にテスト行を「選択削除」で消すだけでも足りる。

### 追記67（2026-07-17）＝Q1軸の実態調査＋移管記事QAの本文重複を刷新（⚠️SQL実行待ち）
- **Q1軸ラベルの「あったりなかったり」（三宅さん指摘）＝仕様**：「人：」等のプレフィクスは**admin編集画面だけの表記**（q1axに軸が明示保存された行のみ表示）。読者向けには出ない（全15社のDB `q1` にプレフィクス残存0を実測）。ただし実態は**4つ全部軸明示済みは藤井・中嶋の2人だけ**、石蔵は4つ全部未設定・井上はq1ax無し＝キーワード推定頼み。
- **「4つ全部同じ軸」問題**：実質単一軸は松永/河野（意味×3）とイトキュー/中原（地域×3）。どれを選んでも同じ軸に+9＝Q1の診断信号が死ぬ。**未指定行に別の軸を明示＋文言微調整で最低2軸確保**が現実的な着地（記事の実態に反して無理に散らさない）。
- **q1ax補完SQL＝✅実行済み・検証完了（2026-07-17）**：`q1ax_fill_2026-07-17.sql`（repo直下＋1_Claudecode\sokonara）。13社（藤井・中嶋は全明示済みで対象外）の軸を内容ベースで明示。**本番プローブで全15社が2軸以上に分散・未設定ゼロを確認**（松永=意味/人/意味/裁量、イトキュー=地域/裁量/地域/人）。以後Q1はどの選択肢を選んでも同じ軸に+9になる状態を脱し、全社で診断信号が機能する。既存の明示軸は尊重し、**変更は松永slot2（意味→人「家族のヒーロー」）とイトキューslot2（地域→裁量「覚悟を決めて家業を継いだ」）の2箇所のみ**（SQLコメントに全文言＋軸＋[変更]表記＝三宅さんがファイル内で検品可能）。文言(q1)は一切変更なし＝Notion正本とのズレなし。実行後は全社が2〜3軸に分散。
- **本文とQAの重複（三宅さん指摘）＝実測で裏付け**：8字n-gram一致率で荒巻35%・中村33%・村岡30%・中原28%（**全て移管8記事**・既存記事は1〜15%）。原因＝STUDIO元記事のINTERVIEWから本文もQAも作ったため同じ話の言い換えに。
- **QA刷新を実施**：移管前の元記事（articles_raw_2026-07-06.md）の**未使用素材**（一問一答Q&A・ライフラインチャート・求人ポジション詳細）から再構成。重複率35/33/30/28%→**0/5/1/2%**。荒巻6→5問（価値観・夢中の瞬間・学生時代イップス/留学・営業本部長/COO/CHRO・生涯）／村岡5→5問（30年の開発具体・4語・Be There Be Now・生涯・転職者へ）／中原5→4問（先代の言葉「とにかく人の役に立て」・海・価値観・展望[既存維持]）／中村6→2問（元記事に一問一答が無く素材薄→営業部長の仕事・求める人材像に縮小）。**✅SQL実行済み・本番プローブ検証完了（2026-07-17）**。ドラフト全文＝`1_Claudecode\sokonara\QA_DRAFTS_2026-07-17.md`。
- **`qa_rewrite_2026-07-17.sql` は最終的に「QAのitemsだけを差し替えるjsonb方式」に変更**（`jsonb_agg(CASE WHEN e->>'type'='qa' THEN jsonb_set(e,'{items}',...) ELSE e END ORDER BY ord)` ＋ `jsonb_array_elements(body::jsonb) WITH ORDINALITY`）。理由＝記事本文13.8k字の全文置換SQLをチャットに打ち直すと**誤字で本文を壊すリスク**（Notion編集で実際に2回誤字を出した）。本文に触れないので同時編集にも安全。この型は今後のbody部分更新に再利用可。
- **検証結果**：①QA4社とも原稿と完全一致②記事本文＝セクション5本・並び順(section×4→qa→section)・写真6枚が全社で無傷③荒巻の文法修正反映（「将来的にへ」消滅）④中原＝理臣（ceo/title/body・正臣0件）⑤ブリヂストン全15社で0件⑥全15社のbody JSONパース可⑦実機（ローカル8124＋本番DB）で中原記事の新QA表示・旧QA消滅・氏名・本文・写真9枚を確認。
- ⚠️**教訓**：検証スクリプトの`json.dumps`がensure_ascii既定で日本語を\uXXXXにエスケープし、置換が効かず**偽の差分NG**を出した。日本語の突合は必ず`ensure_ascii=False`。
- ⚠️**ブリヂストン実名は三宅さんが削除済み**（DB実測でゼロ確認）。SQLはその最新bodyをベースに生成済み＝上書き事故なし。ただし**このSQL実行前にadminで該当4記事を編集保存すると差分が消える**ので、実行を先に。
- **✅Notion側も同期済み（2026-07-17・三宅さん依頼）**：完成記事フォルダ（notion: c1368fea…）の4ページのQAセクションを新QAへ差し替え（notion-update-page・再取得で反映検証済み）。**荒巻ページは本文のブリヂストン実名と年収数値（400万円強→500〜600万円）も削除**（DB側の三宅さん修正と同期・再解析しても復活しない）。年収数値削除で壊れていた文法（「将来的にへ」）はDB向け`qa_rewrite_2026-07-17.sql`に修正を織り込み済み。⚠️荒巻ページ末尾「元情報」（内部メモ）には年収数値が残置（出典記録として意図的・本文には出ない）。
- **事実性の担保**：新QAは全て元記事（articles_raw_2026-07-06.md）の一問一答・チャート・INTERVIEW由来で創作なし（中原④末尾の「自分自身の活動を共に」文のみ現行公開QAからの引き継ぎ＝Notion旧版には無かったが公開済み文言のため4ページ側にも反映）。三宅さん監査OK済み。
- **中原さんの氏名＝「理臣」で統一（三宅さん決定・2026-07-17）**：Notionは元から理臣＝変更不要。DB側（ceo・title・bodyリード文の3箇所が正臣）の修正を **`qa_rewrite_2026-07-17.sql` に織り込み済み**（bodyは全文置換に含め、ceo/titleは末尾のreplace UPDATE）。SQL実行で全箇所が理臣に揃う。ARTICLE_REVIEWの表記ゆれ課題も解消。
- **個人LPの注記削除（三宅さん指示・2026-07-17）**：ENCOUNTERSの「※ 経営層の氏名・社名は、アプリでピンときたあとに開きます（先入観なく、直感で選んでいただくため）。」を削除（`personal/index.html`・data-lp対象外＝コード削除で完結）。ティーザー設計自体は維持。
- **新規記事のQ1軸自動割り振り（三宅さんの質問への回答の記録)**：admin貼り付け→parseNotionで「軸：」明示ラベルを拾い、無い行は**文言からキーワード自動推定してプリフィル**→書き手が確認・上書き→保存でq1/q1axに分離。**Why/Who/What/Lifeは「選択肢の種類」の執筆ガイドとして存続**（Notionフォーマットで各1個）だが、**軸(意味/裁量/人/地域)への割り当てとしては廃止済み**（追記30・位置決め打ちが誤配線を起こしたため内容ベースに）。

### 追記68（2026-07-17・17efbd6/5fbd6b3）＝/sim/ 移住シミュレーターβ 新設（本番反映済み✅・本体への影響ゼロ）
- **背景**：三宅さんの構想＝「関電→福岡」ペルソナで、移住の意思決定を阻む複雑な要因（物価・給与・ローン・老後資金・教育・親の介護・プライド）を統合シミュレーションする機能。**将来は本体から切り離して単体で稼げるプロダクトに育てる可能性あり**。Phase Aの叩き台を盛り盛りで実装→三宅さんが実機で触って機能追加を判断するフェーズ。
- **設計原則**：①本体と完全分離＝新フォルダ `sim/index.html` 1ファイル自己完結（本体のファイルは一切変更なし・リンクも未設置＝URL直打ちのみで到達：`app.sokonara.co.jp/sim/`）②**全データ端末内localStorage（`sn_sim_v1`）完結・サーバー/DB/API不使用**（年収・ローン等のPIIを預からない）③外部ライブラリなし（チャートは手書きSVG）④占い要素は不採用（判断材料の透明性を守る。入れるならシェア面の遊び限定＝三宅さんと合意済み）。
- **実装機能**：入力5セクション（あなたと移住先／収入／支出と住まい／子どもと親／老後）＋ 判定バッジ（成立/要調整/厳しい＝資産が尽きる年齢）／**成立に必要な最低年収の逆算**（二分探索・目玉機能）／年収感度スライダー／月次家計before-after／90歳までの資産カーブSVG（現状維持vs移住の2本線）／**改善レバー**（配偶者就労・年収交渉・進路変更・家賃・持ち家の売却/賃貸切替等を自動試算し効果額順に提示・「これで成立」バッジ）／**移住先4都市比較**（福岡/北九州/久留米/その他九州を同条件で横並び判定）／ライフタイムライン（子の進学・親80歳の手助け・ローン完済・退職・年金）／上がるもの5軸チップ＋**不安の言語化→反転文**（プライド等6項目）／**意思決定メモ自動生成**（コピー可＝家族への説明資料）／デモ（42歳大阪→福岡・年収900→550＝「550では53歳で資産切れ→600なら成立」が見えるリアルな設定）／保存・JSONエクスポート/インポート・**共有リンク**（URLハッシュにデータ埋め込み＝サーバー送信なし）・印刷。
- **モデルの前提（全部コード内定数・目安）**：手取り率テーブル／都市別家賃相場・物価係数（CITY）／教育費＝公立中心・高から私立・私立中心の3プラン年額（EDU）／年金＝基礎81万+厚生（平均標準報酬×0.005481×加入年数・過去は現年収/将来は移住後年収の加重平均・上限762万）／親の手助け＝80〜84歳に月5万+遠方なら月2万／売却は諸費用4%引き・賃貸に出すと15%引き。物価上昇は非考慮（免責に明記）。
- **検証済み**：ローカル（デモ全系統・逆算600万・レバー・都市比較・共有リンクのラウンドトリップ・localStorage保存・モバイル375pxはみ出しなし・コンソールエラー0）＋本番プローブ2回。
- **次の判断（三宅さん）**：実機で触って機能追加の要望出し。候補＝老後の住み替え（家購入）・配偶者の転職シナリオ・移住タイミング比較（今vs3年後）・本体6軸レポートとの接続（Phase B）・AI意思決定メモ（Phase C）・単体プロダクト化するなら独自ドメイン/LP/課金設計。

### 追記69（2026-07-17・3ed8ded）＝記事確認メール準備＋adminログに経営層名表示＋Temp消失復旧＋現データ所見
- **⚠️Temp消失再発→再clone復旧**：`Temp\sokonara`のルートファイル全消失＋.git破損（ディレクトリのみ残存）。`rm -rf`→`git clone`→git config再設定で復旧（全push済みで損失なし）。この時点でHEAD=be21189（別チャットの/sim/作業4コミットが23d6098の上に乗っていた＝同時進行に注意）。
- **adminログに経営層名を表示**（三宅さん依頼「企業だけでなく誰の記事か分かるように」）：`admin.html`の①共鳴データ「フォーム到達（CV）の明細」表 ②「最近のセッション（個別削除）」の2箇所を、会社名だけ→**「経営層名 ／ 会社名」**に変更（既存の`coCeo()`を併用・未登録IDはフォールバック）。ローカルで出力検証（井上 真吾／株式会社Schoo）＋本番プローブ2マーカー確認。
- **記事確認依頼メール作成**（`1_Claudecode\sokonara\MAIL_記事確認依頼_2026-07-17.md`）：全15名にトップURL（app.sokonara.co.jp＝一覧に名前入りカード15名・ログイン不要）を送り本人確認してもらう共通文＋**個別5名の名指し確認**（本人が読んでも気づけない箇所）。⚠️BCC送付・レビュー完了後に計測リセット（DELETE card_logs）の順。
- **🔴指摘の本番DB実測（レビューARTICLE_REVIEW_2026-07-13の現状）**：大半は解消済み（ブリヂストン/年収数値/藤井96年/中嶋タイトル/貝原「先月」/松永誤字/日野写真重複/村岡「昨年」/井上「前福岡市長」の「前」）。**本人確認が要る残り5件**＝荒巻M&A2件（運送会社事業譲受・佐賀の会社買収＝相手特定リスク）／石蔵「難病を経験」の主語（本人か父か・掲載可否）／松永「ベタ生レスポンス」正式表記＋補足可否／井上「福岡市長の高島さん」実名可否／貝原の相対時間（今年50周年・昨年入社・先日中計発表→正確な年月）。返信待ち→SQL修正→検証→**Notion正本にも反映**（忘れると再解析で戻る）。
- **現データ所見（admin共鳴ファネル・スクショ 27件時点）**：①**追記66の直リンクCTA/CV記録バグ修正が効いている**＝CV明細に直接(art_)/list流入の2件が記録（修正前は0だった）。②**「仕事の軸レポートを見た」が0/24＝診断導線が完全に素通り**（記事は読むが誰も診断に進まない）。③記事24→興味3→CV2＝母数27でまだ判断不可だが、CV2件が両方Schoo・7/18・直近＝三宅さんのテストの可能性高（?test=1未タグ）。④H1（軸レポートがCV押上げるか）は閲覧0で検証不能。→**配布前にDELETE card_logsでクリーンにしてから本計測が前提**。診断素通りは配布後も0なら導線設計の見直し要（軸レポートはLINE後特典に降格済みなので、記事から診断への動線が弱いのは設計通りでもある＝要観察）。

### 追記70（2026-07-21・5e9af97）＝「MEETで気になる」バッジ廃止＋軸の加算を共感ベースに（本番反映済み✅）
- **背景（三宅さん実機FB）**：①アプリ一覧の「MEETで気になる ✓」バッジが意味不明（正体＝端末のsn_meet.checksの引き継ぎ表示・追記47b。三宅さんの端末に登壇者制限前のテストチェックが残っていた）②**「選んだだけで軸の数値が加算されるのはロジックとしておかしい。共感を押したら加算されるなら分かる」**——設計指摘として正しい（旧計算は☆チェックだけで企業wb重ね合わせ＋レーダー膨張が効いていた）。
- **①バッジ廃止**：`index.html`からmeet-badge（CSS・描画・チェック済み先頭ソート）を全撤去。**MEETの☆チェック自体は班分け・企業レポート用に存続**（アプリの見た目に影響しなくなっただけ）。
- **②軸計算を共感ベースに**（`meet/axis.js`）：wb重ね合わせ＆イベント数の対象を「共感の実績がある経営層」に限定＝(a)共鳴チップを押した相手 (b)アンケート共鳴度3+（deep/talk） (c)記事を読んで診断送信した相手（`diagCos`＝旧v.cos）。**☆チェックは軸に一切効かない**。`hasData()`（バナー表示判定）もチェックだけではfalse。`_core`のシグネチャ変更（checks→diagCos）だがday.htmlは呼び出し不変で自動追従（thanksレーダー＝共鳴チップ＋理由タグ＋deep/talkのみに）。
- **③文言**：バナー副文「記事やMEETでの選択が貯まるほど〜」→**「記事やMEETで共感ボタンを押すほど、あなたに合う相手や価値観が見えてきます」**（三宅さん文言）。軸モーダル内「記事での選択・MEETでの共鳴」→「記事やMEETでの共感」。
- **検証**：ローカルE2E＝チェックだけの端末（バッジ0・バナー非表示・hasData false・軸全て基準値52以下・ソートなし）／共鳴チップ端末（バナー表示・local 68に上昇）／診断経由（appendHits→meaning64/agency61・dedupe効く）／コンソールエラー0。本番プローブ＝axis.js diagCos×4・meet-badge 0・新文言。
- ⚠️三宅さんの端末に残ったテストチェックは、バッジ廃止により**表示上の混乱は解消**（データ自体は`localStorage.removeItem('sn_meet')`でいつでも消せる・放置しても軸に影響しなくなった）。

### 追記71（2026-07-21・a8dd101）＝ファネル可視化の改修（本番反映済み✅）＝三宅さん「数字に整合性がなさそう」への回答と対策
- **数字自体は整合していた**（全表の合計を検算一致）。「合わない」ように見えた3つの正体＝①入口別の最終列はCVではなく**AI診断**（CV列が無かった）②「直リンク16」と「LINE16」は偶然同数の別集合（LINE配信=日野さん記事は記事読了94%・行動0。興味/CVはutm無し直接アクセスのSchoo×2＝**三宅さんでもマーケ会社でもない実ユーザーと確認**）③**広告→TOP流入がファネルに不可視**（card_logs行は記事開封時にしか生まれない＋広告リンクは `utm_source=personal_lp` でTOPへ）。
- **改修4点**：
  - **①TOP到達の計測**（index）：TOP着地時に `variant='visit'`・`started:false` の行を記録（sessionStorageで1セッション1回・リロード膨張なし）。started:false＝既存の全集計から自動除外＝カード開始数は汚れない。SQL不要（全列ALLOWED済み）。
  - **②utm帰属の穴を修正**（index）：`_snUtm()` 新設＝**list行・moya行にもURLのutmを付与**（SPAなのでTOP→記事遷移でもutmは生きている）。従来は広告→TOP→記事開封が「直接/不明」に化けていた。list行にstarted_atも追加（期間フィルタ対応）。
  - **③admin表の拡充**：チャネル別＝**「TOP到達」列**（visit行・到達→開始率つき。到達<開始の異常比率は%非表示）＋**CV(フォーム)列**追加（AI診断列は補助指標カードへ委譲）。入口別（版別A/B）＝**CV列追加**（teal太字をCVへ移動・AI診断は残置）。visit行への期間フィルタ適用・is_internal除外も対応。
  - **④カード文言A/Bを異アーム対決のみに**：同アーム対決（What vs What等）は構造上50%固定のため除外。従来の「全部ちょうど50%」は情報ゼロだった。
- **検証**：ローカルE2E＝visit発火/ペイロード(utm4種)/1セッション1回ガード/list行utm付与を実測＋adminはモックログでrenderResonance実走（ファネルN=visit除外・チャネル別3行の到達/開始率/CV・入口別CV列・A/B異アームのみ・内部除外注記）＋本番プローブ6マーカー。
- **⚠️運用ノート**：到達列は2026-07-21以降のデータのみ。pf(person)開始と記事直リンクはTOPを通らないため到達は「—」。広告の効果測定は「personal_lp行：到達→開始率（直帰の逆数）→CV」で読める。GA4(GTM)はページ全体の直帰・滞在の補完として併用。

### 追記72（2026-07-21・e22fa7c）＝スクロール到達率の計測＋「いいな」への文言統一（本番反映済み✅・⚠️SQL1本待ち）
- **背景**：LINE配信16件が「着地即article_read=true」で読了と区別できない件（前追記）→三宅さん「スクロール到達率も拾いたい」＋「共感/いいなが混在→『いいな』に統一。『いいな』を届けるほど軸が明確化する仕掛けなので文言も変える。2つのボタンは役割で差別化」。
- **①read_pct（スクロール到達率）**：`updateRead`内で記事画面(S.screen==='p1')のみ、25/50/75/100の閾値を初めて越えた時に`patchCardLog({read_pct:閾値})`（1スクロールで複数閾値を跨いだら最上位1件に集約・値は行の最大＝セッション内max）。TOP画面のスクロールでは発火しない（E2E実証）。card-log ALLOWEDに`read_pct`追記済み。**⚠️要SQL（三宅さん）**：`ALTER TABLE card_logs ADD COLUMN IF NOT EXISTS read_pct int;`（未実行でもAPIの自動ドロップで壊れない＝本番プローブで`dropped:["read_pct"]`を実証済み。実行するまで読了データが貯まらないだけ）。
- **②admin表示**：メインファネルに**「記事を半分以上読んだ（50%+）」行**（記事を読んだ→軸レポートの間）、チャネル別に**「読了50%+」列**（記事(開いた)の隣）。これで「タップ止まり vs 実読」がチャネル別に分離（LINE配信の実読率が次回配信から見える）。
- **③「いいな」統一＋ボタン差別化**：記事内ボタン=**「いいな」を届けて“仕事の軸”を診断**（押下後「「いいな」を届けました！」・p2診断ルート）／スティッキー=**「いいな」を届ける →**（P5フォーム直行）＝2つの役割が文言で分かれた。注記「「いいな」は匿名で経営層に届きます。届けるほど、あなたの“仕事の軸”が明確になります。」。p-wbのCTA「共感を伝える」→「「いいな」を届ける」。軸バナー「記事やMEETで「いいな」を届けるほど、あなたに合う相手や価値観が見えてきます」。軸モーダル「届けた「いいな」（これまでN件）」。ファネル行名「興味（いいな→P5）」。⚠️**罠**：記事切替リセット(renderP1FromCompany)がラベルを旧文言で上書きしていた＝初回検証で発覚し修正（動的上書き箇所も要grepの教訓）。MEETの「共鳴ポイント」は別概念のため据え置き。
- **検証**：ローカルE2E（30%スクロール→25送信／最下部ジャンプ→100のみ／再スクロール再送なし／TOP画面0件／ボタン押下→ラベル変化→p2遷移／スティッキー文言）＋本番プローブ5マーカー＋API書込テスト。
- ⚠️read_pctはセッション内max（記事を跨いでリセットしない設計＝「この端末は実読したか」の指標。記事別の読了が欲しくなったら_readSentを記事切替でリセット＋行分離が必要）。

### 追記73（2026-07-21・a6f5fa8）＝2つの「いいな」ボタンを同一ファネルに統合（A案・本番反映済み✅・⚠️SQL待ち）
- **三宅さん質問「adminでこの二つは分けられてた？」→答え：バラバラだった**。記事内ボタン（旧・共感）は`empathy_logs`別テーブル＋companies.reactionsのみでメインファネル外／スティッキー「いいな」を届けるだけが`contact_cta`＝ファネルの「興味」。統一文言後は同じ「いいな」に見えるのに計測が別、という不整合。
- **A案採用**：記事内ボタン押下時に`patchCardLog({soft_iina:true})`を追加（card-log ALLOWED追記済み）。**メインファネルが8段に**＝カード開始→選択→記事(開いた)→半分以上読んだ(50%+)→**いいな・軽（記事内ボタン→診断）**→軸レポート→**いいな・フォームへ（P5到達）**→CV。「軽いYESがどれだけ出て、何割がフォームまで行くか」が1本で読める。empathy_logs（経営層別の共感ログ一覧・削除機能）は併存＝役割違いでそのまま。
- **検証**：ローカル（押下→art_行へsoft_iina:true・p2遷移・二度押しガード／adminモックで8段ファネル描画）＋本番プローブ（index送信コード・API `dropped:["soft_iina"]`＝列待ちの正常挙動）。

### 追記74（2026-07-21・30c0f64）＝MEET参加者の顔写真・事前登録（本番反映済み✅・⚠️SQL待ち）
- **背景**：三宅さん「参加者の顔写真を事前登録してもらいたい。当日まで参加企業は見せたくない。Google Driveを考えたがもっと良い方法は？」→Drive案の弱点（Formのアップロードは回答者Googleログイン必須・PIIが自社統制外・氏名突合が手作業）を指摘し、**専用ページ案で合意。用途③＝運営用＋企業レポート共有の両方**。
- **`/meet/photo.html` 新設**（参加者向け・noindex）：氏名＋写真1枚＋同意2種（**必須**=当日運営への使用／**任意**=共鳴度4点以上を付けた企業への紹介に写真を含める）。**登壇企業の情報はゼロ**＝当日まで企業を伏せる要件を構造的に満たす。端末内でcanvas圧縮（長辺1200・JPEG q0.85＝MPO対策込み・実測1600x1200→1200x900）。再送はlocalStorageのid再利用＝同じ行・同じパスを上書き（重複しない）。配布＝申込完了メールにURLを記載。
- **`api/meet-photo.js` 新設**（edge・meet-logと同PII方針）：`upload`=公開（サイズ上限・JPEG dataURLのみ・id接頭meet_ph_強制）→Storage**非公開バケットmeet-photos**＋meet_entriesに事前行upsert／`list`・`delete`=ADMIN_PASSWORD認証（署名URL1時間発行）。当日の受付行(meet_*)とは別行＝**氏名で突合**する設計。
- **admin**：MEET運営タブに「📷 事前写真」カード（配布URLコピー・読込→サムネグリッド・共有同意バッジ・削除）。**企業レポート**：共鳴度4点以上リストに顔写真を表示（**共有同意✓＋氏名表示ONの時のみ**・dataURI埋め込み＝PDF保存後も表示）。
- **⚠️要SQL（三宅さん・3行）**：`ALTER TABLE meet_entries ADD COLUMN IF NOT EXISTS photo_path text; ALTER TABLE meet_entries ADD COLUMN IF NOT EXISTS photo_consent boolean; insert into storage.buckets (id, name, public) values ('meet-photos','meet-photos', false) on conflict (id) do nothing;`（バケットは非公開・ポリシー無し＝anon全遮断/service_roleのみ）。実行後に私がアップロードE2Eプローブ。
- **検証**：ローカルE2E（バリデーション順・canvas圧縮実測・APIペイロード・完了画面・再送ID保持／adminモックでグリッド描画・件数/同意集計）＋本番プローブ（photo.html 200・APIが`needBucket:true`を正しく返す＝SQL待ちの期待挙動）。
- ⚠️運用ノート：写真の削除期限（イベント後◯日）はページ文言で「必要な期間を過ぎたら削除」と約束済み＝**イベント後にadminの削除ボタンで一掃する運用**（配布前クリーンアップのチェックリストに追加）。E2Eプローブで作るmeet_ph_probe行も同様に削除。

### ⚠️次チャットへの引き継ぎ（残タスク）
- **✅完了（2026-07-21）**：顔写真3行SQL実行済み・E2Eプローブ成功（実JPEGアップロード＋同ID再送上書き）。⚠️プローブ行`meet_ph_probe_e2e`はadmin📷カードから削除可。⚠️追加修正（1ffbf38）＝**meet_ph_行がadmin参加者統計・名簿・班分けに混入するバグを発見・除外フィルタで根治**（写真行は📷カードと企業レポート突合だけが読む）。残タスク＝申込完了メールにphoto.html URLを記載（三宅さん）
- **✅完了（2026-07-21）**：card_logsの`read_pct`/`soft_iina`列＝三宅さんがSQL実行、本番プローブで両列とも書込成功（droppedなし）を確認。スクロール到達率と記事内「いいな」の計測が稼働開始。プローブ行（probe_readpct_*/probe_soft_*/probe_cols_*）は配布前のDELETE card_logsで一掃される。
- **記事確認メール送付→返信対応**（追記69）：三宅さんが送付→返信来たら私がSQL修正＋Notion反映＋検証。完了後にDELETE card_logsでクリーン計測開始。
- **/sim/ 移住シミュレーターβ**（追記68）：三宅さんの実機フィードバック待ち→機能追加・Phase B/C判断。本体とは完全分離（sim/index.html 1ファイル）。
- **MEET LP刷新の効果測定**（追記45/64）＝CV単価が数千円に近づくか実データで。A/B改善SQLは反映済み✅（追記65）。届かなければ構造/導線・差し色・コピーA/Bへ。
- **（完了・検証済み✅）記事記号クリーンSQL**（追記44）＝2026-07-09に実行、本番プローブで全社body アスタリスク0・全角引用符0・「」変換・JSON有効を確認。
- **貝原さんのカード写真差替**：admin記事管理→貝原→「📁 保存済みから選ぶ」で正しい写真に。バケットに無ければアップロード。
- **8社cc_* SQLの実行確認**（追記41・三宅さん実行→本番プローブ検証）。
- **（完了）個人LP文言のadmin編集機能**（追記40）＝admin「👤 個人LP文言」タブで編集可。三宅さんが実機で編集→保存→/personal/反映を試す段階。
- **WEBCASの氏名を任意化**（三宅さん・CV改善／追記37）＝アプリ「匿名OK」と整合。
- **SecurityScorecard DNS対策（お名前.comで作業・親会社不要）**：①SPFを`v=spf1 include:_spf.google.com include:webcas.net ~all`に置換（管理画面入力済みだがライブ未反映＝保存完了待ち。ドメインプロテクション1,364円は不要）②DKIMは✅設定済み（`mail2025`セレクタ・作業不要）③DMARCに`rua=`追加→段階強化（詳細は追記35）。HTTPヘッダ側は対応済み。
- **内部端末フラグ**：`card_logs_is_internal`のALTERは**適用済み確定✅（追記35で本番実証）**。各内部端末（三宅さん/アントプロさん）で`?test=1`を1回開く→**配布直前に`DELETE FROM card_logs`でクリーン計測開始**。
- **VOICES生声の公開可否確認**（個人LP）／ミックウェア追加写真の依頼。
- **7/30 MEET（あと3週間弱）**：登壇者数確定・当日運営・配布前クリーンアップ（テスト行/セッション消去）。
- **法人営業**：7/30前に「登壇・発信の機会」軸で紹介ベース種まき→7/30を実データ化→アプリのリード価値でアップセル（福岡アトツギはJC/YEG/事業承継塾/関電看板・紹介ベース）。詳細は本チャット後半の議論参照。
- **反応段階化（軽い共感→任意で連絡先）**：記事141→CTA13の本丸。C/Eの効果をクリーン計測で見てから着手（今は保留）。
- 未着手：admin思想ページの配線図v2更新（追記26）、Clarity導入、タイプ生成Phase B（動的OGP）。
