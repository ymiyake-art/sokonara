# ソコナラ 引き継ぎ（2026-07-02・最新）

> 新チャットはまずこれを読む。基礎・詳細履歴は `HANDOFF_2026-06-12.md`／`HANDOFF_2026-06-24.md`、各テーマは専用ドキュメント参照。
> ⚠️ **複数チャットの同時編集は禁止**（上書き事故）。アクティブは常に1つ。切替時は必ず commit & push。

## 0. 基本情報
| 項目 | 値 |
|---|---|
| コード場所(git) | `C:\Users\User\AppData\Local\Temp\sokonara\` ※**tempは消える**→消えたら `git clone https://github.com/ymiyake-art/sokonara.git` 再取得＋git config再設定。**GitHubが正本** |
| ⚠️ ユーザーが普段見るフォルダ | `C:\Users\User\1_Claudecode\sokonara\`（ドキュメント置き場）。**作業物はTemp側**。読ませたいドキュメントはここへコピーする運用 |
| GitHub | `https://github.com/ymiyake-art/sokonara.git`（main・Vercel自動デプロイ・no-cache） |
| 本番URL | `https://app.sokonara.co.jp`（`/`=個人アプリTOP／`/pf/`=課題カード／`/biz/`=法人LP／`/personal/`=個人LP／`/meet/`=イベントLP／`/admin.html`=管理） |
| git | user.email `ymiyake@sokonara.co.jp` / name `ymiyake-art`。pushはPowerShellで `Set-Location` 後、`-m`1行 |
| Supabase | anon公開キーはindex.html内。URL/SERVICE_KEY/ADMIN_PASSWORD等はVercel環境変数(設定済)。Supabase操作はPython(urllib)・結果はJSON→Readで（文字化け回避） |
| 最新コミット | 497b7bf（Well-beingマトリクス管理ページ。本番反映済み） |

### 環境の注意（ハマりどころ・全部現役）
- **プレビュー(static)は誤ルート配信の不調**あり（`.claude/launch.json` の `--directory` が旧パスを向いていた事例＝2026-07-02修正済。`sokonara-temp`→`Temp\sokonara`）。検証は**ファイルgrep＋本番プローブ**が基本。ローカルプレビューを使う時はキャッシュバスター(`?v=`)必須。
- スクショは時々タイムアウト → evalで computed値/textContent 確認で代替。
- ADMIN_PASSWORDは手元に無い → 管理API(list等)は叩けない。読取検証は anon読取可のテーブル or `/api/report` action=get 等で。**admin.htmlをローカルで開くとanonキーで本番companiesが読める**（読取のみ・2026-07-02検証で活用）。
- ⚠️**`git add -A` 厳禁**：Tempは部分的に消えることがあり、消えたファイルを「削除」として拾う事故歴あり（2026-06-26）。**コミット前に必ず `git status`→対象を `git add <path>` で個別ステージ**。削除が出ていたら `git checkout <commit>^ -- <paths>` で復元。

---

## 1. 直近の変更（2026-06-29〜07-02・全部push済み＆本番反映）

### 自己発見ピボット（57f30f6ほか）
- **AIレポートを主役に**：LINEゲート撤廃で全文表示。記事CTA→反応(p2)→レポート(p3)→興味CTA→フォームに再接続。VS3回戦に短縮。LINEは詳細版/近況の**任意導線に降格**。
- 記事直後に**紹介CTA（興味を伝える）復活**＝最重要KPIへ直行路（診断は任意の副路）。
- AI診断は**4問に復帰**（材料増）＋streamモデルを**gpt-4o-mini**へ（速度優先。薄ければ4oに戻す）。
- フォーム導線最小化：共感→即P5。P5は2アクション（経営層名ワンタップコピー→フォーム貼付）。

### モヤモヤ逆引き（829b416/88a0592/e3a0b4b/69977b6）
- ホームに「今のモヤモヤから探す」＝キャリアアンカー式8択→タグマッチ→経営層3人＋偶発枠。**variant='moya'** で card_logs 計測（admin版別A/Bに「モヤモヤ(逆引き)」行）。
- 逆引きの構造化タグ＝`companies.btags/rtags`（`companies_tags.sql`）。経営層編集で記事→自動生成(cardgen)＋手動チェック→保存。

### Career Well-being（db05749/04bbd49/ee36f0f/4262fd9/497b7bf）
- **レポート本体**：6軸レーダー（この経営層×あなたの重心）＋高中低バンド＋3ブロック＋マニフェスト＋紹介CTA。p3から「Career Well-beingを見る」で遷移。
- 6軸＝`meaning`(意味・使命)/`agency`(裁量・挑戦)/`people`(人・近さ)/`local`(地域・つながり)/`time`(時間・ゆとり)/`econ`(経済)。
- **会社スコア＝`companies.wb`（jsonb・`companies_wb.sql`）優先**、無ければ rtags/btags/tags から自動導出。
- **Phase2 比較シミュレーター**：「今の仕事と比べる」＝年収帯1タップ＋足りない軸→現状レーダー重ね＋重み付き総合「今の仕事XX→この経営層YY」。年収ダウンも正直に注記。
- AI採点：`api/feedback.js` に `wbscore`（記事→4軸を0-100推定。time/econは手動）。経営層編集に6軸スライダー＋「記事から推定(4軸)」。
- ⚠️修正済バグ：`toCompanyRow`（DB行ホワイトリスト）に btags/rtags/wb が無く**保存時に破棄されていた**→追加済（ee90f10）。新カラム追加時は toCompanyRow への追記も忘れずに。
- **初期値seed**：`companies_wb_seed.sql`（7社・AI推定4軸＋time/econ=60・フロア45/上限90）→**Supabase適用済み✅**（2026-07-02本番プローブで7社全一致を確認）。
- **📈 Well-beingマトリクス管理ページ**（admin新タブ・497b7bf）：企業×6軸の横並びテーブル。**総合列なし・フロア45/上限90クランプ**・行別「✨AI推定」＋「全社AI推定(4軸)」（記事本文=companies.bodyから直列実行）・「💾一括保存」→`companies.wb`。未保存件数表示・保存失敗時は未保存表示を維持（saveCompaniesが成否を返すよう改善）。

### 計測・レポート強化
- **form_open**（P5フォーム開封＝CV直結）イベント追加（`card_logs_form_open.sql`＋card-log ALLOWED追記）。admin共鳴ファネルの最終CVに。
- **期間フィルタ**：共鳴データに全期間/今日/7日/30日＋日付指定（started_at基準）。
- **企業別・高デザインPDFレポート**：レポートタブ「PDFレポート出力」→A4品質（ロゴ/期間/KPI/刺さった言葉・求めるもの・状況グラフ/声）→window.printでPDF。**データ源はcard_logs**（interest_formsから修正済・共鳴データと一致）・期間フィルタ連動。genReport（詳細レポート）もcard_logsに統一。

### /meet/ イベントLP（7/30 ソコナラMEET）
- 新設→全面リデザイン（明朝ディスプレイ/縦タイムライン/FAQ/固定CTAバー）。**先頭の`EV`設定オブジェクト**（日付/会場/登壇者/gallery/heroImage/videoUrl/FAQ）だけ差し替えれば再利用可。
- 実写真ヒーロー＋当日の様子ギャラリー。登壇6名（シルエットアバター・社名当日発表）。ヒーローコピー＝無圧「会社を選ぶ前に、経営層に会う。／情報収集でOK・応募や選考ではない」。
- **参加者の声＝casesテーブルから動的取得**（個人LPと単一ソース＝admin編集が両LPに反映）。

### その他UI
- **CASES写真＝フォーカルポイント方式**（3:2枠・拡大/左右/上下スライダー。adminプレビュー＋個人LP・MEET LP両方に反映）。
- **adminサイドバー固定**（position:sticky・長いレポートでもメニュー常時表示）。

---

## 2. 変わらない基盤（詳細は HANDOFF_2026-06-24.md）
- **card_logs書込は全て `/api/card-log`（service_role・upsert）経由**（anon UPDATEはRLSで0行更新のため）。新カラムは ①SQLでALTER ②`api/card-log.js` の ALLOWED追記 の順。
- **companies書込は `/api/admin-data`（service_role）経由**＋admin側 `saveCompanies`。**新カラムは `toCompanyRow` にも追記**（忘れると保存時に破棄）。
- AI診断＝`api/feedback.js`（cardfeedback/values/cardgen/articlemeta/q1rewrite/wbscore）。ストリーミング中継・JSON厳格化。記事ルールの正本はNotion。
- UTM/チャネル別計測・Vercel Web Analytics・LINE OAuth state=logId の診断復元 等は06-24版の通り。
- 既定＝人物(偶然性)版 `/pf`。旧課題版は `?v=challenge` 温存。カード文言A/B（cc_what/cc_value/cc_life・arm_events）稼働中。

### 追記（2026-07-02後半・9b80211）＝「報酬の重心」ピボット Phase 0
- **戦略決定**：診断は売りにしすぎない→呼称「**報酬の重心を知る**」／LPは増やさずFVパラメータ出し分け／企業側は「自己開示（AI補助）」立て付けで既存PDFに報酬プロファイル頁を追加予定（Phase 0.5）／TOP改修はH1/H2データで判断。
- **レポート改修**：上段＝あなたの重心のみ（経営層スコア非表示＝評価印象の排除。wbは将来値推定に内部使用）。タイトル「仕事の報酬を、どう最大化するか。」＋年収以外の報酬リード文。**フロー予報**（意味×裁量から派生・チクセントミハイ注釈）。比較＝「いま vs **共鳴後の想定**」（projectWB: cur+(comp-cur)×0.6、econは×0.85）＋Δチップ＋参考値/経済正直注記。
- **p3上下重複解消**：要約(WORKING HINT)のみ無料。詳細（ことばの読み解き＋重心ミニマトリクス`fbWbMini`＋bridge）は**LINE後**（`fbDetailLock`ロック表示・fbLineCta文言=詳細レポート特典）。
- **H1計測**：`wb_view`/`wb_compare`（`card_logs_wb_events.sql`＝**⚠️Supabase未実行**・ALLOWED追記済・admin新導線ファネルに「報酬の重心を見た」行＋H1検証行=閲覧有無別CV率）。

### 追記2（2026-07-02・66f9230）＝MEET当日ページ＋運営基盤（7/30向け）
- **`/meet/day.html`**（受付QR配布用・noindex）：同意文＋氏名登録→企業一覧（写真/キャッチ/記事リンク=本体`/?cid=`）＋**気になるチェック最大3社**（付替自由・stickyバーに残数）→**最低限アンケート**（詳しく知りたい/個別に話したい/氏名共有可否/理由タグ8種/一言）→送信後に**報酬の重心ミニレポート**（チェック企業のwb＋理由タグから導出＝イベント×指標連携）。冒頭`EV`設定（eventId/日付/maxChecks/companyIds）で次回再利用可。localStorage `sn_meet` で復元。
- **PII設計**：`meet_entries`（`meet_entries_setup.sql`＝**⚠️Supabase未実行**・RLS有効/ポリシーなし＝anon全遮断）。書込=`api/meet-log.js`（service_role・ホワイトリスト・欠落列自動ドロップ・id必須`meet_`接頭）。閲覧=`api/admin-data.js`に**listアクション追加**（ADMIN_PASSWORD認証のみ）。重心計算にPII不使用。企業共有はconsent=trueの本人のみ。
- **admin「🎪 MEET運営」タブ**：統計（登録/チェック/アンケ/同意）・**社別の興味表**（チェック/詳しく/話したい人数＋氏名列挙＝班分け用・共有不可注記）・理由タグ匿名集計（企業レポート用）・参加者一覧・イベント切替。
- 企業レポートへの反映（チェック数/接触/希望/重心分布の頁）＝Phase 0.5と合わせて後日。

### 追記3（2026-07-02夜・4260c64）＝企業増加対応・デザイン磨き・思想ページ刷新
- **SQL適用済み✅**（meet_entries＝保存/anon読取遮断/anon書込401を本番実証、wb_view/wb_compare＝ドロップなし記録を実証）。テスト行 `meet_probe_*`/`probe_wbev_*` は当日前に削除すること。
- **企業増加対応**：マトリクスに行チェックボックス＋「✨選択をAI推定」「✨未採点のみ」「✨全社」＋名前絞り込み＋未採点バッジ。day.htmlは記事なし企業（body<50字）に「記事は近日公開」表示（リンク非表示）。
- **レーダー立体化**：index（p-wb/p3ミニ）とday.htmlのradarを層状フィル＋グラデ塗り＋feDropShadow＋白頂点ドットに統一。
- **MEET LP**：時刻正＝19:00–21:30（**受付18:40〜**明記）・タイムテーブルに時刻挿入・写真を白マット＋金ヘアライン＋シャドウの額装仕上げ・実例カードにも艶。⚠️**LPは登壇6名表記のままだが運営計画は7社**——7社確定なら7人目の紹介文が必要（ユーザー判断待ち）。
- **admin思想ページ全面刷新**：報酬の重心ピボット反映／**顧客動線の全体模式図**（WEB2入口→記事→反応→HINT→重心→CV、LINE分岐、MEETレーン、企業側wb/レポート、計測点線＝card_logs列挙）／6軸指標カード／理論に計画的偶発性（クランボルツ）とフロー（チクセントミハイ）を追加／計測学習ループ（North Star・H1/H2）。
- **Clarity導入**：マーケ会社提案に賛成の方針（無料・SPA計測の補完）。導入時は day.html の氏名入力とP5フォームを **clarity mask** 対象にすること。card_logs=意思決定ファネル／Clarity=行動観察／GA4=流入、の3層で重複なし。

### 追記4（2026-07-02深夜）＝呼称の全面改名「報酬の重心」→「あなたの仕事の軸」
- ユーザーから「報酬の重心という言葉が意味わからん」→ **「報酬」「重心」はユーザー向けに使用禁止**。呼称＝「あなたの仕事の軸」（就活・転職の“軸”に馴染みあり）。
- タイトル「あなたの“仕事の軸”は、どこにあるか。」／リード「年収以外にも、仕事から得られるものはたくさんある——年収や知名度だけが、キャリアの正解じゃない」／凡例「あなたの軸」／比較「あなたの軸で重み付けした“総合の手応え”」／p3ボタン・ロック文言・LINE後ミニ「あなたの仕事の軸」／day.html「今日のあなたの“仕事の軸”」(CAREER AXIS)／adminファネル「仕事の軸レポートを見た」・H1検証行。
- 内部識別子（wb_view/wb_compare/companies.wb/openWellbeing等）は据え置き。Career Well-being はフレームワーク名として存続。

### 追記5（2026-07-03）＝MEETリンク＋LP文言のadmin編集
- **リンク**：アプリヘッダー（mode-bar）に「7/30 MEET」→/meet/（別タブ）。admin公開ページナビに「🎪 MEET LP ↗」。
- **LP文言編集**：`site_settings` テーブル新設（`site_settings_setup.sql`＝**⚠️Supabase未実行**・anon読取のみ・書込はadmin-data）。meet/index.html は起動時に `site_settings.meet_lp` をEVへマージしてから描画（`/*EV_START*/`〜`/*EV_END*/`マーカーは管理画面の読込に必須＝削除禁止）。admin「MEET運営」タブ先頭に**LP文言編集**カード：📥現在の文言を読み込む（LPからEV抽出＋保存済み編集を重ねる）→フォーム編集（ヒーロー/日時/会場/定員/締切/登壇者見出し等＋リスト系はJSON。**7人目の登壇者はspeakersのJSONに追加**）→💾保存して公開（全文スナップショット保存・JSON不正はフィールド名付きで検出）。
- ⚠️PowerShell注意：コミットメッセージに全角引用符（“ ”）を入れると引用符として解釈されコミットが壊れる。使用しないこと（2026-07-03に実際に発生）。
- ⚠️**画像差し替えの事故ポイント**：ユーザーが `1_Claudecode\sokonara\images\` の元ファイルを直接上書きしても、そのフォルダはGit/Vercel未連携のドキュメント置き場のため**本番には一切反映されない**（2026-07-03に発生・case2.png）。画像を差し替えたい時は「◯◯の写真を差し替えた」と伝えてもらい、私が `1_Claudecode` 側の新ファイルをTemp側リポジトリへコピー→commit→pushする。
- ⚠️**MEET LP文言編集の重要な仕様**：admin「LP文言編集→保存して公開」は**EV全体を`site_settings.meet_lp`へスナップショット保存**する（一部項目だけでなく全フィールド）。一度保存されると、**以後コード側でEVのデフォルト値を直しても、この保存データがある限りそちらが優先表示される**（2026-07-03に発生：コードで18:30に直したがLPは18:40のまま→保存済みスナップショットが原因と判明）。コード修正だけで直したい場合は、該当フィールドを `UPDATE site_settings SET value = jsonb_set(value, '{フィールド,パス}', '"新しい値"') WHERE key='meet_lp';` でスナップショット側も一緒に直すか、`DELETE FROM site_settings WHERE key='meet_lp';` でスナップショットを削除してコードの既定値に戻す。

### 追記6（2026-07-03昼・5e175da）＝診断導線刷新＋TOP並列化＋SNS共有
- **診断導線の再設計**：記事後ボタン「可能性を見る」→**「仕事の軸を診断」**。診断送信（4問・約1分）→**マトリクス（p-wb）即表示**（AI待ちゼロ・無料側のAIコストもゼロ）。**AIの文章診断（WORKING HINT＋詳細）はLINE特典へ完全移行**＝p-wbの`wbDetailCta`（未登録=🎁特典カード→LINE／登録済=📝詳細レポート→`openDetailReport()`→p3生成表示）。4問を残した理由＝q1_phrase（刺さった言葉＝企業レポートの源泉）の収集維持。`logResonance(aiViewed)`分離＝**q1等は診断送信時に記録・ai_viewedはAI閲覧時のみ**。LINE復帰は既存restore（p3）でそのまま動作。
- **TOP並列化**：モヤモヤボタンを白ソリッド同サイズに（両入口対等）。リード文とステップ①を中立表現（①経営層を選ぶ）に。
- **経営層カードのフォント拡大**：フレーズ19px(SP17.5)/名前16.5/会社11.5/CTA13.5/moya推薦理由13.5。
- **SNS共有**：p-wbレーダー下「📤この“軸”を画像でシェア」＝canvasで1080×1350のネイビー×ゴールド画像を端末内生成→Web Share（非対応はDL）。**画像は本人の軸のみ・経営層名/社名は載せない**。
- site_settings適用済み✅（anon読取[]・anon書込401・admin-data疎通を本番実証）。casesお名前=アルファベット表記✅・受付18:30✅・許諾注記削除✅。

### 追記7（2026-07-03午後・638cb4f）＝記事移管の準備（表示順制御）
- **背景**：ソコナラWEB（sokonara.co.jp/company）の既存記事6社をアプリへ移管予定。LP順＝日野出→アド印刷→松永製作所→トランスパック→M・ZEC→増田桐箱店（中村さん）。アプリ既存はアド印刷/M・ZECのみ＝**日野出/松永製作所/トランスパック/増田桐箱店の4社は新規登録が必要**。
- **companies.sort（表示順）**：`companies_sort.sql`✅適用済み。admin掲載企業一覧の**↑↓ボタン**で並び替え（スワップ→10刻み振り直し→保存）。index/admin/day.htmlの取得を sort.asc(nullslast)→created_at.asc に。toCompanyRowにsort追加（保存破棄防止）。
- **Notion記事の必須フォーマット**（adminのparseNotionが期待する形・0680438でNotion必須3項目に完全対応）：`# タイトル`／`## ソコナラより`（記事冒頭・①〜③の要約。**冒頭セクションでも📋内`**ソコナラより**`でも可**＝メタに回収し本文には出さない）／`## リード文`／本文`## 見出し`／**Q&Aは見出しに「インタビュー」or「伺いました」を含め、質問は`**――質問**`形式**／末尾`## 📋 プラットフォーム登録情報`（**カードタイトル候補**=3案-箇条書き→**adminでワンクリック1案選択**・**タグ**(地域,テーマ,フェーズ)・**引用フレーズ**・**Q1選択肢**(-箇条書き・Why/Who/What/Life各1＝**4件でないと解析時に警告**・ラベルは自動除去)・**こんな人と出会いたい**・**会社一言紹介**）。**人が書いたソコナラより/出会いたいが正＝AI自動生成は欠落時のみ**。`<aside>`はスキップ＝本文厳禁・メモ用。見出しに「タイトル/本文/メモ/備考/下書き/TODO/ファーストビュー」を含むとスキップ。
- **写真**：移管後に sokonara.co.jp/company からダウンロード→リポジトリ `images/` へ→photoUrl設定（ホットリンク回避）。

### 追記8（2026-07-03夕・32e578e）＝実機UXフィードバック対応
- カードフォント差し戻し（拡大で改行崩れ）＋モヤモヤ推薦カードを一覧と同じ`leaders-grid`2列に。記事ヘッダーの0%（読了率）表示を削除（updateReadのパルス処理は残置）。
- **LINE復帰の軸リセットバグ修正**：sn_restore/stateにS.moya/memoChipsが無く復元後にレーダーが変わっていた→追加。**window.MOYA公開**（IIFE内部限定でモヤモヤラベルが全画面表示されない既存バグ）。
- 軸ラベルをフル表記（意味・使命／裁量・挑戦／人・近さ／地域・つながり／時間・ゆとり／経済（年収））＝index/day/共有画像。viewBox拡張ではみ出しなし確認済み。
- **「この軸の導き方」根拠表示**を仕事の軸レポート冒頭に追加（◯◯さんへの反応＋刺さった言葉＋モヤモヤ→「比べてみるとさらにあなた仕様に」）＝いきなり数字が出る不信感への当座の対処。
- meet LPのOGPをイベント版に（og:title/descから「福岡の夜」撤去・og:image=meet-hero.jpg・twitter:card追加）。曜日括弧の正規化（(木)と入力しても((木))にならない）。
- **設計課題（ユーザーと議論中）**：①自由記述の記入タイミング（LINE特典直前へ移す案）②「○○タイプ」命名＋キャラ＋X/Instagramワンクリックシェア（動的OG画像 /api/og が本命。タイプ名はシェア面限定・レポート本体は軸表示の2層で「診断と名乗らない」方針と両立）。
- **③叩き台は実装済み（e77cd69）**：思想ページに「6軸×理論×導出元の対応表」と「診断の配線表」（Q1〜Q3が軸マトリクス未接続＝❌を可視化）。改善提案＝Q2/Q3→軸の静的マッピング／モヤモヤをAI文章診断へ／自由記述をLINE特典直前へ。**配線の事実**：軸マトリクス=経営層wb28%+モヤモヤ+22のみ／AI文章=Q1〜Q4+自由記述+経営層文脈（モヤモヤ未接続）。

### 追記9（2026-07-03夜・1713fd5）＝設問刷新（4問＝感情/価値観/事実/意図）
- **設問設計の原則確定**：4問は種類を変える——Q1感情（記事への反応）／Q2価値観（求めるもの）／Q3事実（年収帯・忙しさ）／Q4意図（聞きたいこと）。「欲しい×足りない」の二度聞きはユーザーに見透かされるため不採用（ユーザー指摘）。
- **Q2刷新**：6軸対応の6択・**最大3つ**（優先順位付け＝アンカー顕在化）→選択軸に**+12**（`Q2_AX`）。モヤモヤ+22と併せ最大+34。
- **Q3差し替え**：年収帯（s3〜s8・6段階）＋忙しさ（b1多忙/b2ふつう/b3ゆとり）の**事実2タップ**。`S.sal/S.busy`＋AI互換で`S.about`に言語化。**比較シミュレーターがゼロタップ化**＝p-wb表示時に自動実行（年収→econ現状・忙しさ→time現状35/55/75）。比較レーダーは結果ボックス内に分離＝上段の重心レーダーと同時表示。LINE復元にsal/busy/moya対応済み。
- **思想ページ**：「原点」カード新設（都市部の“なんでだろう”層／年収は報酬の一要素／「下がるけど、これが上がる」の見える化が使命／創業者の転身＝意味・人・フロー・地域が上がり経済が下がる、が6軸の原体験）。配線表をQ2/Q3⭕に更新・設問原則を記録。
- 残る改善候補：モヤモヤをAI文章診断へ渡す／自由記述をLINE特典直前へ移動（①未実装）／○○タイプ＋ワンクリックシェア（②Phase A/B未実装）。

## 3. TODO / 意思決定待ち
- [ ] **記事移管**：4社の新規登録＋6社の記事貼付（Notion経由）→タグ/wb推定→写真移管→↑↓でLP順に並べ替え
- [x] ~~Supabaseで `site_settings_setup.sql` を実行~~ ✅（2026-07-03適用・本番実証済み）
- [ ] **7/30 LPの登壇者数**：現在6名表記。7社確定ならadminのLP文言編集→speakers JSONに7人目を追加（コード修正不要に）
- [ ] **Clarityタグ導入**（プロジェクトID入手後。index/pf/meet系に挿入＋PIIマスク設定）
- [ ] **計測テスト行の削除**（`probe_*`/`meet_probe_*`/`apicheck_*`）＝配布・当日前に
- [ ] Phase 0.5：企業別PDFに「報酬プロファイル」頁（6軸レーダー＋惹きつけている重心の分布）
- [ ] Phase 1：2〜4週データでTOPの主役を判断（H1=重心レポートのCV押上げ／H2=入口別）→FVサブコピー「年収だけが、仕事の報酬じゃない。」A/B
- [ ] admin画面の整理（方向性が見えた時点で・ユーザー指示）
- [ ] **全セッション消去**（計測リセット。テスト行 `probe_*`/`apicheck_*` 等一掃→クリーン計測開始）※配布前に
- [ ] マトリクスページの実機確認（admin→📈Well-beingマトリクス→AI推定→一括保存。本番ADMIN_PASSWORDが必要なためユーザー操作）
- [ ] Well-beingの time/econ を実態値に手動調整（現状は全社60の初期値のまま）
- [ ] **biz LP を偶然性訴求へ改訂**（personal/index は改訂済）
- [ ] **7/30 MEET機能**（企業一覧＋簡単アンケート＋登録者の班分け＝自動振分→手動調整）
- [ ] FVコピーA/B（偶然性 vs 具体ベネフィット）は後日

## 4. 関連ドキュメント（リポジトリ内）
- `HANDOFF_2026-06-24.md`（前回版：新導線・計測大改修・カードA/B の詳細）
- `HANDOFF_2026-06-12.md`（基礎・SQL実行状況・セキュリティ）
- `MARKETING_BRIEF_2026-06-29.md`（ユーザーフォルダ側）／`MARKETING_BRIEF_2026-06-24.md`
- `CARD_COPY_RULES_2026-06-24.md`／`DESIGN_v2_challenge_cards.md`／`BRIEF_biz_lp.md`
- SQL実行状況：lockdown_s1/s2✅ biz_inquiries✅ card_logs_kpi/utm/variant/form_open✅ cases_setup✅ companies_card_copy✅ companies_tags✅ companies_wb✅ **companies_wb_seed✅（2026-07-02確認）**

### 追記10（2026-07-05）
- MEET LPナビロゴ→アプリを UTM付き（meet_lp/lp/lp_meet0730/nav_logo）＋別タブに（2642dcb）。LP経由のアプリ流入はadminチャネル別で meet_lp として計測。
- LP見出しフォントは**明朝（Shippori Mincho）維持で確定**（ユーザー判断）。本文は元からNoto Sans JP。Noto統一案・Zen Kaku統一案は不採用＝編集記事トーンを差別化資産として残す。

### 追記11（2026-07-05・72510a1）＝軸リセット根治ほか実機FB第2弾
- **軸リセットの真因**：LINE復帰でlocalStorageが失われる経路（LINEアプリ内ブラウザ等）は stateパラメータから復元するが、**stateにQ2が未搭載**だった（Q2が軸+12に効くようになった1713fd5以降に顕在化）。→ Q2を短縮コード化（Q2_CODE: mn/ag/pe/lo/ti/ec）して**lg経路含む両ペイロードに搭載**・復元時に復号。localStorage全消失シナリオで軸レーダー座標の完全一致を検証済み。
- **レーダー出現アニメ**：`.wbGrow`（メイン多角形＋頂点ドット、scale .45→1・0.8s）→`.wbGrowSec`（現状線・0.3s遅延）。prefers-reduced-motion対応。index（p-wb/p3ミニ/比較）とday.html両方。
- マニフェスト「年収以外にも、」で改行。
- **足りないチップの挙動修正**（ユーザー指摘：両方動いて意味不明）：将来線（共鳴後の想定）は**事実ベース（年収帯＋忙しさ＋中立66）から固定**。チップは**現状線のみ**下げる＝「この環境で埋まる幅」が見える。チップは4軸（意味/裁量/人/地域）に削減し「時間と収入はQ3の回答を使用」と注記。

### 追記12（2026-07-05・1c36937）＝実機FB第3弾（再入口・前提可視化・重心の透明化）
- **診断の再入口**：スワイプで戻るとレポートに戻れない問題→ sn_fb_cache にmoya/sal/busyを追加し診断送信時に保存・起動時に24hハイドレート・**TOP（p-list）ヒーロー直下に「📋診断済み」バー**（あなたの仕事の軸を見る／LINE済みなら詳細レポートも）。goScreen(p-list)フックで表示制御（renderDiagReentry）。
- **比較の前提可視化**：「Q3の回答」という表現に依存せず、比較結果内に「前提：年収帯X・忙しさY」を明記＋比較カードに**前提①年収セレクト／前提②忙しさチップ**（変更で即再計算・キャッシュも更新）。
- **重心の経済・時間の透明化**：企業wbの影響を価値4軸（意味/裁量/人/地域）に限定。**時間・収入はQ2で選んだ時だけ+12＝選ばなければ50（こだわり控えめ）**。根拠文に「時間・収入は求めるもので選んだときだけ高くなります」を明記。「なぜこの点か」が説明可能に。
- ユーザー所感「まだまだ見直す余地あり」＝仕事の軸まわりは継続改善フェーズ。

### 追記13（2026-07-05・a583ea7）＝自由記述移設＋タイプ命名＋シェアPhase A
- **自由記述をQ4から詳細レポート特典カード内へ移設**（wbFree・oninputでS.free同期・書き足して再度開くと再生成）。診断は実質3問＋任意チップに軽量化。「書く→すぐAIが読む」の隣接で納得感。
- **○○タイプ（15種）**：上位2軸の組合せ→`TYPE_NAMES`（wb IIFE内・書き換えだけで命名差し替え可）。**シェア面限定の遊び**でレポート本体は軸表示のまま（「診断と名乗らない」方針と2層両立）。p-wbにタイプバッジ表示。
- **ワンクリックシェア（Phase A）**：𝕏（twitter intent）／LINE（line.me/R/share）ボタン＋既存の画像保存。テキスト＝「私の仕事の軸は『◯◯』タイプでした。年収や知名度だけが、キャリアの正解じゃない。#ソコナラ」＋URL。共有画像にもタイプ名を金字で描画。
- **Phase B（未着手・本命）**：結果ごとの共有URL＋Vercel /api/og で動的OGP（レーダー＋タイプ名カード）→Xで自分の結果が展開される。記事OGP動的化と同じ技術で一石二鳥。
