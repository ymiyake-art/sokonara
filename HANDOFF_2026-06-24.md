# ソコナラ 引き継ぎ（2026-06-24・最新）

> 新チャットはまずこれを読む。基礎・詳細履歴は `HANDOFF_2026-06-12.md`、各テーマは専用ドキュメント参照。
> ⚠️ **複数チャットの同時編集は禁止**（上書き事故）。アクティブは常に1つ。切替時は必ず commit & push。

## 0. 基本情報
| 項目 | 値 |
|---|---|
| コード場所(git) | `C:\Users\User\AppData\Local\Temp\sokonara\` ※**tempは消える**→消えたら `git clone` 再取得＋git config再設定。GitHubが正本 |
| ⚠️ ユーザーが普段見るフォルダ | `C:\Users\User\1_Claudecode\sokonara\`（`lp-personal.html`等のみ）。**私の作業物はTemp側**。読みたいドキュメントはここへコピーする運用 |
| GitHub | `https://github.com/ymiyake-art/sokonara.git`（main・Vercel自動デプロイ・no-cache） |
| 本番URL | `https://app.sokonara.co.jp`（`/`=個人アプリTOP／`/pf/`=課題カード／`/biz/`=法人LP／`/personal/`=個人LP／`/admin.html`=管理） |
| git | user.email `ymiyake@sokonara.co.jp` / name `ymiyake-art`。pushはPowerShellで `Set-Location` 後、`-m`1行 |
| Supabase | anon公開キーはindex.html内。URL/SERVICE_KEY/ADMIN_PASSWORD等はVercel環境変数(設定済)。Supabase操作はPython(urllib)・結果はJSON→Readで（文字化け回避） |
| 最新コミット | 74a5f7c（新導線・本番反映済み。＋本handoff更新） |

### 環境の注意（ハマりどころ）
- **プレビュー(static)は誤ルート(`1_Claudecode`)を配信する不調**あり。サブディレクトリindexを404にすることも。→ 検証はファイルgrep＋**本番プローブ**で行う。
- スクショは時々タイムアウト → evalで computed値/textContent 確認で代替。
- ADMIN_PASSWORDは手元に無い → 管理API(list等)は叩けない。読取検証は `/api/report` action=get（パスワード不要・id指定）等で。

---

## 1. 計測（★今セッションで大改修・最重要）
- **card_logsへの書き込みは全て service_role API `/api/card-log`（upsert）経由**。
  - 理由：**anonのUPDATEがRLSで「204だが0行更新」**になる不具合があり、マイルストーン(スワイプ/到達/記事/AI/LINE/面談)も診断保存も全滅していた。APIでバイパスし**本番実証済み**。
  - 呼び出し元：pf=`_saveLog/logStart/logUpdate`、index=`patchCardLog/saveDiagForRestore`。
  - → 旧SQL `card_logs_fix_update_policy.sql` は**不要**（APIで解決済み。RLSに依存しない）。
- ファネル（admin「📊 共鳴データ」）：カード開始→3枚以上スワイプ→診断到達→記事→AI診断→LINE登録→面談CTA。**チャネル別(utm_source)**も。`started=false`の復元専用行は集計から除外。
- **Vercel Web Analytics** 導入済(index/pf/biz)。ダッシュボードで有効化済。
- **UTM**：utm_source/medium/campaign/content を card_logs に記録。入口`/`でも `/pf` へ引き継ぐ（localStorage `sn_utm` ＋ goCards付与）。**社内テストは `utm_source=admin`** でタグ付け→集計から除外して比較。
- ⚠️ **配布前TODO**：admin「全セッション消去」で古い壊れデータ＋テスト行(`apicheck_*`/`restore_test_*`/`vs_*`)を一掃→クリーン計測開始。

## 2. AI診断（`api/feedback.js`）
- 2タイプ：`cardfeedback`(課題カード経由)／`values`(直接導線)。両方 **gpt-4o＋ストリーミング中継**（Edge関数の504回避）。index.htmlは**逐次表示(タイピング)**（`partialJsonStr`）。
- ルール：JSON厳格化／長さ目安12〜18行／おすすめ経営層と別社カードの取り違え防止／**自由記述・本人の言葉を必ず反映**（一般論禁止）。
- 記事ルールの**正本はNotion**（リッチ）。app の cardgen/articlemeta は「Notion準拠の下書き器」。
- **LINE登録前後で診断一致**：生成時にcard_logsへ ai_summary/message/bridge を保存し、OAuth `state` に logId(または vsId) を載せ、復帰時 `/api/report` action=get で**同一診断を復元（再生成しない）**。直接導線(values)も `started=false` 行で対応。
- 自由記述欄に「スマホ音声入力が楽」ヒント追加済。
- CTA文言：LINEは「保存」→「続きを読む／経営層とつながる」に修正済。

## 3. 戦略転換（★新導線=本番反映済み／LP偶然性は未着手）
詳細 → `MARKETING_BRIEF_2026-06-24.md`（マーケ共有用・確定/仮説/計測込み）。
- ✅ **新導線=本番反映済み**（2026-06-24・commit `3f70d06`＋`74a5f7c`）。実装計画は `crystalline-strolling-trinket.md`（~/.claude/plans）。
  - **記事(index.html p1)のCTA**：主＝「話してみる →」(`openEmpathyForm` 直行＝メインCV)／従＝「他の経営者を見る」(`goCards`→/pf 回遊)。**診断ボタンは記事から撤去**（CV食い合い回避）。
  - **フォームのLINE関門を撤去**（`openEmpathyForm` 冒頭の `isLineRegistered` ゲート削除。WEBCASで氏名・メール取得＝連絡可）。
  - **診断＝LINE特典**：p5サンクスに「LINEで自己分析」CTA(`#p5LineBonus`・未登録時のみ・`beginLineLogin`)。p3の診断詳細LINE関門は従来通り維持。
  - **カードA/B（pf）**：`?v=person`=人物版＝**経営層マッチ（運命マッチ）**。「経営層一覧」の `.leader-card` 構造を流用（フォントは見出しと同じ`Zen Kaku Gothic New 900`）。`.vsArena`内に**2枚横並び＋中央VSメダル＋進捗バー＋淡いグロー背景**、ホバーで浮く/相手が薄くなる、登場アニメ。**PCは没入レイアウト**（`body.vmode` で `.wrap` を拡幅＋縦中央寄せ、カード写真360px等）。気になる方をタップ→**選んだ側は同位置で残り相手側だけ入替**を5回→勝った経営層の記事へ。「勝ち残り」ラベルは無し。`renderVersus`/`paintVersus`/`pickVersus`/`finishVersus`/`openLeaderArticle`、プール=`companies`(7社)、`VS_ROUNDS=min(5,社数-1)`。既定/`v=challenge`=従来のスワイプ課題版（`renderChallengeStack`）。`SN_VARIANT`は`localStorage(sn_variant)`で回遊維持、`index.html goCards`も`v`引継ぎ。
  - **計測**：`logStart` が `variant` を `card_logs.variant` に記録（`card_logs_variant.sql`=ALTER 適用済✅）。admin「📊共鳴データ(v2)」を**新導線ファネル(開始→経営者を選んだ→記事→面談CTA=CV)＋補助指標(AI診断/LINE件数＝先方FB用に継続取得)＋版別A/B(person/challenge)**に改修済(`renderResonance`)。さらに**カード文言3アームA/B**＝`companies.cc_what/cc_value/cc_life`（admin企業編集の「カードA/B文言」で赤入れ可）／pfが`SN_ARM`をランダム選択しカード前面に出し分け＋`card_logs.copy_arm`記録／共鳴データに「カード文言A/B(What/Why+Who/Life)」ビュー追加。⚠️**要SQL：`companies_card_copy.sql`（cc_3列＋card_logs.copy_arm＋確定文言の投入）未実行**。pfは列未投入でも catchphrase にフォールバックして動作。**⚠️gotcha**：card-log.js はホワイトリスト式で、列未追加のまま送ると以前は行全体が400で全損→**現在は PGRST204 を検出して欠落列だけ外し再試行**（本体は必ず保存。`{ok:true,dropped:[...]}` を返す）。新カラム追加時は ①SQLでALTER ②`api/card-log.js` の `ALLOWED` 追記 の順で。
  - **★既定＝人物(偶然性)版に確定**（2026-06-24）。`/pf` も `/pf?v=person` も新版。**旧課題版は `?v=challenge` で温存**（撤去せず・比較用）。課題-vs-人物の構造A/Bはやめ、**人物版の中でカード文言A/B（A課題 vs Bパーパス）**を回す方針。
  - **偶然性TOP（intro）追加**：`#screenTop` を計画的偶発性の思想ページに刷新（「キャリアの転機は、まだ出会っていない人にある。」）→ `init` で person 時のみ表示→CTA `startCards()` でマッチへ。challenge は従来通り即開始。
  - **ヘッダー刷新**：pfhead「課題から探す」→「経営者と出会う」＋「経営層一覧」。index `/` の入口ボタン/コピーも「課題を選ぶ」→「経営者と出会う／“偶然”つながる」に更新（**ただし index ホーム全体の偶然性改訂は未完＝下記TODO**）。
  - A/B URL例：広告→ `https://app.sokonara.co.jp/pf/`（=person既定）。文言A/Bは別途。`?v=challenge` で旧版確認。
- **差別化＝「偶然性」（計画的偶発性理論）**。LP `/personal/` と index `/` ホームの文言を「課題ありき」→偶然性へ改訂（**FVコピーはA/B**：偶然性 vs 具体ベネフィット）。←**一部のみ（pf TOP/入口ボタン）反映。LPとホーム本体は未着手**。
- **カード文言**：方針=**B（パーパス起点）に確定**。`CARD_COPY_RULES_2026-06-24.md` のB列＋中嶋(M・ZEC)を `companies.catchphrase`（一覧と共用）へ admin で適用するのが次の一手（DB書込=service_key必要なのでコードからは未反映）。B案ドラフトは下記。

## 4. 意思決定待ち / TODO
- [x] ~~**新導線実装**~~（人物カード/記事CTA改修/診断LINE特典化/LINE関門撤去）＝**本番反映済み**(2026-06-24)。実機A/Bで効果検証はこれから。
- [ ] **全セッション消去**（計測リセット）。※今回のプローブ行 `probe_variant_*`/`probe_fix_*`/`probe_sqlok_*`（started:true）も一掃対象。
- [ ] **LP `/personal/`（＋biz）を偶然性訴求へ改訂**（pf TOP・index `/` ホーム框組み/課題文言・記事遷移フラッシュ・スマホ余白は反映済。残=personal LP本体/biz LP＝コピーは細かく協働レビュー予定）
- [ ] **カード文言＝B(パーパス起点)を admin で `companies.catchphrase` に適用**（方針確定済。ドラフトは `CARD_COPY_RULES_2026-06-24.md` 末尾）。catchphraseは一覧/マッチ/記事ダイジェスト共用＝全面に反映される点に注意。
- [ ] 新導線の **実機ブラウザ検証**（`/pf/` 偶然性TOP→マッチ→記事→フォーム、p5のLINE特典CTA、PC/スマホ）。プレビュー誤ルートのため本番URLで。
- [ ] 6/30 マーケ打合せ（アント・尼様）／7/30 MEET（50〜60名・登壇7〜8社）

## 5. 関連ドキュメント（リポジトリ内）
- `HANDOFF_2026-06-12.md`（基礎・詳細履歴。SQL実行状況・セキュリティ等）
- `MARKETING_BRIEF_2026-06-24.md`（マーケ共有：導線/計測/UTM規約/役割分担）
- `CONSULT_BRIEF_2026-06-23.md`（コンサル説明用サマリ）
- `CARD_COPY_RULES_2026-06-24.md`（カード文言ルール＋A/B候補）
- `DESIGN_v2_challenge_cards.md`（v2設計）／`BRIEF_biz_lp.md`（法人LP方針）
- SQL：`card_logs_setup.sql`/`_kpi_upgrade`/`_utm`/`biz_inquiries_setup`/`lockdown_s1`/`lockdown_s2` 等
  - 実行状況(本番プローブ確認)：lockdown_s1/s2＝済✅、biz_inquiries＝済✅、card_logs_kpi/utm＝済✅。card_logsのUPDATEはAPI経由に変更済でRLS依存解消。
